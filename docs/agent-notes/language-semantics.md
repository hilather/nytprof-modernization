# Language semantics gotchas (Perl & Rust) — light ledger

**Status:** living light index — expand under [`details/`](https://github.com/hilather/nytprof-modernization/blob/main/docs/agent-notes/details/) when a row needs more than one line  
**Duty:** [`AGENTS.md`](https://github.com/hilather/nytprof-modernization/blob/main/AGENTS.md) §6 — save corrected **Perl** and **Rust** misunderstandings **automatically**  
**Not:** a language tutorial; not a substitute for oracle fixtures or COMPAT contracts

Record **language- or runtime-specific rules** that an agent got wrong (or nearly wrong) while working this repo — especially NYTProf / XS / dual-engine edges. Prefer pointers into contracts, fixtures, or oracle code over restating whole manuals. Keep rows **light**; open a detail file only when the correction needs more than one line.

| Date | Lang | Topic | Wrong assumption | Correct rule / pointer | Open? | Detail |
|------|------|-------|------------------|------------------------|-------|--------|
| 2026-08-17 | perl | `slowops=full` exclusive | E4 full table is 6.15 `pp_subcall_profiler(is_slowop=1)` savestack; parent excl matches pin | **Thin deviation (accepted).** `pp_slowop_profiler` is orig + `nytp_emit_*` + `product_credit_child_excl`. A slowop that re-enters Perl (`sort` / backtick / `(?{ })`) already credits wrap children, then the slowop wall (which includes those children) is credited again. `product_in_slowop` skips nested slowops; it is not a nest. Do **not** claim 6.15 exclusive on `=full`. Default `slowops=2` / g08 / g09 unchanged. [`PROVENANCE.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/graft/PROVENANCE.md) E4 deltas. | yes | — |
| 2026-08-17 | perl | di02 `sub_entry_events=27` | Live product wrap/`entersub=1` `calls=2` is 27 after INIT | **27 is oracle `start=begin`** (BEGIN/`import`/`_bits`). Product emit-after-INIT wrap on this host is **21** (15 leaf + 3 mid + 2 `CORE:match` + 1 `CORE:print`) — already true on E0. E1a opcode matches wrap **21**. Do not enable emit at `file=` to fake 27 (KD-E17). Recount is a later honesty PR with dual-path evidence. | yes | — |
| 2026-08-17 | perl | product `use_db_sub=1` | Product `use_db_sub=1` is 6.15 stmt `DB::DB` + skip NEXTSTATE while opcode `ENTERSUB` stays on | **Fork (KD-E11):** product `use_db_sub=1` is a synonym for **`wrap=1`** (Perl `DB::sub` wrap escape). **Not** 6.15 `init_profiler` `if (opt_use_db_sub)`. Do not copy that block as the E1 switch. After E1b, default omit is opcode `OP_ENTERSUB`; wrap is `wrap=1` / `use_db_sub=1` / `entersub=0` (`wrap=1` wins). Runbook: [`R1_PREVIEW_OPERATOR_RUNBOOK.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/R1_PREVIEW_OPERATOR_RUNBOOK.md). Plan: [`DI03_OPCODE_ENTERSUB_ATTACH_v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/plan/DI03_OPCODE_ENTERSUB_ATTACH_v0.md). | no | — |
| 2026-08-17 | perl | wrap site pin from XSUB | `product_db_pin_cop` from `wrap_push` equals Perl `caller(0)` in `DB::sub` | First package-DB frame is the **XSUB** (`blk_oldcop` = NYTProfM.pm). Skip **non-`CXt_SUB`** frames (returning on them keeps `PL_curcop` = NYTProfM.pm). Skip `DB` stash CVs; keep the last `blk_oldcop` whose file is not NYTProfM.pm. Smoke [`g16_wrap_enter_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/g16_wrap_enter_smoke.sh) (`SITE_OK`). | no | — |
| 2026-08-17 | perl | `$DB::single` vs TIME_LINE | G04 still needs `$DB::single=1` or report fail-closes on zero TIME_LINE | **PR-15:** default `stmts=1` emits TIME_LINE from C `OP_DBSTATE`. `INIT` leaves `$DB::single=0` so Perl `DB::DB` is not entered. `$DB::single=1` is only the fallback when the C hook is not installed. Do **not** hook NEXTSTATE/UNSTACK on this path (`blocks=1` 780). Smoke [`g15_dbstate_timeline_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/g15_dbstate_timeline_smoke.sh). | no | — |
| 2026-08-17 | perl | `PL_ppaddr` vs `op_ppaddr` | Swap `PL_ppaddr[OP_DBSTATE]` in `INIT` and every compiled `dbstate` will emit | Each COP **copies** `op_ppaddr` at compile. A late `PL_ppaddr` swap only affects ops compiled after the swap (`eval` / later `require`). Install the hook at `file=` enable; keep emit **inactive** until `INIT` so `BEGIN`/`use` do not write TIME_LINE. | no | — |
| 2026-08-17 | perl | HTML “Profile of …” file | Hottest `.pl` is the user script; `/lib/perl` matches EL8 INC | 6.15 writes `ATTRIBUTE application=$0`. Product did not, so HTML used `primary_workload_fid`. EL8 `Config_heavy.pl` lives under `/usr/lib64/perl5/` — not `/lib/perl` — so it won as a “non-@INC .pl”. Emit `$0`; treat `Config_heavy.pl` / `/lib64/perl` as INC. | no | — |
| 2026-08-17 | perl | exclusive vs child incl | `excl = incl − Σ child exclusive` (KD-E hash-stack credit) | Exclusive is **incl − Σ child inclusive**. Crediting child *excl* leaks grandchildren into the parent (`lab_run` excl ≈ YAML). Leaves (MATCH) have excl=incl so 2-level / g09 still pass. Smoke [`g14_nested_excl_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/g14_nested_excl_smoke.sh). | no | — |
| 2026-08-17 | perl | `caller` + `eval { &$raw }` | Wrapping workload subs in `eval` is invisible to app `caller()` because the wrap is `package DB` | `caller` skips **package-DB sub** frames, not `CXt_EVAL`. `eval { &$raw }` at NYTProfM.pm ~308 made every logger `caller(0)` report `NYTProfM.pm`. Call `&$raw` directly; `DESTROY` still emits `SUB_RETURN` if the callee dies. Smoke [`g13_logger_caller_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/g13_logger_caller_smoke.sh). | no | — |
| 2026-08-17 | perl | `Memoize::memoize` + `caller` | Wrapping `Memoize::memoize` is fine; the named sub is looked up in the user's package | `memoize('fn')` does `my $uppack = caller` then requires `&{$uppack.'::'.$fn}`. Wrap ⇒ `caller` is `DB` ⇒ `Cannot operate on nonexistent function \`fn'` even though `main::fn` exists (works without `-d`). Goto `Memoize::`. Smoke [`g12_memoize_caller_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/g12_memoize_caller_smoke.sh). | no | — |
| 2026-08-17 | perl | `GvCV` + stash walk | `isGV(sv)` is enough before `GvCV((GV*)sv)` | `GvCV` is `((CV*)GvGP(gv)->gp_cv)` on 5.26 and 5.38 — a GP-less `SVt_PVGV` (`isGV` true, `isGV_with_GP` false) is a **SEGV**. `Package::Stash` / `namespace::clean` leave those slots. Walk via `isGV_with_GP` (or `product_stash_val_cv`). Smoke [`g11_nodebug_stash_nogp_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/g11_nodebug_stash_nogp_smoke.sh). | no | — |
| 2026-08-17 | perl | `XSLoader::load` + `caller` | Wrapping `XSLoader::load` / `DynaLoader::bootstrap` is fine | `XSLoader::load()` with no args uses `caller` as the XS module name. Wrap ⇒ `Can't locate loadable object for module DB` (Rex `use Fcntl` / Storable). Goto `XSLoader::` and `DynaLoader::`. | no | — |
| 2026-08-17 | perl | inherited `import` name in `$DB::sub` | `$DB::sub` for `use Rex::Shared::Var` is `Exporter::import`, already on the goto list | Inherited methods are named **`Child::import`** (`Rex::Shared::Var::import`). Wrapping that exports into `DB`, so `share qw(@SUMMARY)` in `Rex::TaskList::Base` is a syntax error and `->new` is missing. Goto every `::import`/`::unimport`, not just `Exporter::`. Rex lab: [`complex_app_docker_profile.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/field/complex_app_docker_profile.sh). | no | — |
| 2026-08-17 | perl | `CORE::GLOBAL::require` / `DB::sub` + `%^H` | Wrapping `CORE::require` *or* `DB::sub` (`goto &$raw`) during `on_scope_end` is harmless to DateTime | Both break compile-time `%^H` / Variable::Magic. `use namespace::autoclean` in DateTime::Duration dies in `B::Hooks::EndOfScope::XS` line 39: `Can't use string ("#pod\n") as an ARRAY ref`. Do **not** install `CORE::GLOBAL::require`. Do **not** defer `$^P` 0x01 to `INIT` (subs compiled without `PERLDBf_SUB` never call `DB::sub` — g04 15/3/15 → 0). Preload BHES/Variable::Magic/namespace::* and `CvNODEBUG` their CVs **before** 0x01. Smoke [`g10_datetime_hints_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/g10_datetime_hints_smoke.sh). | no | — |
| 2026-08-15 | both | oracle vs native HTML times | A 25s 6.15 site vs a 60s native site is the same comparison; or same 25s wall should yield the same exclusive seconds | Inputs must match (script + seconds + corpus). Even then 6.15 is heavier: paired 25s/2-file run was oracle **1151** passes / match **4.47s** vs native **3788** passes / match **15.5s** (scales with work). Oracle “9s of 24s” is `profiler_active`; native summary is Σ statement ticks. Tokenize excl must be a **remainder** (137ms / 470ms), not ≈ match. See `AGENTS.md` §5 interpreting table. | no | — |
| 2026-08-15 | perl | slowop excl vs parent `DB::sub` | Measuring MATCH/PRINT in C is enough for parent exclusive | Slowops emit their own `SUB_RETURN` but do not change the wrapping Perl frame. Parent excl = incl − children only after `product_pending_child_excl` is absorbed via `DB::take_pending_child_excl` on push/pop. `emit_sub_callers(1,1,…)` is a stub, not CopLINE — capture `caller(0)` at entry. | no | — |
| 2026-08-14 | both | default-calls1 SUB_CALLERS units | HTML `format_time_cell` can always divide model incl/excl by `ticks_per_sec` | `SUB_RETURN` totals on this fixture are **integer ticks** (leaf excl 524). `SUB_CALLERS` / `call_edges` NVs are already **seconds-scale** (mid→leaf excl `0.0000524` = 524/1e7). Blind divide → `0s` on every edge. HTML treats `|v|<1` whose scaled value underflows as already-seconds (OI-003-02 still open). Text/CSV/JSON stay raw `format_ticks`. | yes | — |
| 2026-08-14 | perl | `PERLDBf_SAVESRC` vs `$^P \|= 0x400` | `$^P \|= 0x400` is the portable way to keep `@{'_<$file'}` for `SRC_LINE` | Use **`PL_perldb \|= PERLDBf_SAVESRC \| PERLDBf_SAVESRC_NOSUBS`** (6.15 `NYTProf.xs` ~3177–3179). On 5.38 those are `0x400` / `0x800`; `0x400` alone is also `PERLDBf_SAVESRC_INVALID` (eval-source) on some perls. Product `DB::set_savesrc` applies the macros at `file=`; `savesrc=0` skips file `SRC_LINE`. | no | — |
| 2026-08-14 | perl | product `DB::sub` + `goto` | `perl -d:NYTProfM` can profile any script that `use`s Getopt::Long / Time::HiRes / Exporter | Do **not** set `$DB::single=1` at `file=` enable (`pp_dbstate` would run `DB::DB` during `use`/`BEGIN`). Keep `$^P \|= 0x01\|0x02\|0x20`; set `$DB::single=1` in `INIT`. Before INIT, `DB::sub` `goto &$raw` for every call. After, Exporter / Getopt / `vars` / `constant` / `overload` stay `goto` (not `&$raw` wrap): `goto &heavy_*` becomes `heavy_(eval)`, and `vars`/`constant` `caller` would see `DB` so `use vars qw($VERSION)` / `use constant CTL_*` fail under strict (Getopt::Long). Normal workload subs keep the hash-stack `&$raw` wrap (g04 15/3/15). Smoke [`g07_getopt_compile_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/g07_getopt_compile_smoke.sh). Host Perl 5.38 reproduced the same `$VERSION` abort as EL8 5.26. | no | — |
| 2026-08-13 | perl | for-modifier + dbstate | `$x++ for 1..50` with `$^P` / `$DB::single` fires `DB::DB` 52× (or `nextstate` per iter if `PERLDBf_NOOPT`) | Optree is one `dbstate` then `enteriter`/`iter`/`preinc`/`unstack`/`leaveloop`. 780 = 15×(1 dbstate + 50 unstack + 1 post-unstack replay). 6.15 uses UNSTACK leave + previous-statement write. DI-01 slice emits `TIME_BLOCK` on those ops only. | no | [`di01-spike`](https://github.com/hilather/nytprof-modernization/blob/main/fixtures/v5/product-attach/di01-spike/) |
| 2026-08-07 | both | oracle `PERL5LIB` | Putting `crates/` or native tree on oracle `PERL5LIB` is fine for “convenience” | **Never** put `crates/` on oracle `PERL5LIB` — isolates 6.15 oracle; see `AGENTS.md` + residual matrix isolation rule | no | — |
| 2026-08-07 | rust | `total_events` on JSON | ProfileModel event counter equals advertised `total_events` (e.g. 2473) | Advertised default-calls1 **`total_events` is 2474** (`model.total_events + 1` / stream account as frozen by JSON-TOTAL-EVENTS-MVP + residual matrix) | no | — |
| 2026-08-07 | rust | v6 chunk payload | Default `parse_chunk_frame` should inflate ZLIB and verify CRC | Default parse is **non-inflating / non-CRC-verify**; inflate and CRC are **explicit** helpers (`FMT-V6-PAYLOAD-ZLIB/ZSTD/LZ4-*`, `FMT-V6-CRC-*`) | no | — |
| 2026-08-07 | rust | v6 LZ4 wire | LZ4 frame with embedded size is the MVP wire | MVP is **raw LZ4 block**; size is chunk `uncompressed_len` only (`FMT-V6-PAYLOAD-LZ4-*`) | no | — |
| 2026-08-07 | perl | SUB_ENTRY multiplicity | Every profile has SUB_ENTRY events | `calls=1` default-calls1 → **0**; `calls=2` calls2-default → **27** (multiplicity only; not full call-stack freeze) | no | — |
| 2026-08-11 | both | v5 NV vs v6 u64 SUB_RETURN times | Dual-sink fractional wall NV (0.01/0.005) stays E4-equal across formats | v5 stores **NV doubles** on wire; v6 `nv_to_u64` **truncates** toward 0 → fractional times become 0. E4-v0 dual fixtures use **integer tick** doubles (e.g. 100.0/40.0) so aggregates match (`e4_v0_*`, E4 policy) | no | — |
| 2026-08-12 | rust | `nytp_status` enum | Duplicate start-deflate / illegal lifecycle is `NYTP_ERR_IO` (3) | `NYTP_ERR_STATE` is **2**; `NYTP_ERR_IO` is **3**; overflow is **4**. See `collector/include/nytp_types.h`. G03e duplicate `nytp_emit_start_deflate` returns 2. | no | — |
| 2026-08-12 | perl | XS link vs Embed `ldopts` | `perl -MExtUtils::Embed -e ldopts` is the right way to link a loadable XS `.so` | Embed `ldopts` is for **embedding** perl (`-lperl`). Debian often has `libperl.so.5.38` but **no** `libperl.so` symlink, so `-lperl` fails. XS `.so` files are loaded into the running interpreter — use `$Config{lddlflags}` (`-shared`) + product libs (`-lz` for D1-B). `ccopts` is still correct for compile. G02 `xs-bootstrap` in `collector/Makefile`. | no | — |
| 2026-08-12 | perl | `$^P` 0x01 vs single-step | `$^P` 0x01 is single-step / statement profiler (`DB::DB`) | **0x01** is **sub enter/exit** (`DB::sub`). **0x02** is line-by-line (`DB::DB`). **0x20** is “start with single-step on.” See `perlvar` / `perldebguts`. G03a omitted 0x01 so Perl would not require `DB::sub`; G04 sets 0x01 only when `NYTPROF file=` is present. | no | — |
| 2026-08-12 | perl | `DB::DB` / `$DB::single` | Setting `$^P \|= 0x02` is enough for `DB::DB` to run each statement | `pp_dbstate` calls `DB::DB` only when **`$DB::single` is true**. `-d` already compiles `dbstate` once `$^P != 0`. G04 still needs `$DB::single = 1` (and `$^P \|= 0x20`) or shipped `report` fail-closes on zero `TIME_LINE`. **PR-7:** set it in `INIT`, not at `file=` enable. | no | — |
| 2026-08-12 | perl | D1-A vs D1-B `format=v6` | Product `format=v6` can write v6 from the default `xs-nytprof` D1-B `.so` | Default `xs-nytprof` is **D1-B** (`libnytp_sink_v5.a`, `-lz` only, `NYTPROF_V6_COLLECT` undefined) — `format=v6` **croaks** with the `v6_collect` rebuild string and must not write `NYTPROF6`. D1-A is a **separate** `xs-nytprof-v6` dest (`-DNYTPROF_V6_COLLECT`, `-lz -lzstd -llz4`). `format=dual` is always rejected. | no | — |
| 2026-08-12 | perl | product `fork` + addpid | 6.15 opcode `pp_fork` is required to get a child addpid file | Smallest G06 hook is **`CORE::GLOBAL::fork`** (installed when `addpid=1` + `file=`) calling shipped `nytp_fork_prepare` / `resume_parent` / `resume_child` + `nytp_v5_sink_fork_child_reinit` to `<file>.<pid>`. Skip `CORE::GLOBAL::fork` in `DB::sub` so the hook is not wrapped. Child `exit` runs `END` → `finish_profiler`. ParseXS rejects column-1 `#ifdef` inside XSUB CODE — keep v6/v5 reinit in a C helper. | no | — |
| 2026-08-12 | perl | prefix `nytprof-engine` | Installed `$PREFIX/bin/nytprof-engine` can still `find_repo_root` via Cargo.toml | `EngineDispatch::find_repo_root` requires `Cargo.toml` + `crates/nytprof-cli`. A product prefix has neither. I03: add `@INC` `../lib/perl5` + `../lib`; on failed walk treat prefix as repo for sibling `bin/nytprof-cli`. `query --json --jsonl` must stay cargo-free. | no | — |
| 2026-08-12 | perl | J01 MakeMaker NAME | Facade `NAME => NYTProf::Modernization::PackagingEntry` / `0.001` can stay as advertised CPAN identity | KD-16/17: advertised dist is **`Devel::NYTProf` ≥ 7.00**. `VERSION_FROM` `collector/xs/Devel/NYTProf.pm` (already `$VERSION = '7.00'`). `make manifest` + `MANIFEST.SKIP` must drop `baseline/` `target/` `prefix/`. `CPAN-TRIAL-READY` is J02, not J01. | no | — |
| 2026-08-13 | perl | Option B identity | Product debugger stays `-d:NYTProf` / `Devel::NYTProf` ≥ 7.00 | **Option B:** `perl -d:NYTProfM` / `Devel::NYTProfM` **6.15**, CPAN `DISTNAME` **NYTProfM**. Operators switch. RPM `perl-NYTProfM`. Do **not** `Provides: perl(Devel::NYTProf)`. | no | — |
| 2026-08-13 | perl | `CORE:print` / `CORE:match` | The 3 extra `calls=2` events (of 27) are XSUB `OP_ENTERSUB` | They are **slowops**: `OP_PRINT` / `OP_MATCH` via `pp_slowop_profiler`; names `${CopSTASHPV}::CORE:` + `PL_op_name` when `slowops=2` (`subr_entry_setup` ~2457–2485; `slowops.h`). Thin XSUB-only ENTERSUB does **not** close 27. See [`docs/DROP_IN_RPM_COMPLETION_v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/DROP_IN_RPM_COMPLETION_v0.md) KD-26/35. | no | — |
| 2026-08-13 | perl | `#ifdef OP_MATCH` | Guard MATCH hook with `#ifdef OP_MATCH` | `OP_MATCH` is an **enum** (31), not a macro — `#ifdef OP_MATCH` is always false. Always compile the MATCH branch. `use warnings` / `no warnings` in the debugger `.pm` compiles `warnings.pm` **before** XS BOOT, baking the original `op_ppaddr` into MATCH ops; do not `use warnings` before BOOT, and rebind `warnings` CVs after `require warnings`. `$DB::sub` for BEGIN is a **CV ref** (name as `main::BEGIN@line`). | no | — |

## Scope examples (what belongs here)

| Belongs | Does not belong |
|---------|-----------------|
| Perl `calls=1` vs `calls=2` SUB_ENTRY multiplicity | Generic “remember to use strict” |
| Rust fail-closed oversize length before alloc | Style-only clippy nits |
| Oracle `PERL5LIB` isolation (never `crates/`) | Unrelated crate ecosystem trivia |
| Tick/display vs exact integer counts (COMPAT-003) | Restating the whole COMPAT-001 field list |

## How to append (agents)

1. One row per distinct misconception (merge duplicates by editing the existing row).  
2. **Open?** = `yes` if still unresolved / needs ADR; `no` if corrected and settled.  
3. Prefer absolute links to contracts, schemas, or oracle paths.  
4. If the light row cannot carry the nuance (e.g. dual-equality timing), add `details/<slug>.md` and link it.
