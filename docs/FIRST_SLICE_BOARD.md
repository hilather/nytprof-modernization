# First-Slice Effort Board

**Horizon:** Phase-0 foundations → path to R1; **R2-preview opt-in packaging** (PR-B13)  
**Slice status:** **complete** for offline R0 / R1-preview first-slice scope (all primary rows **done**; **COL-007** product E3-EVENT with C is **done** at PR-B09; **COL-009** C baseline **reaffirmed** at PR-B13 / ADR-0007; **COL-008** remains **deferred** — batched Rust writer non-baseline). **R2-preview** opt-in base; **R2-stable** packaging cut **done** (PR-C05) (see `R2-PREVIEW-READINESS-CUT` + [`docs/RELEASE_NOTES_R2_PREVIEW.md`](RELEASE_NOTES_R2_PREVIEW.md)).  
**Not claimed:** full product R1 (FFI/XS Data, full nytprofhtml DOM, multi-OS CI, certified public perf, R3/R4 defaults, CLI v6 **collection** default) — see residual matrix. E3-mixed C fixtures are **done (MVP)**. **Wire freeze** (ADR-0006 / major=6 IDs) is **done** after E3+E4-v0. **Convert/merge/salvage** land with PR-C01/C02 on the R2-stable integrated stack (PR-C05).  
**Status key:** `todo` | `in_progress` | `done` | `blocked` | `deferred`  
**Agent quality bars:** [`AGENTS.md`](https://github.com/hilather/nytprof-modernization/blob/main/AGENTS.md)

This board is the lightweight program view of the 206-task plan. It is ordered for **value and risk**, not full plan enumeration.

## Now (Phase-0 roots)

| ID | Work | Status | Owner profile | Evidence |
|----|------|--------|---------------|----------|
| DOC-IMPORT | Import plan package; doc fixes (index name, crates, COL-008) | done | program | `docs/plan/` |
| CHARTER | Program charter + Phase-0 exit + time-boxes + packaging priority | done | program | `docs/PROGRAM_CHARTER.md`, siblings |
| ARCH-008 | ADR process + log | done | tech lead | `docs/governance/ARCH-008_ADR_PROCESS.md`, `docs/adrs/` |
| COMPAT-000 | Ratify non-negotiables contract | done | maintainer | `docs/governance/COMPAT-000_RATIFICATION.md` |
| BASE-001 | Pin 6.15 oracle, clean build/test, manifest | done | release/build | `baseline/6.15/` |
| BASE-002 | Provisional v5 record inventory | done | format/C | `baseline/inventories/` |
| BASE-003 | Timing/lifecycle freeze notes + open items | done | internals | `baseline/inventories/` |
| FIXTURE-0 | Fixture capture + comparator scaffold | done | QA | `tools/oracle/`, `fixtures/` |
| BUILD-SPIKE | Packaging spike notes (this window) | done | build | `docs/PACKAGING_SPIKE.md` |

## Next (unblocks R1; not all required for this goal’s exit)

| ID | Work | Status | Notes |
|----|------|--------|-------|
| COMPAT-001 | Provisional logical-event contract | done | `docs/contracts/COMPAT-001_LOGICAL_EVENT_CONTRACT.md`, `docs/contracts/logical-events.schema.json` (full freeze still in-progress; not a v6 wire freeze) |
| COMPAT-002-NORM | Volatile-field normalization (dump structural mode) | done | `docs/contracts/COMPAT-002_VOLATILE_NORMALIZATION.md`; shipped `tools/oracle/normalize_jsonl.py`; evidence `tools/oracle/selftest_normalize_compat.sh` (+ harness volatiles). Full plan COMPAT-002 still in-progress |
| COMPAT-003-PREC | Precision / numeric policy (provisional) | done | `docs/contracts/COMPAT-003_PRECISION_NUMERIC_POLICY.md`; integer ticks + dump `%.17g` / `normalize_number`; evidence `tools/oracle/selftest_normalize_compat.sh`. Full plan COMPAT-003 still in-progress |
| TEST-001/002 | Oracle harness + comparator mutations | done | `tools/oracle/selftest_harness.sh`, `normalize_jsonl.py`, `docs/schemas/canonical-event-dump-v0.md` |
| RUST-001/004 | Workspace + v5 decoder MVP | done | `crates/nytprof-*`, `cargo run -p nytprof-cli -- fixtures/v5/...`; normalized parity vs oracle dumps |
| RUST-006 | Compact model (first-slice MVP) | done | `crates/nytprof-model` (+ `crates/nytprof-aggregate` façade); A1–A9 via `docs/schemas/aggregate-comparison-v0.md` (`call_edges` / `source_lines` / `sub_defs`); oracle helper `tools/oracle/aggregate_from_jsonl.py` / `selftest_aggregates.sh`; native vs `aggregates.oracle.json` cargo tests. Not full plan RUST-006 (intern arenas / RSS instrumentation) |
| REPORT-MVP | One opt-in native report path | done | `crates/nytprof-report` text summary; `cargo run -p nytprof-cli -- report fixtures/v5/default-calls1/nytprof.out` (also `summary`). Not full REPORT-001..020 / HTML |
| MODEL-DEPTH | Call edges (A7) + source lines (A8) in compact model | done | `ProfileModel.call_edges` / `source_lines`; mid→leaf count=15, RUNTIME→mid count=3; `source_line(1,5)` workload text; tests in `nytprof-model` |
| CSV-REPORT | Native `csv` CLI path | done | `render_subs_csv` / `render_edges_csv`; `cargo run -p nytprof-cli -- csv fixtures/v5/default-calls1/nytprof.out` |
| ORACLE-COMPARE | Binary model vs oracle aggregates / dumps | done | `native_matches_aggregates_oracle_json` tests; `tools/oracle/compare_native_aggregates.sh`; A5+A7+A8+A9 vs `aggregates.oracle.json` |
| LIGHT-BENCH | Light offline wall-time harness | done | `tools/bench/light_bench.sh` — P2 `size`, P1 `collector_micro`, dump/verify/report/csv/html on `fixtures/v5/default-calls*`; notes in `docs/BENCH_NOTES.md`; **claim: none** |
| BENCH-001 | Noise study notes (not certification) | done | `docs/BENCH_NOTES.md` + runnable `tools/bench/light_bench.sh`; **no public performance claims** |
| HTML-REPORT-MVP | Minimal native HTML report path | done | `render_html_summary` / `escape_html` in `nytprof-report`; `nytprof-cli html <profile.out> [-o path.html]`; schema [`docs/schemas/html-report-mvp-v0.md`](schemas/html-report-mvp-v0.md). Subs table + workload source + line_totals. Not full REPORT-001..020 / nytprofhtml |
| HTML-RICHER | HTML call-edges + exclusive ranking | done | `render_html_summary` Call edges table + Top exclusive; mid→leaf count 15; leaf/mid returns 15/3; workload source. Schema items 7–8 |
| BLOCK-LINE-TOTALS | TIME_BLOCK fills A4 line_totals + A4b block_line_totals | done | Schema A4/A4b; `ProfileModel` + `aggregate_from_jsonl.py`; `blocks-calls1` `line_total(1,5).calls==780`; `block_line_totals` non-empty; oracle baselines regenerated |
| EXPORT-CALLGRIND-FOLDED | Callgrind + folded-stack export CLI | done | `render_callgrind` / `render_folded_stacks`; `nytprof-cli folded` / `callgrind` / `cg`; schema [`docs/schemas/export-formats-mvp-v0.md`](schemas/export-formats-mvp-v0.md). Not full legacy `nytprofcg` parity |
| FIXTURE-EXPAND | Broader v5 fixture set beyond default-calls | done | `fixtures/v5/blocks-calls1` (`calls=1:blocks=1`, TIME_BLOCK=916); aggregates + model tests |
| DECODE-ROBUST | Truncate / malformed decode tests | done | format-v5: empty/bad header/truncate/unsupported tag → Err; model `from_path` on truncated profile → Err |
| SUB-INFO-MODEL | Compact model A9 `sub_defs` from `SUB_INFO` | done | `ProfileModel.sub_defs` / `sub_info_events` / `sub_def(name)`; `SubDef { fid, first_line, last_line }`; last-write-wins; default-calls1 leaf `fid=1 first=3 last=7`, mid `1/8–12`; binary↔oracle jsonl + `aggregates.oracle.json` (`sub_defs`/`sub_info_events`); `aggregate_from_jsonl.py` A9 |
| HTML-MULTIFILE | Multi-file HTML site (`--out-dir`) | done | `HtmlSite` / `render_html_site` / `write_html_site` in `nytprof-report`; `nytprof-cli html <profile.out> --out-dir DIR` writes `index.html` + `file-*.html` + `source.html` (mutex with `-o`); schema [`docs/schemas/html-multifile-mvp-v0.md`](schemas/html-multifile-mvp-v0.md). Index: subs/edges/excl + Source files links; per-fid pages: A4 calls/ticks + hot loop. Single-file path unchanged. Per-fid expansion: HTML-PER-FILE |
| HTML-BLOCKS-LINES | HTML source line/block call counts (blocks fixtures) | done | Single-file + multi-file source tables show A4 `line_totals` calls/ticks; `html_summary_blocks_calls1_line_calls` + site source for blocks-calls1 line 5 calls **780**. A4b HTML is HTML-A4B-BLOCKS. Not full REPORT-006 / nytprofhtml block pages |
| HTML-PER-FILE | Per-file HTML pages (`file-<fid>.html`) under `--out-dir` | done | `HtmlSite.file_pages` + `write_html_site` emit `file-<fid>.html` for every eligible fid (`files` ∩ (`source_lines` ∪ `line_totals` ∪ `block_line_totals`)); index **Source files** lists relative links to all `file-*.html` (≥2 on default-calls1: `file-1.html` workload + `file-2.html` warnings); `source.html` is a copy of the primary workload page. Schema [`docs/schemas/html-per-file-mvp-v0.md`](schemas/html-per-file-mvp-v0.md). Tests: `html_site_default_calls1_render_html_site`, `write_html_site_default_calls1_tempdir` |
| HTML-A4B-BLOCKS | Surface A4b `block_line_totals` in HTML | done | `push_block_line_totals_table` in `nytprof-report`: dedicated **Block line totals** table (fid, block_line, calls, ticks) from `model.block_line_totals`; single-file summary (all fids) + per-fid `file-*.html` / primary `source.html` (that fid only); skipped when empty. Tests `html_summary_blocks_calls1_block_line_totals` / `html_site_blocks_calls1_block_line_totals` use model-derived positive calls (not hard-coded). Schema notes in [`docs/schemas/html-per-file-mvp-v0.md`](schemas/html-per-file-mvp-v0.md) |
| ATOMIC-HTML-PUBLISH | Fail-closed `write_html_site` (temp-then-rename) | done | `write_html_site` renders in memory, writes sibling `.nytprof-html-*` temp under `out_dir` parent, then `rename` publish (existing dir: bak → new → remove bak; restore bak on failure). Existing non-dir `out_dir` → Err unchanged. Schema notes in [`docs/schemas/html-multifile-mvp-v0.md`](schemas/html-multifile-mvp-v0.md) / [`html-per-file-mvp-v0.md`](schemas/html-per-file-mvp-v0.md). Tests: `write_html_site_atomic_default_calls1`, `write_html_site_atomic_overwrite_same_outdir`, `write_html_site_atomic_outdir_is_file_err`, `write_html_site_atomic_parent_is_file_err` |
| HTML-OUTDIR-SAFETY | Fail-closed path rules for `--out-dir` / `write_html_site` | done | `validate_html_out_dir`: reject empty path, embedded `\0`, any `..` component (`InvalidInput`); absolute paths allowed; called before publish. Schema [`docs/schemas/html-outdir-safety-mvp-v0.md`](schemas/html-outdir-safety-mvp-v0.md). Tests: `write_html_site_rejects_dotdot_component`, `write_html_site_rejects_null_byte`, `write_html_site_rejects_empty_path`; safe path still leaf 15 / mid 3 / edge 15 via atomic tests. **Before COL-007.** |
| VERIFY-CLI | `verify` / `inspect` CLI for profile health check | done | `nytprof_report::verify_profile` + `nytprof-cli verify` / `inspect`; schema [`docs/schemas/verify-cli-mvp-v0.md`](schemas/verify-cli-mvp-v0.md). Success: `OK: <path>` + events/TIME_LINE/TIME_BLOCK/files/subs; truncated/malformed → Err / non-zero. Tests `verify_profile_default_calls1_ok` / `verify_profile_truncated_default_calls1_err` |
| PACKAGING-OPTIONAL | optional Cargo path docs + native smoke | done | `docs/PACKAGING_SPIKE.md`; README packaging smoke; `./scripts/packaging/native_optional_smoke.sh` (cargo test of format-v5/model/report/cli; skip if no cargo); `./scripts/packaging/engine_select_smoke.sh` |
| ENGINE-SELECT | `--engine` / `NYTPROF_ENGINE` | done | Schema [`docs/schemas/engine-selection-mvp-v0.md`](schemas/engine-selection-mvp-v0.md); `crates/nytprof-cli/src/engine.rs` (`resolve_engine`, `peel_engine_flag`; unit tests); CLI flag overrides env, default native, `auto`→native, `legacy` exit 2 + oracle message; smoke `./scripts/packaging/engine_select_smoke.sh` |
| LEGACY-ONLY-SMOKE | `scripts/packaging/legacy_only_smoke.sh` | done | `./scripts/packaging/legacy_only_smoke.sh` — oracle `PERL5LIB` isolation, no `/crates/`, loads `Devel::NYTProf` from `baseline/6.15/install`, optional oracle dump; never invokes Cargo |
| PERL-ENGINE-DISPATCH | Perl engine-dispatch facade (`nytprof-engine`) | done | Schema [`docs/schemas/perl-engine-dispatch-mvp-v0.md`](schemas/perl-engine-dispatch-mvp-v0.md); `perl/bin/nytprof-engine` + `perl/lib/Devel/NYTProf/EngineDispatch.pm` (`resolve_engine`, `peel_engine_flag`, `run_cli` / `dispatch`); smoke `./scripts/packaging/perl_engine_dispatch_smoke.sh` |
| PERL-NATIVE-BRIDGE | Perl → native `nytprof-cli` / `nytprof-dump` subprocess | done | `EngineDispatch::run_native` / `find_native_cli`: `NYTPROF_NATIVE_CLI` → `prefix/bin/{nytprof-cli,nytprof-dump}` → `target/{debug,release}/nytprof-dump` → `cargo run -q -p nytprof-cli --`; install via `scripts/packaging/install_native.sh`; smoke asserts `main::leaf` / `returns=15` |
| PERL-LEGACY-BRIDGE | Perl → oracle tools (no Cargo) | done | `LegacyBridge::run_legacy_report` + `EngineDispatch::run_legacy`: oracle `PERL5LIB` from `baseline/6.15/oracle-perl5lib.txt` only (refuse `/crates/` / candidate `perl/`); verify install load path; `dump_readstream.pl` (line count > 0); optional `nytprofcsv` (non-fatal); smoke `./scripts/packaging/perl_engine_dispatch_smoke.sh` |
| PERL-READSTREAM-JSONL | Pure-Perl ReadStream-from-dump JSONL bridge | done | Schema [`docs/schemas/perl-jsonl-readstream-mvp-v0.md`](schemas/perl-jsonl-readstream-mvp-v0.md); `perl/lib/Devel/NYTProf/JsonlReadStream.pm` (`for_chunks` / `process_jsonl` / `count_sub_returns`; core `JSON::PP` only); smoke `./scripts/packaging/perl_jsonl_readstream_smoke.sh` + `prove -Iperl/lib perl/t/jsonl_readstream_default_calls1.t` — default-calls1 **leaf=15** / **mid=3** from real `SUB_RETURN` events (golden JSONL + optional native dump). Not full XS `PERL-004` |
| NATIVE-INSTALL-PATH | Stable `prefix/bin` native CLI install | done | Schema [`docs/schemas/native-install-mvp-v0.md`](schemas/native-install-mvp-v0.md); `./scripts/packaging/install_native.sh` → `$REPO/prefix/bin/nytprof-cli` (+ `nytprof-dump` alias); smoke `./scripts/packaging/native_install_smoke.sh` (report leaf/mid 15/3 via prefix / `NYTPROF_NATIVE_CLI`) |
| PERL-DISPATCH-EXPAND | `find_native_cli` includes prefix install | done | `EngineDispatch::find_native_cli`: `NYTPROF_NATIVE_CLI` → `prefix/bin/{nytprof-cli,nytprof-dump}` → `target/{release,debug}/nytprof-dump` → `cargo run`; schema [`docs/schemas/perl-engine-dispatch-mvp-v0.md`](schemas/perl-engine-dispatch-mvp-v0.md) |
| PACKAGING-GATE | Unified packaging fail-fast gate | done | `./scripts/packaging/packaging_gate.sh` — legacy_only → engine_select → perl_engine_dispatch → (if present) install_native + native_install_smoke → native_optional_smoke → capability_selftest when cargo/prefix/target CLI present; never puts `crates/` on oracle `PERL5LIB` |
| BUILD-DUAL-PATH | Dual-path build support policy (BUILD-001 draft) | done | Policy [`docs/BUILD_SUPPORT_POLICY.md`](BUILD_SUPPORT_POLICY.md): tiers legacy-only (no Cargo) vs optional-native; `crates/` never on oracle `PERL5LIB`; verify via `./scripts/packaging/dual_path_smoke.sh` (legacy required; native if cargo present else honest skip). Non-goals: BUILD-003 full MakeMaker dual-build, BUILD-006 CI matrix, multi-OS prebuilts. Spike pointer in [`docs/PACKAGING_SPIKE.md`](PACKAGING_SPIKE.md) |
| BUILD-MAKEMAKER-OPT | Candidate MakeMaker dual-path packaging entry | done | Root [`Makefile.PL`](../Makefile.PL) facade (not full XS CPAN): `NYTPROF_NATIVE=0` default legacy / `=1` requires cargo / `make native-install`; targets `legacy-smoke` → `legacy_only_smoke.sh`, `dual-path-smoke` → `dual_path_smoke.sh`, `native-install` → `install_native.sh`. Smoke `./scripts/packaging/makemaker_dual_path_smoke.sh`. Policy section in [`docs/BUILD_SUPPORT_POLICY.md`](BUILD_SUPPORT_POLICY.md). **Before COL-007.** Not BUILD-003 full dual-build |
| I02-MAKEMAKER-NATIVE | MakeMaker optional native CLI (`NYTPROF_NATIVE`) | **done (MVP)** | I02: `=1` fail-closed without cargo (real `Makefile.PL`); `=0`/`auto` cargo-free when cargo hidden; with cargo `=1` + `auto` `make all` install `nytprof-cli` via [`install_native.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/install_native.sh); smoke [`i02_makemaker_native_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/i02_makemaker_native_smoke.sh) report **15/3/15**. **Not** BUILD-003-FULL / S2 / CPAN-TRIAL. |
| I03-DIST-SCRIPTS | Familiar report script names + EngineDispatch in product prefix | **done (MVP)** | I03: cargo-free [`install_product_scripts.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/install_product_scripts.sh) → `$PREFIX/lib/perl5/Devel/NYTProf/{EngineDispatch,JsonlData,JsonlReadStream,LegacyBridge}.pm` + `$PREFIX/bin/{nytprof-engine,nytprofhtml,nytprofcsv}`; smoke [`i03_dist_scripts_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/i03_dist_scripts_smoke.sh) installed `query --json --jsonl` **15/3/15**. Schema [`product-dist-scripts-mvp-v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/schemas/product-dist-scripts-mvp-v0.md). **Not** 6.15 nytprofhtml DOM / COMPAT-007 / BUILD-003-FULL / S2. |
| MIG01-MIGRATION-GUIDE | Operator drop-in migration guide | **done (docs)** | [`docs/MIGRATION_DROP_IN_v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/MIGRATION_DROP_IN_v0.md) — Option B `NYTProfM` / `-d:NYTProfM` / `perl-NYTProfM` 6.15; CPAN vs EL8, D1-B/D1-A, rollback, v5 profile compat. **Not** CPAN-TRIAL upload / public COPR. |
| K03-PREBUILT-CLI-ADR | Signed CI prebuilt `nytprof-cli` policy (EL8 tools) | **done (docs)** | [ADR-0010](https://github.com/hilather/nytprof-modernization/blob/main/docs/adrs/0010-signed-ci-prebuilt-native-cli.md) — KD-13; K02 spec consumes this contract. **Not** signed publish pipeline. |
| EL8-RPM-MODULE | EL8 `perl-NYTProfM` D1-B spec | **done (MVP)** | K01: [`perl-NYTProfM.spec`](https://github.com/hilather/nytprof-modernization/blob/main/packaging/rpm/perl-NYTProfM.spec); smoke [`k01_el8_module_rpm_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/k01_el8_module_rpm_smoke.sh). Option B / **6.15**; **collection-only** + `nytprofm-cli`; no I03 `nytprofhtml` / no stock bindir overwrite; no `Provides: perl(Devel::NYTProf)`. Testdrive unsigned RPM: [`build_el8_module_rpm.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/build_el8_module_rpm.sh) + tag workflow [`release-el8-rpm.yml`](https://github.com/hilather/nytprof-modernization/blob/main/.github/workflows/release-el8-rpm.yml). **Not** mock-certified / D1-A default / signed COPR. |
| EL8-RPM-TOOLS | EL8 `nytprof-cli` companion spec | **done (MVP)** | K02: [`nytprof-cli.spec`](https://github.com/hilather/nytprof-modernization/blob/main/packaging/rpm/nytprof-cli.spec); smoke [`k02_el8_tools_rpm_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/k02_el8_tools_rpm_smoke.sh). ADR-0010; **not** signed-pipeline complete. |
| ROCKY8-DOCKER-PROFILE-LAB | Rocky 8 Docker testdrive profile lab | **done (MVP)** | Operator demo [`rocky8_docker_profile_demo.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/field/rocky8_docker_profile_demo.sh); integration smoke [`rocky8_docker_profile_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/field/rocky8_docker_profile_smoke.sh) (`--lab`, honest docker SKIP). Schema [`rocky8-docker-profile-lab-mvp-v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/schemas/rocky8-docker-profile-lab-mvp-v0.md). **Not** in `offline_gate`. **Not** mock-certified / ack attach (product `DB::sub` residual). |
| COMPLEX-APP-CATALOG | 20-app attach catalog + diverse top-10 drivers | **done (MVP)** | Catalog [`complex-app-catalog-v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/schemas/complex-app-catalog-v0.md) / [`catalog.tsv`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/field/workloads/complex_apps/catalog.tsv). Top-10 distinct families. Lab `--app ID`. Tests `t/complex_app_catalog.t` + `t/attach_survival_failclosed.t`. Findings [`complex-app-findings-v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/schemas/complex-app-findings-v0.md). **Not** 20-app HTML sweep / exclusive-time vs 6.15. |
| COMPLEX-APP-DOCKER-PROFILE-LAB | Rocky 8 Docker Rex + DateTime attach lab | **done (MVP)** | Real CPAN app: [`complex_app_docker_profile.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/field/complex_app_docker_profile.sh) profiles `rex lab` (DateTime / Duration / YAML, no SSH). Smoke [`complex_app_docker_profile_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/field/complex_app_docker_profile_smoke.sh). Schema [`complex-app-docker-profile-mvp-v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/schemas/complex-app-docker-profile-mvp-v0.md). In-tree `xs-nytprof` (not testdrive RPM `.so`). Default HTML `~/Downloads/nytprof-rex-demo/`. **Not** in `offline_gate`. |
| ADR-0011-HTML-OP-V1 | Native operator HTML v1 policy | **done (policy)** | [ADR-0011](https://github.com/hilather/nytprof-modernization/blob/main/docs/adrs/0011-native-operator-html-v1.md). New class (heat/links/vanilla sort/HTML seconds). Does **not** un-waive M01 jquery/tablesorter. Design [`OPERATOR_HTML_AND_LIVE_METRICS_v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/OPERATOR_HTML_AND_LIVE_METRICS_v0.md). |
| ADR-0012-HTML-OP-V2 | Native operator HTML v2 policy | **done (policy)** | [ADR-0012](https://github.com/hilather/nytprof-modernization/blob/main/docs/adrs/0012-native-operator-html-v2.md). Chrome / IA / sort / compact time / six-col source. Does **not** un-waive M01. Design [`OPERATOR_HTML_V2_DESIGN_v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/OPERATOR_HTML_V2_DESIGN_v0.md). |
| ADR-0013-COALESCE | In-memory v5 coalesced checkpoints | **proposed** | [ADR-0013](https://github.com/hilather/nytprof-modernization/blob/main/docs/adrs/0013-v5-coalesced-checkpoints.md) + [ADR-Q027](https://github.com/hilather/nytprof-modernization/blob/main/docs/plan/18_OPEN_QUESTIONS_AND_ADR_QUEUE.md). Charter exception; default `aggregate=0`. **Not accepted.** No C1/C2 until owner sign-off. Design [`OPERATOR_PROFILE_SIZE_AND_DURABILITY_v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/OPERATOR_PROFILE_SIZE_AND_DURABILITY_v0.md). |
| HTML-OP-V2 | Operator HTML v2 (chrome, index IA, files table, compact time, 6-col source) | **done (MVP)** | `nytprof-report` `render_html_site` + CLI `html --out-dir`. Schema [`html-operator-v2-mvp-v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/schemas/html-operator-v2-mvp-v0.md). Tests `html_operator_v2.rs`. **Not** jquery. |
| HTML-OP-V2-GRAPHVIZ | Native `packages-callgraph.dot` / `subs-callgraph.dot` | **done (MVP)** | From `call_edges`; index links; `.dot` source only (no `dot` PNG). M01 still **WAIVE**. |
| HTML-OP-V2-CALLS | Source call-in/out + Calls/Time-in-subs | **done (MVP)** | Usable `call_sites` only; `(1,1)` stubs omitted (`—`). |
| TOKENIZE-EXCL-SPLIT | Parent excl = incl − slowop children | **done (MVP)** | `take_pending_child_excl`; smoke [`g09_tokenize_excl_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/g09_tokenize_excl_smoke.sh). **Not** full `slowops.h`. |
| ROCKY8-DUAL-DOCKER-LAB | Dual-container oracle + native Rocky lab | **done (MVP)** | `--engine native\|oracle\|both`; KD-LAYOUT migrate-then-link. Oracle builds 6.15 from `baseline/6.15/archives/` (no `crates/` on PERL5LIB). Honest SKIP on oracle compile fail. **Not** in `offline_gate`. |
| LIVE-ATTACH-METRICS | Live incl/excl + elapsed TIME_LINE | **done (MVP)** | `nytp_clock_now` + attributed `TIME_LINE` + SRC_LINE/SUB_INFO at finish. g04 15/3/15, leaf incl > 0, verify OK. **PR-8:** stmt-ops `TIME_BLOCK` last-site ticks (di01 not identically 1) + PRINT/MATCH incl/excl (g08). **Still not** full opcode / `slowops.h`. |
| P01-GA-CANDIDATE | GA-candidate drop-in honesty cut | **done (MVP)** | P01: [`RELEASE_NOTES_GA_CANDIDATE_v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/RELEASE_NOTES_GA_CANDIDATE_v0.md); smoke [`p01_ga_candidate_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/p01_ga_candidate_smoke.sh). Rocky default **D1-B only**. **Not** SEC-012 complete / PAUSE / R3–R4 / S2. |
| P02-SEC-CUT | SEC-012 checklist + SEC-002 job MVP | **done (MVP / checklist / job)** | P02: [`SEC_012_RELEASE_REVIEW_CHECKLIST_v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/contracts/SEC_012_RELEASE_REVIEW_CHECKLIST_v0.md); wrapper [`sec002_continuous_fuzz_mvp.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/ci/sec002_continuous_fuzz_mvp.sh) invokes shipped [`selftest_security_fuzz.sh`](https://github.com/hilather/nytprof-modernization/blob/main/tools/oracle/selftest_security_fuzz.sh) / `decode_fuzz` (honest `SKIP:` without cargo); workflow [`.github/workflows/sec002-fuzz-mvp.yml`](https://github.com/hilather/nytprof-modernization/blob/main/.github/workflows/sec002-fuzz-mvp.yml); smoke [`p02_sec_cut_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/p02_sec_cut_smoke.sh). **Not** independent sign-off / full continuous fuzz / GA marketing / S2. |
| J01-CPAN-HYGIENE | CPAN dist identity `NYTProfM` / `Devel::NYTProfM` **6.15** | **done (MVP)** | J01: root [`Makefile.PL`](https://github.com/hilather/nytprof-modernization/blob/main/Makefile.PL) `NAME => Devel::NYTProfM`, `DISTNAME => NYTProfM`, `VERSION_FROM` product [`collector/xs/Devel/NYTProfM.pm`](https://github.com/hilather/nytprof-modernization/blob/main/collector/xs/Devel/NYTProfM.pm); [`MANIFEST.SKIP`](https://github.com/hilather/nytprof-modernization/blob/main/MANIFEST.SKIP) excludes `baseline/` `target/` `prefix/`; smoke [`j01_cpan_hygiene_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/j01_cpan_hygiene_smoke.sh). **Not** PAUSE upload / BUILD-003-FULL / S2. |
| CPAN-TRIAL-READY | CPAN TRIAL attach-preview notes | **done (notes-ready / MVP)** | J02: [`RELEASE_NOTES_CPAN_TRIAL_v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/RELEASE_NOTES_CPAN_TRIAL_v0.md); smoke [`j02_cpan_trial_notes_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/j02_cpan_trial_notes_smoke.sh). Not uploaded to PAUSE. |
| REPORT-SEMANTIC-PARITY | Oracle-aligned report semantics (leaf/mid/edge) on default-calls1 | done | Schema [`docs/schemas/report-semantic-parity-mvp-v0.md`](schemas/report-semantic-parity-mvp-v0.md); model+HTML test `report_semantic_parity_default_calls1` (`nytprof-report`); smoke `./tools/oracle/report_semantic_parity.sh` (oracle `nytprofhtml` under isolated PERL5LIB + native `html -o` / `--out-dir`). Exact: leaf returns **15**, mid **3**, mid→leaf **15**. Not full DOM/REPORT-001..020; ticks only under COMPAT-003 |
| NATIVE-DUMP-PARITY | Shipped CLI dump vs golden JSONL structural equality (default-calls1) | done | Schema [`docs/schemas/native-dump-parity-mvp-v0.md`](schemas/native-dump-parity-mvp-v0.md); smoke `./tools/oracle/selftest_native_dump_parity.sh` (dump×2 stability + normalize + `compare_jsonl.pl` full match + TIME_LINE/TIME_BLOCK/SUB_RETURN multiplicity); harness step in `selftest_harness.sh`; optional `cargo test -p nytprof-format-v5 native_dump_tag_counts_match_golden_default_calls1`. Full structural equality on default-calls1 after normalize; no residual list |
| DUMP-PARITY-EXPAND | Multi-fixture native dump structural parity | done | Same schema; `selftest_native_dump_parity.sh` accepts fixture name/path args (default still `default-calls1`); `selftest_native_dump_parity_all.sh` runs **default-calls1** + **calls2-default** + **blocks-calls1**; multiplicity from **per-fixture golden** (blocks-calls1 uses `TIME_BLOCK`, not default-calls1 `TIME_LINE` counts); harness nests `_all.sh`; optional Rust `native_dump_tag_counts_match_golden_{calls2_default,blocks_calls1}`. **Before COL-007.** |
| BASE-004-INV | Public Perl API surface inventory (6.15) | done | `baseline/inventories/perl-api-surface.md` — Data/ReadStream/Reader/FileInfo/SubInfo/SubCallInfo/FileHandle + Apache/Run/Util/Core dispositions; open items OI-BASE004-01..08 |
| BASE-005-INV | CLI/report surface inventory (6.15) | done | `baseline/inventories/cli-report-surface.md` — nytprofhtml/csv/cg/calls/merge + flamegraph.pl; flags/outputs/dispositions; open items OI-BASE005-01..07 |
| COMPAT-004-CLASS | Surface classification (public / de-facto / internal / legacy-only) | done | `docs/contracts/COMPAT-004_SURFACE_CLASSIFICATION.md` — Perl Data/ReadStream/Reader/FileInfo/SubInfo (+ SubCallInfo/FileHandle notes); CLI nytprofhtml/csv/cg/calls/merge; class × native disposition; open items OI-C004-01..13. Provisional only — not full COMPAT-004 freeze |
| BASE-006-MATRIX | Feature-to-test matrix (BASE-004/005 surfaces ↔ fixtures/tests) | done | `baseline/inventories/feature-to-test-matrix.md` — default-calls1 primary + blocks-calls1; Data/ReadStream/html/csv/dump rows; oracle selftests + cargo patterns; open gaps G-006-01..12 (no XS ReadStream, no full HTML DOM, no merge, no FFI) |
| COMPAT-010-ERR | Provisional fail-closed policy on corrupt v5 input (empty / truncated / bad magic) | done | Contract [`docs/contracts/COMPAT-010_ERROR_FAIL_CLOSED.md`](contracts/COMPAT-010_ERROR_FAIL_CLOSED.md): shipped `verify` / `dump` / `report` → non-zero / `Err`, no panic, no silent success. Evidence: `fail_closed_*` in `nytprof-report` + `crates/nytprof-cli/tests/fail_closed.rs`; format-v5 `decode_empty`/`decode_bad_header`/`decode_truncated_*`; model `from_path_truncated_profile_errors`; smoke `tools/oracle/selftest_fail_closed.sh` (harness step). Related: VERIFY-CLI, DECODE-ROBUST. **Not** full COMPAT-010 taxonomy freeze |
| FIXTURE-EXPAND-2 | Additional v5 fixture beyond default-calls1/2 + blocks-calls1 | done | `fixtures/v5/calls2-default` (`calls=2`); leaf returns **15**, mid **3**, mid→leaf **15**; aggregates + model tests `calls2_default_*`; `tools/oracle/fixture_expand_2.sh` |
| BLOCKS-SEMANTIC-PARITY | Oracle-aligned blocks semantics (line5 calls / leaf / mid) on blocks-calls1 | done | Schema [`docs/schemas/blocks-semantic-parity-mvp-v0.md`](schemas/blocks-semantic-parity-mvp-v0.md); model+HTML test `blocks_semantic_parity_blocks_calls1` (`nytprof-report`); smoke `./tools/oracle/blocks_semantic_parity.sh` (native `report` + `html -o` / `--out-dir`). Exact: `line_total(1,5).calls` **780**, leaf returns **15**, mid **3**. Consolidates A4 TIME_BLOCK + sub returns under one named gate. Not full DOM/REPORT-001..020; ticks only under COMPAT-003 |
| PERL-DATA-JSONL | Pure-Perl Data-from-dump JSONL query path | done | Schema [`docs/schemas/perl-jsonl-data-mvp-v0.md`](schemas/perl-jsonl-data-mvp-v0.md); `perl/lib/Devel/NYTProf/JsonlData.pm` (`from_jsonl` / `from_cli` / `sub_returns` / `sub_return_totals` / `call_edge_count`; reuses `JsonlReadStream`; core `JSON::PP` only); smoke `./scripts/packaging/perl_jsonl_data_smoke.sh` + `prove -Iperl/lib perl/t/jsonl_data_default_calls1.t` — default-calls1 **leaf=15** / **mid=3** / **mid→leaf=15** from real `SUB_RETURN` + `SUB_CALLERS` events (golden JSONL + optional native dump). Not full XS Data |
| PERL-LINE-TOTALS | Pure-Perl A4 line_totals from TIME_LINE + TIME_BLOCK | done | Schema [`docs/schemas/perl-jsonl-data-mvp-v0.md`](schemas/perl-jsonl-data-mvp-v0.md): `JsonlData` `_ingest` accumulates **`TIME_BLOCK`** into same A4 `line_totals` as `TIME_LINE` (statement fid@1 line@2); `line_calls($fid,$line)`. Test `perl/t/jsonl_data_blocks_calls1_line_totals.t` + smoke `./scripts/packaging/perl_line_totals_smoke.sh` — blocks-calls1 **`line_calls(1,5)==780`** from real TIME_BLOCK events; default-calls1 leaf/mid **15/3** kept. No XS; never `crates/` on oracle PERL5LIB. **Before COL-007.** |
| PERL-A4B-JSONL | Pure-Perl A4b block_line_totals from TIME_BLOCK | done | Schema [`docs/schemas/perl-jsonl-data-mvp-v0.md`](schemas/perl-jsonl-data-mvp-v0.md): `JsonlData` `_ingest` accumulates **`TIME_BLOCK`** into A4b `block_line_totals` keyed by `(fid, block_line)` at fid@1 block_line@3 (still fills A4 via statement line); API `block_line_totals` / `block_line_calls($fid,$block_line)`. Test `perl/t/jsonl_data_a4b_blocks_calls1.t` + smoke `./scripts/packaging/perl_a4b_smoke.sh` — blocks-calls1 **`"1:4".calls==810`**, **`line_calls(1,5)==780`**, non-empty A4b; default empty A4b. No XS; never `crates/` on oracle PERL5LIB. **Before COL-007.** |
| PERL-ENGINE-QUERY | Shipped Perl entry: sub/edge query via JsonlData | done | Schema [`docs/schemas/perl-engine-dispatch-mvp-v0.md`](schemas/perl-engine-dispatch-mvp-v0.md) (`query` / `data-query`); `EngineDispatch::run_query` + `print_query_results` → native dump via `find_native_cli` or `--jsonl` golden; smoke `./scripts/packaging/perl_engine_query_smoke.sh` + `prove -Iperl/lib perl/t/engine_query_default_calls1.t` — default-calls1 **leaf=15** / **mid=3** / **mid→leaf=15**. No XS; never `crates/` on oracle PERL5LIB. **Before COL-007.** |
| PERL-ENGINE-QUERY-EXPAND | Expand `query` output: sub_defs / source_line / line_calls / A4b | done | Schema [`docs/schemas/perl-engine-dispatch-mvp-v0.md`](schemas/perl-engine-dispatch-mvp-v0.md) always-full MVP `print_query_results` after returns/edges: `sub_def main::leaf fid=1 first=3 last=7`, `sub_def main::mid … 8–12`, `source_line 1:5=…$x++…1 .. 50`, blocks-calls1 `line_calls 1:5=780` + `block_line_calls 1:4=810`. Smoke `./scripts/packaging/perl_engine_query_expand_smoke.sh` + expanded `perl/t/engine_query_default_calls1.t`. JsonlData APIs only; no XS; never `crates/` on oracle PERL5LIB. **Before COL-007.** |
| PERL-QUERY-PID-META | Expand `query` output: PID lifecycle + ATTRIBUTE/OPTION | done | Schema [`docs/schemas/perl-engine-dispatch-mvp-v0.md`](schemas/perl-engine-dispatch-mvp-v0.md): `print_query_results` after returns/edges/sub_def/source_line/line_calls/block_line also prints dump-derived `pid_start_count` / `pid_end_count` / `pid_start` / `pid_end` and key `attribute` / `option` lines via JsonlData APIs only (`pid_starts`/`pid_ends`/`attributes`/`options`). Smoke `./scripts/packaging/perl_engine_query_pid_meta_smoke.sh` + expanded `perl/t/engine_query_default_calls1.t` — default-calls1 **15/3/15**, **pid≥1/≥1** matching **2975381**, **ticks_per_sec** + **calls**. No XS; never `crates/` on oracle PERL5LIB. **Before COL-007.** |
| QUERY-JSON-MVP | Structured JSON for `nytprof-engine query` | done | Schema [`docs/schemas/perl-engine-dispatch-mvp-v0.md`](schemas/perl-engine-dispatch-mvp-v0.md) JSON section; CLI `query --json` / `--format=json` / `--format json` → `{"ok":true,"subs":{…},"edges":{…},"leaf_returns":15,"mid_returns":3,"mid_leaf_edge":15}` (core `JSON::PP`); human greppable default unchanged. Smoke `./scripts/packaging/perl_query_json_smoke.sh` (`--json`×2 + parse + consistency + format aliases + human path). Default-calls1 **15/3/15** from real JsonlData. No XS; never `crates/` on oracle PERL5LIB. **Before COL-007.** |
| QUERY-JSON-EXPAND | Expand `query --json` object (discount + stream complete) | done | Schema [`docs/schemas/perl-engine-dispatch-mvp-v0.md`](schemas/perl-engine-dispatch-mvp-v0.md) JSON section; same CLI → also `discount_events` **818**, `is_stream_complete` **true**, `incompleteness_reasons` **[]**, plus `time_line_events` / `pid_start_events` / `pid_end_events` (JsonlData APIs only). Smoke `./scripts/packaging/perl_query_json_smoke.sh` asserts expand fields. Human path unchanged. No XS; never `crates/` on oracle PERL5LIB. **Before COL-007.** |
| PERL-SUBDEFS-JSONL | Pure-Perl A9 sub_defs + NEW_FID files from dump JSONL | done | Schema [`docs/schemas/perl-jsonl-data-mvp-v0.md`](schemas/perl-jsonl-data-mvp-v0.md): `JsonlData` ingests **`SUB_INFO`** → `sub_defs` / `sub_def($name)` (`{fid,first_line,last_line}`, last-write-wins) and **`NEW_FID`** → `files` / `file($fid)` / `file_basename($fid)`. Test `perl/t/jsonl_data_subdefs_default_calls1.t` + smoke `./scripts/packaging/perl_subdefs_smoke.sh` — default-calls1 leaf **1/3–7**, mid **1/8–12**, `file(1)` contains **workload.pl** from real events (golden + optional native dump). No XS; never `crates/` on oracle PERL5LIB. **Before COL-007.** |
| PERL-SOURCE-JSONL | Pure-Perl A8 source_lines from SRC_LINE dump JSONL | done | Schema [`docs/schemas/perl-jsonl-data-mvp-v0.md`](schemas/perl-jsonl-data-mvp-v0.md): `JsonlData` ingests **`SRC_LINE`** → `source_lines` `{ "fid:line" => text }` last-write-wins; API `source_line($fid,$line)` / `source_lines()`. Test `perl/t/jsonl_data_source_default_calls1.t` + smoke `./scripts/packaging/perl_source_smoke.sh` — default-calls1 **`source_line(1,5)`** exact **`    $x++ for 1 .. 50;\n`** from real events (golden + optional native dump; stream re-count). No XS; never `crates/` on oracle PERL5LIB. **Before COL-007.** |
| PERL-META-JSONL | Pure-Perl ATTRIBUTE + OPTION metadata from dump JSONL | done | Schema [`docs/schemas/perl-jsonl-data-mvp-v0.md`](schemas/perl-jsonl-data-mvp-v0.md): `JsonlData` ingests **`ATTRIBUTE`** → `attributes` / `attribute($key)` and **`OPTION`** → `options` / `option($key)` (last-write-wins; dump values as-is). Test `perl/t/jsonl_data_meta_default_calls1.t` + smoke `./scripts/packaging/perl_meta_smoke.sh` — default-calls1 has **`ticks_per_sec`** (or **`basetime`**) + option **`calls`** from real events (golden + optional native dump; stream re-count). No XS; never `crates/` on oracle PERL5LIB. **Before COL-007.** |
| PERL-PID-JSONL | Pure-Perl PID_START / PID_END from dump JSONL | done | Schema [`docs/schemas/perl-jsonl-data-mvp-v0.md`](schemas/perl-jsonl-data-mvp-v0.md): `JsonlData` ingests **`PID_START`** / **`PID_END`** → `pid_starts` / `pid_ends` (hashes with at least `pid`; optional `ppid` / `start_time` / `end_time`), `pid_start_count` / `pid_end_count` (`pid_start_events` / `pid_end_events` aliases), `pids()`. Test `perl/t/jsonl_data_pid_default_calls1.t` + smoke `./scripts/packaging/perl_pid_smoke.sh` (wired into `perl_jsonl_data_all_smoke.sh`) — default-calls1 **start≥1 end≥1**, start pid matches end pid (**2975381** dump-derived; stream re-count). No XS; never `crates/` on oracle PERL5LIB. **Before COL-007.** |
| PERL-STREAM-COMPLETE | Pure-Perl JsonlData stream-completeness (COMPAT-010) | done | Schema [`docs/schemas/perl-jsonl-data-mvp-v0.md`](schemas/perl-jsonl-data-mvp-v0.md) + contract [`docs/contracts/COMPAT-010_INCOMPLETE_STREAM.md`](contracts/COMPAT-010_INCOMPLETE_STREAM.md): `JsonlData` tracks `time_line_events` / `time_block_events`; API `is_stream_complete` / `stream_incompleteness_reasons` (PID balance when starts seen **and** `TIME_LINE+TIME_BLOCK > 0`). Test `perl/t/jsonl_data_stream_complete_default_calls1.t` + smoke `./scripts/packaging/perl_stream_complete_smoke.sh` (wired into `perl_jsonl_data_all_smoke.sh`) — default-calls1 **complete**/reasons empty; incomplete craft from real golden header lines → incomplete/reasons non-empty. No XS; never `crates/` on oracle PERL5LIB. **Before COL-007.** |
| PERL-DISCOUNT-JSONL | Pure-Perl A3 DISCOUNT event multiplicity from dump JSONL | done | Schema [`docs/schemas/perl-jsonl-data-mvp-v0.md`](schemas/perl-jsonl-data-mvp-v0.md): `JsonlData` ingests **`DISCOUNT`** (empty args) → `discount_events` / `discount_count` (A3 multiplicity only — not exclusive-time policy freeze). Test `perl/t/jsonl_data_discount_default_calls1.t` + smoke `./scripts/packaging/perl_discount_smoke.sh` (wired into `perl_jsonl_data_all_smoke.sh`) — default-calls1 **818** matches independent stream re-count of `DISCOUNT` tags (golden + optional native dump). No XS; never `crates/` on oracle PERL5LIB. **Before COL-007.** |
| PERL-SUB-ENTRY-JSONL | Pure-Perl SUB_ENTRY event multiplicity from dump JSONL | done | Schema [`docs/schemas/perl-jsonl-data-mvp-v0.md`](schemas/perl-jsonl-data-mvp-v0.md): `JsonlData` ingests **`SUB_ENTRY`** (`caller_fid`, `caller_line`) → `sub_entry_events` / `sub_entry_count` (multiplicity only — not full call-stack / arg freeze). Test `perl/t/jsonl_data_sub_entry.t` + smoke `./scripts/packaging/perl_sub_entry_smoke.sh` (wired into `perl_jsonl_data_all_smoke.sh`) — default-calls1 **0** (`calls=1`) and calls2-default **27** (`calls=2`) match independent stream re-count of `SUB_ENTRY` tags (golden + optional native dump). No XS; never `crates/` on oracle PERL5LIB. **Before COL-007.** |
| PERL-ENGINE-EXPORT | Wire `folded` / `callgrind` / `cg` through `nytprof-engine` | done | Schema [`docs/schemas/perl-engine-dispatch-mvp-v0.md`](schemas/perl-engine-dispatch-mvp-v0.md); `EngineDispatch` allowed actions + native subprocess (no Perl reimplementation); legacy = stream-dump smoke + `NOTE:`; smoke `./scripts/packaging/perl_engine_export_smoke.sh` — default-calls1 folded `main::mid;main::leaf 15` / `main::RUNTIME;main::mid 3`, callgrind `fn=main::leaf` + `calls`. **Before COL-007.** |
| INCOMPLETE-STREAM | Fail closed on record-aligned incomplete streams (no silent verify OK) | done | Contract [`docs/contracts/COMPAT-010_INCOMPLETE_STREAM.md`](contracts/COMPAT-010_INCOMPLETE_STREAM.md): after decode/model, require PID balance (`pid_end >= pid_start` when starts seen) **and** `TIME_LINE+TIME_BLOCK > 0`. Default `verify`/`report` → `Err` / exit ≠ 0; dump lenient; salvage `NYTPROF_ALLOW_INCOMPLETE=1` → verify `INCOMPLETE:`. Model `pid_start_events`/`pid_end_events`; evidence `verify_profile_incomplete_prefix_*`, `stream_completeness_*`, CLI `incomplete_stream_cli_*`, smoke `tools/oracle/selftest_incomplete_stream.sh`. **Before COL-007.** Not full SEC recovery freeze |
| CSV-SEMANTIC-PARITY | Native CSV path semantics (leaf/mid/edge) on default-calls1 | done | Schema [`docs/schemas/csv-semantic-parity-mvp-v0.md`](schemas/csv-semantic-parity-mvp-v0.md); model+CSV test `csv_semantic_parity_default_calls1` (`nytprof-report`); smoke `./tools/oracle/csv_semantic_parity.sh` (native `csv` ×2 + optional cargo gate). Exact: leaf returns **15**, mid **3**, mid→leaf **15** as `main::leaf,15,` / `main::mid,3,` / `main::mid,main::leaf,15,`. Consolidates `subs_csv_*` / `edges_csv_*` / `csv_report_dual_section`. **Not** full Reader/`nytprofcsv` dialect. **Before COL-007.** |
| EXPORT-SEMANTIC-PARITY | Folded + callgrind export semantics (leaf/mid/edge) on default-calls1 | done | Schema [`docs/schemas/export-semantic-parity-mvp-v0.md`](schemas/export-semantic-parity-mvp-v0.md); model+export test `export_semantic_parity_default_calls1` (`nytprof-report`); smoke `./tools/oracle/export_semantic_parity.sh` (native `folded` + `callgrind`/`cg` ×2 each + optional cargo gate). Exact: leaf **15**, mid **3**, mid→leaf **15** as folded `main::mid;main::leaf 15` / `main::RUNTIME;main::mid 3`; callgrind `cfn=main::leaf` + `calls=15`, `cfn=main::mid` + `calls=3`. Consolidates `folded_stacks_default_calls1_real_render` / `callgrind_default_calls1_real_render`. **Not** full `nytprofcg`/`nytprofcalls`. **Before COL-007.** |
| REPORT-CONTRACT-FREEZE | Provisional freeze of advertised native report surfaces + semantic counts | done | Contract [`docs/contracts/REPORT_SURFACE_CONTRACT_v0.md`](contracts/REPORT_SURFACE_CONTRACT_v0.md): text/HTML single/HTML multi/CSV dual-section/callgrind/folded/verify/dump **advertised**; frozen default-calls1 leaf **15** / mid **3** / mid→leaf **15**; blocks-calls1 line5 calls **780**; **not** full REPORT-001..020 / DOM / flame / Graphviz. Evidence smoke: `./tools/oracle/report_semantic_parity.sh` (oracle `nytprofhtml` under isolated PERL5LIB only — never `crates/` — + native `html -o` / `--out-dir`). Links existing MVP schemas under `docs/schemas/*`. **Before COL-007.** |
| REPORT-HTML-RESIDUAL-INV | Inventory oracle `nytprofhtml` artifacts vs native HTML paths (residual honesty) | done | Contract [`docs/contracts/REPORT_HTML_RESIDUAL_INVENTORY_v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/contracts/REPORT_HTML_RESIDUAL_INVENTORY_v0.md): default-calls1 artifact class matrix (index / per-file line pages / CSS/JS / flame SVG / `.dot` / packages-callgraph / source pages); residual yes/partial/no; produce oracle via `tools/oracle/env.sh` + File::Which bootstrap; lister `tools/oracle/list_html_artifacts.sh`; semantic re-verify `./tools/oracle/report_semantic_parity.sh` (leaf **15** / mid **3** / mid→leaf **15**, non-empty oracle). Pointed from R1 residual matrix report row + REPORT_SURFACE residual honesty. Never `crates/` on oracle PERL5LIB. **Before COL-007.** |
| M01-HTML-JS-WAIVE | WAIVE shared JS / tablesorter for GA-candidate (docs) | **done (docs)** | **PR-M01** / Q4 user-final: GA-candidate does **not** require shipping jquery/tablesorter/floatThead. Inventory Shared JS disposition **WAIVE**; ADR-0003 Amendment 2026-08-12; residual matrix + runbook honesty. Native HTML remains MVP (A01 CSS + A02 excl), **not** oracle DOM. **Not** a tablesorter implementation. Flame A03 is **closed (MVP)** opt-in `--flame` (not `flamegraph.pl`). |
| DECODE-FUZZ-MVP | Deterministic corrupt/truncate/mutate battery: decode+verify never panic | done | Schema [`docs/schemas/decode-fuzz-mvp-v0.md`](schemas/decode-fuzz-mvp-v0.md): empty / bad magic / half default-calls1 / stepped prefixes / ~32–64 single-byte XOR flips call shipped `decode_all`/`decode_path`/`verify_profile` only → `Ok` or `Err`, **never panic**; fail-closed empty/half/magic → verify `Err`. Integration tests `crates/nytprof-format-v5/tests/decode_fuzz.rs` + `crates/nytprof-report/tests/decode_fuzz.rs` (`decode_fuzz_no_panic_*` / `fuzz_truncated_mutations*`); smoke `tools/oracle/selftest_decode_fuzz.sh` (harness step). **Before COL-007.** Not full SEC-002 continuous fuzz |
| CAPABILITY-SELFTEST | Documented runnable native offline capability self-test | done | Schema [`docs/schemas/capability-selftest-mvp-v0.md`](schemas/capability-selftest-mvp-v0.md); CLI `nytprof-cli capability` / `selftest` / `capabilities` → `OK: native capability self-test` + `decode/report/verify: yes` + optional golden `profile_ok:` probe (CWD / `CARGO_MANIFEST_DIR` / `--profile`); exit 0 on success, non-zero if claimed tools fail verify. Smoke `./scripts/packaging/capability_selftest_smoke.sh` (capability×2 + markers; fails closed without native CLI/cargo). Wired into `packaging_gate.sh` and offline_gate step 7 (**CI-CAPABILITY-GATE**) when cargo or prefix/target CLI present. Never `crates/` on oracle PERL5LIB. **Before COL-007.** Not full BUILD-005 codec/ABI manifest. |
| CAPABILITY-JSON-MVP | Structured JSON form for capability / selftest | done | Schema [`docs/schemas/capability-selftest-mvp-v0.md`](schemas/capability-selftest-mvp-v0.md) JSON section; CLI `capability --json` / `--format=json` / `--format json` → `{"ok":true,"decode":true,"report":true,"verify":true,"profile_ok":"<path>"|null}` on exit 0; human greppable default unchanged. Smoke `./scripts/packaging/capability_selftest_smoke.sh` (capability --json ×2 + parse + field asserts); cargo tests in `crates/nytprof-cli/tests/capability_selftest.rs`. Never `crates/` on oracle PERL5LIB. **Before COL-007.** Not full BUILD-005 codec/ABI manifest. |
| NATIVE-AGG-JSON | Shipped native CLI structured JSON aggregates from real ProfileModel | done | Schema [`docs/schemas/native-aggregates-json-mvp-v0.md`](schemas/native-aggregates-json-mvp-v0.md); preferred CLI `nytprof-cli report --json <profile.out>` (also `report PATH --json`, `--format=json`, `summary --json`, `aggregates` / `agg`); fields `ok`/`profile`/`leaf_returns`/`mid_returns`/`mid_leaf_edge`/`discount_events`/`subs`/`edges` (TAB edge keys); `load_model_for_report` fail-closed; cargo `crates/nytprof-cli/tests/native_agg_json.rs`; smoke `./scripts/packaging/native_agg_json_smoke.sh` (×2 → **15/3/15**). Never `crates/` on oracle PERL5LIB. **Before COL-007.** |
| NATIVE-QUERY-JSON-CROSS | Cross-check native `report --json` vs Perl `query --json` shared fields | done | Smoke `./scripts/packaging/native_query_json_cross_smoke.sh`: real CLIs only (no re-aggregation); default-calls1 native `report --json` vs `nytprof-engine query --json --jsonl` pair ×2; equal `leaf_returns`/**15**, `mid_returns`/**3**, `mid_leaf_edge`/**15**, `discount_events`/**818**; optional `query --json <profile>` dump path. Schemas: [`native-aggregates-json-mvp-v0.md`](schemas/native-aggregates-json-mvp-v0.md), [`perl-engine-dispatch-mvp-v0.md`](schemas/perl-engine-dispatch-mvp-v0.md). Wired into `offline_gate.sh` when native available. Never `crates/` on oracle PERL5LIB. **Before COL-007.** |
| NATIVE-QUERY-JSON-CROSS-EXPAND | Expand cross-parity shared fields with `sub_entry_events` | done | Same smoke `./scripts/packaging/native_query_json_cross_smoke.sh`: when **both** sides expose `sub_entry_events`, default-calls1 equal **0** (pair ×2 + dump path); **fixture-scoped expand** on `fixtures/v5/calls2-default` side-by-side native `report --json` + Perl `query --json --jsonl` → **27**. Real CLIs only; skip-with-NOTE if only one side exposes the field. Schemas: [`native-aggregates-json-mvp-v0.md`](schemas/native-aggregates-json-mvp-v0.md), [`perl-engine-dispatch-mvp-v0.md`](schemas/perl-engine-dispatch-mvp-v0.md). Offline_gate step 8 when native. Residual honesty: still no production FFI/XS Data, no full nytprofhtml DOM, no v6/COL-007, no multi-OS CI, no perf claims. Never `crates/` on oracle PERL5LIB. **Before COL-007.** |
| NATIVE-QUERY-JSON-CROSS-BLOCKS | Cross-check A4/A4b greppable ints on blocks-calls1 native↔query | done | Same smoke `./scripts/packaging/native_query_json_cross_smoke.sh`: on `fixtures/v5/blocks-calls1`, native `report --json` vs Perl `query --json --jsonl` pair ×2; assert `line_calls_1_5` **780** and `block_line_calls_1_4` **810** equal native↔perl (real CLIs only; no re-aggregation). Keeps default-calls1 shared fields **15/3/15** + discount **818** + `sub_entry` **0** when both expose; calls2 expand **27** when both expose. Schemas: [`native-aggregates-json-mvp-v0.md`](schemas/native-aggregates-json-mvp-v0.md), [`perl-engine-dispatch-mvp-v0.md`](schemas/perl-engine-dispatch-mvp-v0.md). Residual honesty: still no production FFI/XS Data, no full nytprofhtml DOM, no v6/COL-007, no multi-OS CI, no perf claims. Never `crates/` on oracle PERL5LIB. **Before COL-007.** |
| NATIVE-QUERY-JSON-CROSS-META | Cross-check stream/PID + A9/A8 samples on default-calls1 native↔query | done | Same smoke `./scripts/packaging/native_query_json_cross_smoke.sh`: on `fixtures/v5/default-calls1` pair ×2 (+ dump path), when **both** sides expose fields: equal `is_stream_complete` **true**, `time_line_events` / `pid_start_events` / `pid_end_events`, prefer equal empty `incompleteness_reasons` (**JSON-NATIVE-STREAM-MVP**); equal `sub_def_leaf` / `sub_def_mid` (fid/first_line/last_line) + `source_line_1_5` (**JSON-SUBDEF-SOURCE-MVP**). Optional scalar meta (e.g. `attribute_ticks_per_sec`) equal only when both expose. Keeps leaf/mid/edge **15/3/15**, discount **818**, `sub_entry` **0**, calls2 **27**, blocks **780/810**. Real CLIs only; no re-aggregation; never `crates/` on oracle PERL5LIB. Schemas: [`native-aggregates-json-mvp-v0.md`](schemas/native-aggregates-json-mvp-v0.md), [`perl-engine-dispatch-mvp-v0.md`](schemas/perl-engine-dispatch-mvp-v0.md). **Before COL-007.** |
| NATIVE-QUERY-JSON-CROSS-TIMEBLOCK | Cross-check `time_block_events` + require greppable meta when both expose | done | Same smoke `./scripts/packaging/native_query_json_cross_smoke.sh`: when **both** sides expose `time_block_events`, default-calls1 equal **0** (pair ×2 + dump path); blocks-calls1 equal **916** (pair ×2, with A4/A4b **780**/**810**). Greppable meta **required** equal when both expose on default-calls1: `attribute_ticks_per_sec`, `option_calls`, `file_1` (exact equal **or** both paths contain **`workload.pl`**); skip-with-NOTE only if missing on one side. Keeps leaf/mid/edge **15/3/15**, discount **818**, `sub_entry` **0**/calls2 **27**, stream/PID + A9/A8 (**CROSS-META**). Real CLIs only; no re-aggregation; never `crates/` on oracle PERL5LIB. Schemas: [`native-aggregates-json-mvp-v0.md`](schemas/native-aggregates-json-mvp-v0.md), [`perl-engine-dispatch-mvp-v0.md`](schemas/perl-engine-dispatch-mvp-v0.md). Emission: native `render_aggregates_json` + Perl `EngineDispatch` `print_query_results` (`time_block_events` from ProfileModel / JsonlData). **Before COL-007.** |
| NATIVE-QUERY-JSON-CROSS-COUNTS | Cross-check event counters + `file_1_basename` on default-calls1 native↔query | done | Same smoke `./scripts/packaging/native_query_json_cross_smoke.sh`: on default-calls1 pair ×2 (+ dump path), when **both** sides expose fields: equal `sub_return_events` **27**, `new_fid_events` **3**, `sub_callers_events` **13**, `src_line_events` **632**, `sub_info_events` **31** (**JSON-EVENT-COUNTS-MVP**); equal `file_1_basename` exact **or** both contain **`workload.pl`** (**JSON-FILE-BASENAME-MVP**). Absolute `file_1` remains volatile; basename is the greppable stable sample. Keeps leaf/mid/edge **15/3/15**, discount **818**, `sub_entry` **0**/calls2 **27**, blocks **780/810**, `time_block` **0**/**916**, stream/PID + A9/A8 + greppable meta. Real CLIs only; no re-aggregation; never `crates/` on oracle PERL5LIB. Schemas: [`native-aggregates-json-mvp-v0.md`](schemas/native-aggregates-json-mvp-v0.md), [`perl-engine-dispatch-mvp-v0.md`](schemas/perl-engine-dispatch-mvp-v0.md). Residual honesty: still no production FFI/XS Data, no full nytprofhtml DOM, no v6/COL-007, no multi-OS CI, no perf claims. **Before COL-007.** |
| NATIVE-QUERY-JSON-CROSS-TOTAL | Cross-check `total_events` + `attribute_basetime` on default-calls1 native↔query | done | Same smoke `./scripts/packaging/native_query_json_cross_smoke.sh`: on default-calls1 pair ×2 (+ dump path), when **both** sides expose fields: equal `total_events` **2474** (**JSON-TOTAL-EVENTS-MVP**; dump stream incl. synthetic `_END`) and equal `attribute_basetime` dump string (**JSON-ATTR-BASETIME-MVP**; golden often **`"1786111723"`**). Keeps prior CROSS fields. Real CLIs only; no re-aggregation; never `crates/` on oracle PERL5LIB. Schemas: [`native-aggregates-json-mvp-v0.md`](schemas/native-aggregates-json-mvp-v0.md), [`perl-engine-dispatch-mvp-v0.md`](schemas/perl-engine-dispatch-mvp-v0.md). Offline_gate step 8 when native. **Before COL-007.** |
| JSON-TOTAL-EVENTS-MVP | Expose dump-stream `total_events` on both JSON surfaces | done | Field **`total_events`** on native `report --json` / `aggregates` (`ProfileModel.total_events + 1` for dump-only synthetic `_END`) and Perl `query --json` (`JsonlData.records_seen`); default-calls1 **2474** = golden `readstream.jsonl` / `nytprof-cli dump` line count. Schema notes: model decoded-tag count is **2473**. Cargo `native_agg_json.rs`; smoke `./scripts/packaging/json_total_basetime_smoke.sh`. Wired offline_gate step 6i. Cross-parity: **NATIVE-QUERY-JSON-CROSS-TOTAL**. No full maps/FFI/XS/COL-007; never `crates/` on oracle PERL5LIB. **Before COL-007.** |
| JSON-ATTR-BASETIME-MVP | Expose greppable ATTRIBUTE `basetime` sample on both JSON surfaces | done | Field **`attribute_basetime`** (string-or-null) on native `report --json` (`attributes["basetime"]`) and Perl `query --json` (`attribute('basetime')`); default-calls1 golden **`"1786111723"`** (dump sample, not wall-clock policy freeze). Smoke `./scripts/packaging/json_total_basetime_smoke.sh` (shared with TOTAL-EVENTS). Wired offline_gate step 6i. Cross-parity: **NATIVE-QUERY-JSON-CROSS-TOTAL**. No full ATTRIBUTE map/DOM/XS; never `crates/` on oracle PERL5LIB. **Before COL-007.** |
| JSON-EVENT-COUNTS-MVP | Expose dump/model stream tag multiplicities on both JSON surfaces | done | Fields **`sub_return_events`** / **`new_fid_events`** / **`sub_callers_events`** / **`src_line_events`** / **`sub_info_events`** on native `report --json` / `aggregates` (`ProfileModel.*`) and Perl `query --json` (`JsonlData` counters + accessors); schemas [`native-aggregates-json-mvp-v0.md`](schemas/native-aggregates-json-mvp-v0.md) + [`perl-engine-dispatch-mvp-v0.md`](schemas/perl-engine-dispatch-mvp-v0.md) + [`perl-jsonl-data-mvp-v0.md`](schemas/perl-jsonl-data-mvp-v0.md). Cargo `crates/nytprof-cli/tests/native_agg_json.rs` (default-calls1 model-matched **27**/**3**/**13**/**632**/**31**); smoke `./scripts/packaging/json_event_counts_smoke.sh` (both surfaces + optional golden tag recount). Wired offline_gate step 6h. Cross-parity: **NATIVE-QUERY-JSON-CROSS-COUNTS**. No full maps/FFI/XS/COL-007; never `crates/` on oracle PERL5LIB. **Before COL-007.** |
| JSON-SUB-ENTRY-MVP | Expose dump/model `sub_entry_events` on both JSON surfaces | done | Field **`sub_entry_events`** on native `report --json` / `aggregates` (`ProfileModel.sub_entry_events`) and Perl `query --json` (`$data->sub_entry_events`); schemas [`native-aggregates-json-mvp-v0.md`](schemas/native-aggregates-json-mvp-v0.md) + [`perl-engine-dispatch-mvp-v0.md`](schemas/perl-engine-dispatch-mvp-v0.md). Cargo `crates/nytprof-cli/tests/native_agg_json.rs` (default-calls1 **0**, calls2-default **27**); smoke `./scripts/packaging/json_sub_entry_smoke.sh` + extended `native_agg_json_smoke` / `perl_query_json_smoke` (default **0**). Wired into offline_gate after query JSON. No XS; never `crates/` on oracle PERL5LIB. **Before COL-007.** |
| ENGINE-AUTO-SMOKE | Document + exercise `--engine=auto` / `NYTPROF_ENGINE=auto` | done | Schema notes in [`docs/schemas/engine-selection-mvp-v0.md`](schemas/engine-selection-mvp-v0.md) + [`docs/schemas/perl-engine-dispatch-mvp-v0.md`](schemas/perl-engine-dispatch-mvp-v0.md). Smoke `./scripts/packaging/engine_auto_smoke.sh` — `--engine=auto` report ×2 + env report + query on default-calls1 → **leaf=15** / **mid=3** when native discoverable. True fallback policy: **ENGINE-AUTO-FALLBACK**. Never `crates/` on oracle PERL5LIB. **Before COL-007.** |
| ENGINE-AUTO-FALLBACK | True `engine=auto` prefer-native / fall-back-legacy on Perl facade | done | `resolve_engine` returns `native\|legacy\|auto` (no collapse); `select_runtime_engine($repo,$req)` → auto tries `find_native_cli` else legacy + STDERR note; `NYTPROF_FORCE_NO_NATIVE=1` test hook. Smoke `./scripts/packaging/engine_auto_fallback_smoke.sh` — native present leaf **15**/mid **3**; force-no-native auto report/verify exit 0 legacy; explicit native+force fails. Schemas updated; Rust `nytprof-cli` auto residual still → native. Never `crates/` on oracle PERL5LIB. **Before COL-007.** |
| CI-OFFLINE-GATE | Single fail-fast offline R1 gate | done | `./scripts/ci/offline_gate.sh` (+ `make offline-gate`): (1) `cargo test -p nytprof-format-v5 -p nytprof-model -p nytprof-report -p nytprof-cli` with honest skip if cargo absent; (2) required `./tools/oracle/selftest_harness.sh`; (3) primary packaging `./scripts/packaging/dual_path_smoke.sh` (not packaging_gate / makemaker). Fail-fast banners; never `crates/` on oracle PERL5LIB; not multi-OS CI. Docs: [`docs/BUILD_SUPPORT_POLICY.md`](BUILD_SUPPORT_POLICY.md), README. **Before COL-007.** |
| CI-OFFLINE-GATE-EXPAND | Expand offline gate fail-fast steps | done | Same `./scripts/ci/offline_gate.sh`: after dual_path, (4) required `./scripts/packaging/engine_auto_fallback_smoke.sh`; (5) required `./scripts/packaging/perl_jsonl_data_all_smoke.sh` thin roll-up of pure-Perl JsonlData smokes (`perl_jsonl_data` / `line_totals` / `subdefs` / `source` / `a4b` / `meta` / `pid` / `stream_complete` / `discount` / `sub_entry`). Fail-fast banners; never `crates/` on oracle PERL5LIB. Docs: [`docs/BUILD_SUPPORT_POLICY.md`](BUILD_SUPPORT_POLICY.md), README. **Before COL-007.** |
| CI-CAPABILITY-GATE | Wire capability_selftest into offline gate | done | Same `./scripts/ci/offline_gate.sh` step 9: `./scripts/packaging/capability_selftest_smoke.sh` when cargo **or** `prefix`/`target` native CLI (or `$NYTPROF_NATIVE_CLI`) present — same condition as `packaging_gate`; honest skip otherwise. dual_path with cargo typically installs `prefix/bin` first so step usually runs (after optional native_agg + cross steps 7–8). Fail-fast banner; never `crates/` on oracle PERL5LIB. Docs: [`docs/BUILD_SUPPORT_POLICY.md`](BUILD_SUPPORT_POLICY.md), README offline gate table, residual matrix offline gate row. **Before COL-007.** |
| CI-QUERY-JSON-GATE | Wire query-JSON smoke into offline gate | done | Same `./scripts/ci/offline_gate.sh` step 6 (after `perl_jsonl_data_all`; before capability): required `./scripts/packaging/perl_query_json_smoke.sh` (QUERY-JSON-MVP / QUERY-JSON-EXPAND golden `--jsonl`; no cargo). Fail if script missing; fail-fast banner; never `crates/` on oracle PERL5LIB. Docs: [`docs/BUILD_SUPPORT_POLICY.md`](BUILD_SUPPORT_POLICY.md), README, [`docs/R1_PREVIEW_OPERATOR_RUNBOOK.md`](R1_PREVIEW_OPERATOR_RUNBOOK.md) offline gate table, residual matrix offline gate row. **Before COL-007.** |
| R1-RESIDUAL-MATRIX | Freeze advertised offline R0/R1-preview ready vs residual full R1 | done | Contract [`docs/contracts/R1_RESIDUAL_READINESS_MATRIX_v0.md`](contracts/R1_RESIDUAL_READINESS_MATRIX_v0.md): native CLI dump/verify/report/html/csv/folded/callgrind + aggregates JSON (incl. **JSON-SUB-ENTRY-MVP** + **JSON-BLOCKS-MVP** + **JSON-META-FILES-MVP** + **JSON-SUBDEF-SOURCE-MVP** + **JSON-NATIVE-STREAM-MVP**) + parity gates (incl. **NATIVE-QUERY-JSON-CROSS** / **CROSS-EXPAND** / **CROSS-BLOCKS** / **CROSS-META**) + dual-path/MakeMaker facade/offline_gate/prefix + `nytprof-engine` actions (incl. expanded `query` / query JSON) + pure-Perl JsonlData (returns/edges/line_totals/A4b/sub_defs/files/source_lines/attributes/options/PID lifecycle/stream completeness/A3 discount + **SUB_ENTRY multiplicity**). Residual: no RUST-010 FFI/cdylib, no PERL-004/005 XS materializers, no full REPORT-001..020 DOM, wire freeze done (ADR-0006); COL-008 deferred; COL-007 E3-EVENT done, no BUILD-003 full / BUILD-006 multi-OS CI, no perf claims; exclusive-time discount policy freeze remains residual; Perl `engine=auto` prefer-native/fallback (ENGINE-AUTO-FALLBACK); Rust CLI auto still → native. Provisional snapshot — not release certification. Operator map: [`docs/R1_PREVIEW_OPERATOR_RUNBOOK.md`](R1_PREVIEW_OPERATOR_RUNBOOK.md). Honesty sync: **R1-HONESTY-SYNC**. **Before COL-007.** |
| R1-PREVIEW-RUNBOOK | Operator runbook consolidating offline R0 / R1-preview stack | done | [`docs/R1_PREVIEW_OPERATOR_RUNBOOK.md`](R1_PREVIEW_OPERATOR_RUNBOOK.md): what this is (R0 / R1-preview opt-in, not full R1); one-command `./scripts/ci/offline_gate.sh` steps 1–9; `install_native.sh` + `capability` / `--json`; NATIVE-AGG-JSON + **NATIVE-QUERY-JSON-CROSS** / **CROSS-EXPAND** / **CROSS-BLOCKS** / **CROSS-META** + **JSON-SUB-ENTRY-MVP** + **JSON-BLOCKS-MVP** + **JSON-META-FILES-MVP**; facade `nytprof-engine` report/query/auto/fallback; pure-Perl JsonlData roll-up + key APIs incl. **SUB_ENTRY**; contracts (REPORT_SURFACE, residual matrix, HTML residual inventory); residual honesty (no FFI/XS Data, no full DOM, no v6/COL-007, no multi-OS CI, no perf claims); golden fixtures default-calls1 leaf/mid **15/3** edge **15**, meta samples **10000000**/**1**/`workload.pl`, blocks-calls1 line5 **780** + JSON A4/A4b **780**/**810**, calls2-default SUB_ENTRY **27**. Matrix points at runbook. Honesty sync: **R1-HONESTY-SYNC**. **Before COL-007.** |
| R1-HONESTY-SYNC | Sync residual matrix + operator runbook to advertised preview honesty | done | Updated [`docs/contracts/R1_RESIDUAL_READINESS_MATRIX_v0.md`](contracts/R1_RESIDUAL_READINESS_MATRIX_v0.md) + [`docs/R1_PREVIEW_OPERATOR_RUNBOOK.md`](R1_PREVIEW_OPERATOR_RUNBOOK.md) to advertise **native↔query JSON cross-parity** / **CROSS-EXPAND** / **CROSS-BLOCKS** / **CROSS-META** / **CROSS-TIMEBLOCK** / **CROSS-COUNTS** (`scripts/packaging/native_query_json_cross_smoke.sh`; offline_gate step 8 when native; blocks-calls1 **780**/**810**; `time_block_events` **0**/**916**; event counts **27/3/13/632/31** + `file_1_basename`; stream/PID + A9/A8 + greppable meta), **JSON-EVENT-COUNTS-MVP**, **JSON-FILE-BASENAME-MVP** (absolute `file_1` volatile; basename greppable stable sample), **JSON-TIME-BLOCK-MVP**, **JSON-REPORT-INCOMPLETE-FAILCLOSED** (`json_report_incomplete_smoke.sh`), **JSON-META-FILES-MVP** (`attribute_ticks_per_sec` / `option_calls` / `file_1`), **SUB_ENTRY on JSON surfaces**, **blocks A4/A4b JSON convenience**, and **pure-Perl SUB_ENTRY multiplicity** while still listing full-R1 residuals: no production FFI/XS Data, no full nytprofhtml DOM, COL-007 E3-EVENT done with C (E3-mixed residual), no multi-OS CI, no perf claims. **Before COL-007.** |
| JSON-BLOCKS-MVP | Greppable A4/A4b ints on `query --json` (+ native `report --json`) | done | Schema [`docs/schemas/perl-engine-dispatch-mvp-v0.md`](schemas/perl-engine-dispatch-mvp-v0.md) + [`native-aggregates-json-mvp-v0.md`](schemas/native-aggregates-json-mvp-v0.md): always-present `line_calls_1_5` / `block_line_calls_1_4` from JsonlData `line_calls(1,5)` / `block_line_calls(1,4)` and ProfileModel `line_total` / `block_line_total`. Smoke `./scripts/packaging/json_blocks_smoke.sh` — blocks-calls1 **780** / **810**; default-calls1 line ≥1 / block **0**; optional native path. Wired offline_gate step 6c. No full maps/DOM/XS; never `crates/` on oracle PERL5LIB. **Before COL-007.** |
| JSON-NATIVE-STREAM-MVP | Dump/model stream + PID fields on native `report --json` | done | Schema [`docs/schemas/native-aggregates-json-mvp-v0.md`](schemas/native-aggregates-json-mvp-v0.md): `is_stream_complete` **true**, `incompleteness_reasons` **[]**, `time_line_events` / `pid_start_events` / `pid_end_events` from `ProfileModel` only (default-calls1 ≥1 each; golden TL **916**). Matches Perl `query --json` keys (QUERY-JSON-EXPAND); Perl fields retained. Cargo `crates/nytprof-cli/tests/native_agg_json.rs` (model-matched); smokes `./scripts/packaging/native_agg_json_smoke.sh` + `./scripts/packaging/json_native_stream_smoke.sh` (optional Perl shared-field compare). Wired offline_gate with native_agg step when native. No FFI/XS/COL-007; never `crates/` on oracle PERL5LIB. **Before COL-007.** |
| JSON-SUBDEF-SOURCE-MVP | Greppable A9/A8 samples on `query --json` (+ native `report --json`) | done | Schema [`docs/schemas/perl-engine-dispatch-mvp-v0.md`](schemas/perl-engine-dispatch-mvp-v0.md) + [`native-aggregates-json-mvp-v0.md`](schemas/native-aggregates-json-mvp-v0.md): always-present `sub_def_leaf` / `sub_def_mid` / `source_line_1_5` from JsonlData `sub_def` / `source_line` and ProfileModel `sub_def` / `source_line` (**null** when absent). Smoke `./scripts/packaging/json_subdef_source_smoke.sh` — default-calls1 leaf **fid=1 first=3 last=7**, mid **1/8–12**, source contains **`$x++`** and **`1 .. 50`** (golden exact often `    $x++ for 1 .. 50;\n`); optional native path. Wired offline_gate step 6d. Cargo asserts in `native_agg_json.rs`. No full A8/A9 maps/DOM/XS; never `crates/` on oracle PERL5LIB. **Before COL-007.** |
| JSON-META-FILES-MVP | Greppable ATTRIBUTE/OPTION/NEW_FID samples on both JSON surfaces | done | Schema [`docs/schemas/perl-engine-dispatch-mvp-v0.md`](schemas/perl-engine-dispatch-mvp-v0.md) + [`native-aggregates-json-mvp-v0.md`](schemas/native-aggregates-json-mvp-v0.md): always-present string-or-null `attribute_ticks_per_sec` / `option_calls` / `file_1` from JsonlData `attribute` / `option` / `file` and ProfileModel `attributes` / `options` / `file_name`. Smoke `./scripts/packaging/json_meta_files_smoke.sh` — default-calls1 **ticks_per_sec=10000000**, **calls=1**, **file_1** contains **workload.pl** (JsonlData/model + optional golden ATTRIBUTE/OPTION/NEW_FID recount); optional native path. Wired offline_gate step 6e. Cargo asserts in `native_agg_json.rs`. No full ATTRIBUTE/OPTION/files maps/DOM/XS; never `crates/` on oracle PERL5LIB. **Before COL-007.** |
| JSON-FILE-BASENAME-MVP | Greppable stable `file_1_basename` on both JSON surfaces | done | Schema [`docs/schemas/perl-engine-dispatch-mvp-v0.md`](schemas/perl-engine-dispatch-mvp-v0.md) + [`native-aggregates-json-mvp-v0.md`](schemas/native-aggregates-json-mvp-v0.md): always-present string-or-null `file_1_basename` from JsonlData `file_basename(1)` and ProfileModel `fid_basename(1)`. Smoke `./scripts/packaging/json_file_basename_smoke.sh` — default-calls1 equals/contains **`workload.pl`** (typically exact **`"workload.pl"`**); real CLIs only; optional native path. Wired offline_gate step 6g. Cargo asserts in `native_agg_json.rs`. Absolute `file_1` remains volatile (`/tmp/...`) — basename is the greppable stable sample / contract. Cross-parity: **NATIVE-QUERY-JSON-CROSS-COUNTS**. Residual honesty: still no production FFI/XS Data, no full nytprofhtml DOM, no v6/COL-007, no multi-OS CI, no perf claims. No full files map/DOM/XS; never `crates/` on oracle PERL5LIB. **Before COL-007.** |
| JSON-TIME-BLOCK-MVP | Expose dump/model `time_block_events` (A2) on both JSON surfaces | done | Field **`time_block_events`** on native `report --json` / `aggregates` (`ProfileModel.time_block_events`) and Perl `query --json` (`$data->time_block_events`); schemas [`native-aggregates-json-mvp-v0.md`](schemas/native-aggregates-json-mvp-v0.md) + [`perl-engine-dispatch-mvp-v0.md`](schemas/perl-engine-dispatch-mvp-v0.md). Cargo `crates/nytprof-cli/tests/native_agg_json.rs` (default-calls1 **0**, blocks-calls1 **916**, model-matched); smoke `./scripts/packaging/json_time_block_smoke.sh` (both surfaces + optional golden TIME_BLOCK tag recount). Wired offline_gate step 6f. No full maps/FFI/XS/COL-007; never `crates/` on oracle PERL5LIB. **Before COL-007.** |
| JSON-REPORT-INCOMPLETE-FAILCLOSED | Shipped `report --json` / aggregates fail closed on incomplete streams | done | Aligns with **INCOMPLETE-STREAM** / [`COMPAT-010_INCOMPLETE_STREAM`](contracts/COMPAT-010_INCOMPLETE_STREAM.md): `load_model_for_report` → `require_complete_stream` (same as text report; salvage `NYTPROF_ALLOW_INCOMPLETE=1`). Default incomplete 500-byte prefix of default-calls1 → exit ≠ 0; must **not** print successful complete `{"ok":true,"is_stream_complete":true,...}`. Smoke `./scripts/packaging/json_report_incomplete_smoke.sh` (report --json / --format=json / aggregates / agg + golden complete + salvage note); cargo `incomplete_stream_report_json_prefix_default_calls1` (`fail_closed.rs`) + `report_json_incomplete_prefix_fails_closed` (`native_agg_json.rs`). Wired offline_gate when native (with native_agg / stream). Residual honesty: still no production FFI/XS Data, no full nytprofhtml DOM, no v6/COL-007, no multi-OS CI, no perf claims. Never `crates/` on oracle PERL5LIB. **Before COL-007.** |
| FMT-V6-HEADER-PROVISIONAL | v6 fixed-header contract (IDs frozen ADR-0006) | done | Schema [`docs/schemas/v6-fixed-header-provisional-v0.md`](schemas/v6-fixed-header-provisional-v0.md): magic `NYTPROF6`, major/minor, header length, fail-closed rules. Status elevated by **FMT-V6-WIRE-FREEZE** / ADR-0006 (filename retained). Residual: CLI v6 default. |
| FMT-V6-HEADER-PARSE-MVP | Shipped fail-closed provisional v6 header parse + tests | done | Crate `crates/nytprof-format-v6`: `parse_fixed_header` on pure byte slices; unit tests for valid full header, bad magic, truncated, unsupported major. Evidence `cargo test -p nytprof-format-v6`. Wired into `offline_gate` cargo step. COL-007 C encoder still deferred. |
| FMT-V6-CHUNK-PROVISIONAL | v6 chunk-frame contract (IDs frozen ADR-0006) | done | Schema [`docs/schemas/v6-chunk-frame-provisional-v0.md`](schemas/v6-chunk-frame-provisional-v0.md): sync `NYT6`, kind/codec/flags, lengths. Status elevated by **FMT-V6-WIRE-FREEZE**. Residual: default-parse inflate. |
| FMT-V6-CHUNK-PARSE-MVP | Shipped fail-closed provisional v6 chunk-frame parse + tests | done | `nytprof-format-v6::chunk::parse_chunk_frame` on pure byte slices; tests for valid empty payload, bad sync, truncated, oversize lengths, unknown required kind, optional unknown kind. Evidence `cargo test -p nytprof-format-v6`. COL-007 C encoder still deferred. |
| FMT-V6-VARINT-PROVISIONAL | ULEB128 varint contract (algorithm frozen ADR-0006) | done | Schema [`docs/schemas/v6-varint-uleb128-provisional-v0.md`](schemas/v6-varint-uleb128-provisional-v0.md): canonical encode, max 10 bytes for `u64`, fail-closed. Status elevated by **FMT-V6-WIRE-FREEZE**. **Not** v5 packed-int dialect. |
| FMT-V6-VARINT-MVP | Shipped ULEB128 encode/decode + fail-closed tests | done | `nytprof-format-v6::varint::{encode_u64,decode_u64}` (strict); round-trip magnitudes, truncated, overlong → Err. Evidence `cargo test -p nytprof-format-v6`. COL-007 C encoder still deferred. |
| FMT-V6-SVARINT-PROVISIONAL | signed ZigZag+ULEB128 contract (algorithm frozen ADR-0006) | done | Schema [`docs/schemas/v6-svarint-zigzag-provisional-v0.md`](schemas/v6-svarint-zigzag-provisional-v0.md): ZigZag map + ULEB128. Status elevated by **FMT-V6-WIRE-FREEZE**. SLEB128 residual alternative remains. |
| FMT-V6-SVARINT-MVP | Shipped signed encode/decode + fail-closed tests | done | `encode_i64`/`decode_i64` compose ZigZag with strict `encode_u64`/`decode_u64`; round-trip negatives/zero/positives incl. `i64::MIN`/`MAX`; truncated + overlong → Err. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-STRING-PROVISIONAL | length-prefixed string/blob contract (frame frozen ADR-0006) | done | Schema [`docs/schemas/v6-string-blob-provisional-v0.md`](schemas/v6-string-blob-provisional-v0.md): id + length + flags + bytes. Status elevated by **FMT-V6-WIRE-FREEZE**. Global pool residual (OQ-6). |
| FMT-V6-STRING-MVP | Shipped string/blob encode/decode + fail-closed tests | done | `encode_string_blob`/`decode_string_blob`; empty + non-empty round-trip; truncated; oversize declared length. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-TLV-PROVISIONAL | header TLV frame contract (type_ids frozen ADR-0006) | done | Schema [`docs/schemas/v6-header-tlv-provisional-v0.md`](schemas/v6-header-tlv-provisional-v0.md): known PRODUCER/TICKS_PER_SEC. Status elevated by **FMT-V6-WIRE-FREEZE**. Full TLV catalog residual. |
| FMT-V6-TLV-MVP | Shipped header TLV encode/decode + fail-closed tests | done | `encode_tlv`/`decode_tlv`; known empty/non-empty round-trip; truncated; oversize; unknown required type. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-TLV-REGION-PROVISIONAL | multi-TLV header region + END (frozen ADR-0006) | done | Schema [`docs/schemas/v6-tlv-region-provisional-v0.md`](schemas/v6-tlv-region-provisional-v0.md): END terminator 0x7E. Status elevated by **FMT-V6-WIRE-FREEZE**. |
| FMT-V6-TLV-REGION-MVP | Shipped multi-TLV region encode/decode + fail-closed tests | done | `encode_tlv_region`/`decode_tlv_region` compose single-TLV APIs; empty region, ≥2 TLVs round-trip, truncated, missing terminator, unknown required. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-FILE-PREFIX-PROVISIONAL | Provisional v6 file-prefix composition (fixed header + multi-TLV region) | done | Schema [`docs/schemas/v6-file-prefix-provisional-v0.md`](schemas/v6-file-prefix-provisional-v0.md): TLV region starts at `header_len`; fail-closed bad magic / truncated / missing END. **Not** chunk stream; **not** COL-007 C writer. |
| FMT-V6-FILE-PREFIX-MVP | Shipped file-prefix encode/decode + fail-closed tests | done | `encode_file_prefix`/`decode_file_prefix` compose fixed-header + `encode_tlv_region`/`decode_tlv_region`; empty + multi-TLV round-trip; bad magic; truncated before END. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-PREFIX-CHUNK-STREAM-PROVISIONAL | Provisional prefix + chunk stream file layout (not wire freeze) | done | Schema [`docs/schemas/v6-prefix-chunk-stream-provisional-v0.md`](schemas/v6-prefix-chunk-stream-provisional-v0.md): `[file prefix][chunk…]`; codec **NONE** MVP; zero chunks Ok; fail-closed bad magic / truncated mid-chunk / bad sync. **Not** payload inflate; **not** COL-007 C writer. |
| FMT-V6-PREFIX-CHUNK-STREAM-MVP | Shipped prefix+chunk stream compose encode/decode + fail-closed tests | done | `encode_prefix_chunk_stream`/`decode_prefix_chunk_stream` compose shipped file-prefix + chunk frame APIs; 0/≥1 chunk round-trip (opaque payload, codec NONE); bad magic; truncated mid-chunk; bad sync. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-EVENT-BODY-PROVISIONAL | event-body opcode codec (opcodes/flags frozen ADR-0006) | done | Schema [`docs/schemas/v6-event-body-provisional-v0.md`](schemas/v6-event-body-provisional-v0.md): opcodes 0–19 + flags. Status elevated by **FMT-V6-WIRE-FREEZE**. Residual: full OI-002 key vocabulary; default-parse inflate. |
| FMT-V6-EVENT-BODY-MVP | Shipped event-body encode/decode + fail-closed tests | done | `encode_event_body`/`decode_event_body` compose shipped ULEB128 + string-blob; empty + ≥1 record round-trip; truncated mid-record; reserved 0; unknown required opcode. Optional codec NONE chunk payload smoke. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-MINI-PROFILE-PROVISIONAL | Provisional mini-profile composition (prefix + EVENT event-body + optional FOOTER) | done | Schema [`docs/schemas/v6-mini-profile-provisional-v0.md`](schemas/v6-mini-profile-provisional-v0.md): `[file prefix][EVENT codec NONE…][optional FOOTER]`; fail-closed bad magic / truncated / bad sync. **Not** inflate / full catalog; **not** COL-007 C writer. |
| FMT-V6-MINI-PROFILE-MVP | Shipped mini-profile compose encode/decode + fail-closed tests | done | `encode_mini_profile`/`decode_mini_profile` compose stream + event-body; empty prefix-only + ≥1 TIME_LINE/MARK round-trip; optional FOOTER; bad magic; truncated mid-chunk; truncated mid-event-body; bad sync. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-MULTI-CHUNK-EVENT-PROVISIONAL | Provisional multi-chunk EVENT body framing (not wire freeze) | done | Schema [`docs/schemas/v6-multi-chunk-event-provisional-v0.md`](schemas/v6-multi-chunk-event-provisional-v0.md): records-per-chunk split; reassemble ordered event-body across ≥2 codec-NONE EVENT chunks. **Not** mid-record span / inflate; **not** COL-007 C writer. |
| FMT-V6-MULTI-CHUNK-EVENT-MVP | Shipped multi-chunk EVENT encode/decode + fail-closed tests | done | `encode_multi_chunk_event_profile`/`decode_multi_chunk_event_profile` + `partition_event_records`; single-chunk + ≥2-chunk ordered round-trip; bad magic; truncated mid-chunk; truncated mid-record; bad sync. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-SOURCE-BODY-PROVISIONAL | Provisional SOURCE chunk body (codec NONE; not wire freeze) | done | Schema [`docs/schemas/v6-source-body-provisional-v0.md`](schemas/v6-source-body-provisional-v0.md): fid/line + string-blob text records; fail-closed truncated mid-record. **Not** full SRC_LINE catalog / inflate; **not** COL-007 C writer. |
| FMT-V6-SOURCE-BODY-MVP | Shipped SOURCE-body encode/decode + EVENT+SOURCE composition tests | done | `encode_source_body`/`decode_source_body`; empty + ≥1 record round-trip; truncated mid-record; codec-NONE SOURCE chunk payload; `encode_event_source_profile`/`decode_event_source_profile` mixed EVENT+SOURCE+FOOTER. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-INDEX-BODY-PROVISIONAL | Provisional INDEX chunk body (codec NONE; not wire freeze) | done | Schema [`docs/schemas/v6-index-body-provisional-v0.md`](schemas/v6-index-body-provisional-v0.md): key_id/file_offset/length + string-blob label; fail-closed truncated mid-record. **Not** full catalog / inflate; **not** COL-007 C writer. |
| FMT-V6-INDEX-BODY-MVP | Shipped INDEX-body encode/decode + mixed composition tests | done | `encode_index_body`/`decode_index_body`; empty + ≥1 record round-trip; truncated mid-record; codec-NONE INDEX chunk payload; `encode_mixed_kind_profile`/`decode_mixed_kind_profile` EVENT+SOURCE+INDEX+FOOTER. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-SUMMARY-BODY-PROVISIONAL | Provisional SUMMARY chunk body (codec NONE; not wire freeze) | done | Schema [`docs/schemas/v6-summary-body-provisional-v0.md`](schemas/v6-summary-body-provisional-v0.md): key_id/count/value + string-blob label; fail-closed truncated mid-record. **Not** full catalog / inflate; **not** COL-007 C writer. |
| FMT-V6-SUMMARY-BODY-MVP | Shipped SUMMARY-body encode/decode + mixed composition tests | done | `encode_summary_body`/`decode_summary_body`; empty + ≥1 record round-trip; truncated mid-record; codec-NONE SUMMARY chunk payload; mixed EVENT+SOURCE+INDEX+SUMMARY+FOOTER via `encode_mixed_kind_profile`. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-FOOTER-BODY-PROVISIONAL | Provisional FOOTER chunk body (codec NONE; not wire freeze) | done | Schema [`docs/schemas/v6-footer-body-provisional-v0.md`](schemas/v6-footer-body-provisional-v0.md): key_id/value + string-blob label; empty body Ok; FOOTER must be last. **Not** CRC freeze / inflate; **not** COL-007 C writer. |
| FMT-V6-FOOTER-BODY-MVP | Shipped FOOTER-body encode/decode + mixed composition tests | done | `encode_footer_body`/`decode_footer_body`; empty + ≥1 record round-trip; truncated mid-record; codec-NONE FOOTER payload; mixed profile with structured FOOTER last via `encode_mixed_kind_profile`. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-CRC-PROVISIONAL | Provisional CRC32 IEEE for header_crc + chunk payload_checksum (not wire freeze) | done | Schema [`docs/schemas/v6-crc-provisional-v0.md`](schemas/v6-crc-provisional-v0.md): CRC-32/IEEE; header covers `[0,32)`; payload-only checksum; optional verify fail-closed. **Not** always-on default parse; **not** COL-007 C writer. |
| FMT-V6-CRC-MVP | Shipped CRC compute/verify + sealed encode helpers + tests | done | `crc32_ieee` / `fill_header_crc` / `verify_header_crc` / `compute_payload_crc` / `verify_chunk_payload_crc`; `encode_fixed_header_full_sealed` / `encode_chunk_frame_sealed`; match + mismatch; standard `"123456789"` check. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-PAYLOAD-ZLIB-PROVISIONAL | Provisional chunk payload codec ZLIB (not wire freeze) | done | Schema [`docs/schemas/v6-payload-zlib-provisional-v0.md`](schemas/v6-payload-zlib-provisional-v0.md): codec ZLIB = zlib wire; `uncompressed_len` bounds inflate; NONE identity; fail-closed corrupt/size mismatch. **Not** default parse inflate; **not** COL-007 C writer. Sibling zstd/LZ4 preflights separate. |
| FMT-V6-PAYLOAD-ZLIB-MVP | Shipped ZLIB deflate/inflate + chunk composition tests | done | `deflate_zlib`/`inflate_zlib`/`decode_chunk_payload`/`encode_chunk_frame_zlib` (flate2); empty + non-empty round-trip; NONE identity; corrupt + size mismatch; ZLIB EVENT chunk composition. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-PAYLOAD-ZSTD-PROVISIONAL | Provisional chunk payload codec ZSTD (not wire freeze) | done | Schema [`docs/schemas/v6-payload-zstd-provisional-v0.md`](schemas/v6-payload-zstd-provisional-v0.md): codec ZSTD = 2; zstd frame wire; `uncompressed_len` bounds inflate; fail-closed corrupt/size. **Not** dictionaries; **not** default parse inflate; **not** COL-007 C writer. |
| FMT-V6-PAYLOAD-ZSTD-MVP | Shipped ZSTD compress/decompress + chunk composition tests | done | `compress_zstd`/`decompress_zstd`/`encode_chunk_frame_zstd` + `decode_chunk_payload` (zstd); empty + non-empty round-trip; corrupt + size mismatch; ZSTD EVENT chunk composition. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-PAYLOAD-LZ4-PROVISIONAL | Provisional chunk payload codec LZ4 (not wire freeze) | done | Schema [`docs/schemas/v6-payload-lz4-provisional-v0.md`](schemas/v6-payload-lz4-provisional-v0.md): codec LZ4 = 3; raw LZ4 block (size from header); fail-closed corrupt/size. **Not** LZ4 frame format ADR; **not** default parse inflate; **not** COL-007 C writer. |
| FMT-V6-PAYLOAD-LZ4-MVP | Shipped LZ4 compress/decompress + chunk composition tests | done | `compress_lz4`/`decompress_lz4`/`encode_chunk_frame_lz4` + `decode_chunk_payload` (lz4_flex); empty + non-empty round-trip; corrupt + size mismatch; LZ4 EVENT chunk composition. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-COMPRESSED-PROFILE-PROVISIONAL | Provisional compressed multi-codec mini-profile composition (not wire freeze) | done | Schema [`docs/schemas/v6-compressed-profile-provisional-v0.md`](schemas/v6-compressed-profile-provisional-v0.md): EVENT payload codecs NONE/ZLIB/ZSTD/LZ4 over event-body; explicit inflate on decode; fail-closed corrupt/size. **Not** default parse inflate; **not** COL-007 C writer. |
| FMT-V6-COMPRESSED-PROFILE-MVP | Shipped compressed mini-profile encode/decode + per-codec tests | done | `encode_compressed_mini_profile`/`decode_compressed_mini_profile`; round-trip NONE+ZLIB+ZSTD+LZ4 with non-empty event-body; corrupt ZLIB + size-mismatch ZSTD/LZ4 fail-closed. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-MULTI-CHUNK-COMPRESSED-PROVISIONAL | Provisional multi-chunk EVENT with compressed payloads (not wire freeze) | done | Schema [`docs/schemas/v6-multi-chunk-compressed-provisional-v0.md`](schemas/v6-multi-chunk-compressed-provisional-v0.md): `partition_event_records` + per-chunk NONE/ZLIB/ZSTD/LZ4; ordered reassembly; fail-closed corrupt/size. **Not** mid-record span; **not** default parse inflate; **not** COL-007 C writer. |
| FMT-V6-MULTI-CHUNK-COMPRESSED-MVP | Shipped multi-chunk compressed EVENT encode/decode + tests | done | `encode_multi_chunk_compressed_profile`/`decode_multi_chunk_compressed_profile`; ≥2 EVENT chunks under NONE+ZLIB+ZSTD+LZ4; ordered fields; corrupt second ZLIB + ZSTD size mismatch fail-closed. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-COMPRESSED-MIXED-PROVISIONAL | Provisional compressed multi-kind mixed profile (not wire freeze) | done | Schema [`docs/schemas/v6-compressed-mixed-provisional-v0.md`](schemas/v6-compressed-mixed-provisional-v0.md): EVENT/SOURCE/INDEX/SUMMARY under NONE/ZLIB/ZSTD/LZ4; FOOTER codec NONE last; fail-closed corrupt/size. **Not** default parse inflate; **not** COL-007 C writer. |
| FMT-V6-COMPRESSED-MIXED-MVP | Shipped compressed mixed encode/decode + per-codec tests | done | `encode_compressed_mixed_profile`/`decode_compressed_mixed_profile`; EVENT+SOURCE(+INDEX/SUMMARY)+FOOTER under NONE+ZLIB+ZSTD+LZ4; corrupt SOURCE ZLIB + EVENT ZSTD size mismatch fail-closed. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-PER-KIND-CODEC-PROVISIONAL | Provisional per-kind payload codecs on mixed profiles (not wire freeze) | done | Schema [`docs/schemas/v6-per-kind-codec-provisional-v0.md`](schemas/v6-per-kind-codec-provisional-v0.md): EVENT/SOURCE/INDEX/SUMMARY may each use NONE/ZLIB/ZSTD/LZ4 independently; FOOTER NONE last; fail-closed. **Not** default parse inflate; **not** COL-007 C writer. |
| FMT-V6-PER-KIND-CODEC-MVP | Shipped per-kind codec mixed encode/decode + tests | done | `KindCodecs` + `encode_compressed_mixed_profile_per_kind`; ZSTD EVENT + LZ4 SOURCE (and multi-kind) round-trip; corrupt LZ4 SOURCE + ZSTD size mismatch fail-closed; shared codec = uniform. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-MULTI-CHUNK-KIND-PROVISIONAL | Provisional multi-chunk EVENT under compressed mixed profiles (not wire freeze) | done | Schema [`docs/schemas/v6-multi-chunk-kind-mixed-provisional-v0.md`](schemas/v6-multi-chunk-kind-mixed-provisional-v0.md): ≥2 EVENT chunks via `partition_event_records` + co-present SOURCE (per-kind codecs); FOOTER NONE last; fail-closed. **Not** mid-record span; **not** default parse inflate; **not** COL-007 C writer. |
| FMT-V6-MULTI-CHUNK-KIND-MVP | Shipped multi-chunk EVENT + SOURCE mixed encode/decode + tests | done | `encode_multi_chunk_kind_mixed_profile` + `decode_compressed_mixed_profile`; ≥2 ZSTD EVENT + LZ4 SOURCE ordered reassembly; NONE/ZLIB/LZ4 EVENT paths; corrupt 2nd EVENT + size mismatch fail-closed. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-MULTI-CHUNK-SOURCE-PROVISIONAL | Provisional multi-chunk SOURCE under compressed mixed (not wire freeze) | done | Schema [`docs/schemas/v6-multi-chunk-source-provisional-v0.md`](schemas/v6-multi-chunk-source-provisional-v0.md): ≥2 SOURCE chunks via `partition_source_records` + co-present EVENT; per-kind codecs; FOOTER NONE last; fail-closed. **Not** mid-record span; **not** default parse inflate; **not** COL-007 C writer. |
| FMT-V6-MULTI-CHUNK-SOURCE-MVP | Shipped multi-chunk SOURCE encode/decode + tests | done | `partition_source_records` + `encode_multi_chunk_source_mixed_profile`; ≥2 LZ4 SOURCE + ZSTD EVENT ordered reassembly; NONE/ZLIB/ZSTD SOURCE paths; both EVENT+SOURCE split; corrupt 2nd SOURCE + size mismatch fail-closed. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-MULTI-CHUNK-INDEX-PROVISIONAL | Provisional multi-chunk INDEX under compressed mixed (not wire freeze) | done | Schema [`docs/schemas/v6-multi-chunk-index-provisional-v0.md`](schemas/v6-multi-chunk-index-provisional-v0.md): ≥2 INDEX chunks via `partition_index_records` + co-present EVENT; per-kind codecs; FOOTER NONE last; fail-closed. **Not** mid-record span; **not** default parse inflate; **not** COL-007 C writer. |
| FMT-V6-MULTI-CHUNK-INDEX-MVP | Shipped multi-chunk INDEX encode/decode + tests | done | `partition_index_records` + `encode_multi_chunk_index_mixed_profile`; ≥2 ZSTD INDEX + LZ4 EVENT ordered reassembly; NONE/ZLIB/LZ4 INDEX paths; EVENT+SOURCE+INDEX split; corrupt 2nd INDEX + size mismatch fail-closed. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-MULTI-CHUNK-SUMMARY-PROVISIONAL | Provisional multi-chunk SUMMARY under compressed mixed (not wire freeze) | done | Schema [`docs/schemas/v6-multi-chunk-summary-provisional-v0.md`](schemas/v6-multi-chunk-summary-provisional-v0.md): ≥2 SUMMARY chunks via `partition_summary_records` + co-present EVENT; per-kind codecs; FOOTER NONE last; fail-closed. **Not** mid-record span; **not** default parse inflate; **not** COL-007 C writer. |
| FMT-V6-MULTI-CHUNK-SUMMARY-MVP | Shipped multi-chunk SUMMARY encode/decode + tests | done | `partition_summary_records` + `encode_multi_chunk_summary_mixed_profile`; ≥2 ZSTD SUMMARY + LZ4 EVENT ordered reassembly; NONE/ZLIB/LZ4 SUMMARY paths; all kinds split; corrupt 2nd SUMMARY + size mismatch fail-closed. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-MID-RECORD-SPAN-PROVISIONAL | Provisional mid-record span across EVENT chunks (not wire freeze) | done | Schema [`docs/schemas/v6-mid-record-span-provisional-v0.md`](schemas/v6-mid-record-span-provisional-v0.md): event-body bytes split mid-record into ≥2 EVENT payloads; join+`decode_event_body` on decode; NONE/ZLIB/ZSTD/LZ4; fail-closed truncated/corrupt. **Not** default parse inflate; **not** COL-007 C writer. |
| FMT-V6-MID-RECORD-SPAN-MVP | Shipped mid-record-span EVENT encode/decode + tests | done | `encode_mid_record_span_event_profile`/`decode_mid_record_span_event_profile` + `split_event_body_bytes`; NONE+ZLIB+ZSTD+LZ4 round-trip with mid-record split; truncated join + corrupt continuation fail-closed. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-MID-RECORD-SOURCE-PROVISIONAL | Provisional mid-record span across SOURCE chunks (not wire freeze) | done | Schema [`docs/schemas/v6-mid-record-source-provisional-v0.md`](schemas/v6-mid-record-source-provisional-v0.md): source-body bytes split mid-record into ≥2 SOURCE payloads; join+`decode_source_body` on decode; NONE/ZLIB/ZSTD/LZ4; fail-closed truncated/corrupt. **Not** INDEX/SUMMARY mid-record; **not** default parse inflate; **not** COL-007 C writer. |
| FMT-V6-MID-RECORD-SOURCE-MVP | Shipped mid-record-span SOURCE encode/decode + tests | done | `encode_mid_record_span_source_profile`/`decode_mid_record_span_source_profile` + `split_source_body_bytes`; NONE+ZLIB+ZSTD+LZ4 round-trip with mid-record split; truncated join + corrupt continuation fail-closed. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-MID-RECORD-INDEX-PROVISIONAL | Provisional mid-record span across INDEX chunks (not wire freeze) | done | Schema [`docs/schemas/v6-mid-record-index-provisional-v0.md`](schemas/v6-mid-record-index-provisional-v0.md): index-body bytes split mid-record into ≥2 INDEX payloads; join+`decode_index_body` on decode; NONE/ZLIB/ZSTD/LZ4; fail-closed truncated/corrupt. **Not** SUMMARY mid-record; **not** default parse inflate; **not** COL-007 C writer. |
| FMT-V6-MID-RECORD-INDEX-MVP | Shipped mid-record-span INDEX encode/decode + tests | done | `encode_mid_record_span_index_profile`/`decode_mid_record_span_index_profile` + `split_index_body_bytes`; NONE+ZLIB+ZSTD+LZ4 round-trip with mid-record split; truncated join + corrupt continuation fail-closed. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-MID-RECORD-SUMMARY-PROVISIONAL | Provisional mid-record span across SUMMARY chunks (not wire freeze) | done | Schema [`docs/schemas/v6-mid-record-summary-provisional-v0.md`](schemas/v6-mid-record-summary-provisional-v0.md): summary-body bytes split mid-record into ≥2 SUMMARY payloads; join+`decode_summary_body` on decode; NONE/ZLIB/ZSTD/LZ4; fail-closed truncated/corrupt. **Not** default parse inflate; **not** COL-007 C writer. |
| FMT-V6-MID-RECORD-SUMMARY-MVP | Shipped mid-record-span SUMMARY encode/decode + tests | done | `encode_mid_record_span_summary_profile`/`decode_mid_record_span_summary_profile` + `split_summary_body_bytes`; NONE+ZLIB+ZSTD+LZ4 round-trip with mid-record split; truncated join + corrupt continuation fail-closed. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-DECODED-CHUNK-PROVISIONAL | Provisional decoded-chunk consumer path (always inflate + optional CRC; not wire freeze) | done | Schema [`docs/schemas/v6-decoded-chunk-provisional-v0.md`](schemas/v6-decoded-chunk-provisional-v0.md): `parse_chunk_frame` → optional CRC → `decode_chunk_payload`; NONE/ZLIB/ZSTD/LZ4 plain recovery; fail-closed corrupt/CRC. **Default `parse_chunk_frame` stays non-inflating**; **not** COL-007 C writer. |
| FMT-V6-DECODED-CHUNK-MVP | Shipped decoded-chunk encode/decode + fail-closed tests | done | `decode_chunk`/`decode_chunk_frame_plain`; NONE+ZLIB+ZSTD+LZ4 plain round-trip with optional CRC; corrupt zlib + CRC mismatch fail-closed; structural check that default parse stays non-inflating. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-DECODED-STREAM-PROVISIONAL | Provisional decoded prefix+chunk stream (always inflate + optional CRC; not wire freeze) | done | Schema [`docs/schemas/v6-decoded-stream-provisional-v0.md`](schemas/v6-decoded-stream-provisional-v0.md): prefix+≥2 chunks → per-chunk optional CRC + always inflate via `decode_prefix_chunk_stream_plain`; NONE/ZLIB/ZSTD/LZ4 ordered plains; fail-closed truncated/corrupt/CRC. **Default parse stays non-inflating**; **not** COL-007 C writer. |
| FMT-V6-DECODED-STREAM-MVP | Shipped decoded-stream encode/decode + fail-closed tests | done | `encode_prefix_sealed_chunks`/`decode_prefix_chunk_stream_plain`; ≥2 chunks NONE+compressed ordered plain recovery; truncated mid-stream + corrupt zlib + CRC mismatch fail-closed. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-DECODED-EVENT-PROVISIONAL | Provisional decoded EVENT profile (stream→inflate→event-body; not wire freeze) | done | Schema [`docs/schemas/v6-decoded-event-provisional-v0.md`](schemas/v6-decoded-event-provisional-v0.md): prefix+EVENT (NONE/ZLIB/ZSTD/LZ4) via `decode_prefix_chunk_stream_plain` + join plains + `decode_event_body`; optional CRC; multi-chunk record-aligned; fail-closed truncated/corrupt/CRC. **Default parse non-inflating**; **not** full opcode freeze; **not** COL-007 C writer. |
| FMT-V6-DECODED-EVENT-MVP | Shipped decoded-EVENT encode/decode + fail-closed tests | done | `encode_decoded_event_profile`/`decode_decoded_event_profile`; NONE+ZLIB+ZSTD+LZ4 logical event round-trip; multi-chunk ZLIB; corrupt zlib + CRC mismatch + truncated fail-closed. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-DECODED-SOURCE-PROVISIONAL | Provisional decoded SOURCE profile (stream→inflate→source-body; not wire freeze) | done | Schema [`docs/schemas/v6-decoded-source-provisional-v0.md`](schemas/v6-decoded-source-provisional-v0.md): prefix+SOURCE (NONE/ZLIB/ZSTD/LZ4) via `decode_prefix_chunk_stream_plain` + join plains + `decode_source_body`; optional CRC; multi-chunk record-aligned; fail-closed truncated/corrupt/CRC. **Default parse non-inflating**; **not** full SRC_LINE freeze; **not** COL-007 C writer. |
| FMT-V6-DECODED-SOURCE-MVP | Shipped decoded-SOURCE encode/decode + fail-closed tests | done | `encode_decoded_source_profile`/`decode_decoded_source_profile`; NONE+ZLIB+ZSTD+LZ4 logical source round-trip; multi-chunk ZLIB; corrupt zlib + CRC mismatch + truncated fail-closed. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-DECODED-INDEX-PROVISIONAL | Provisional decoded INDEX profile (stream→inflate→index-body; not wire freeze) | done | Schema [`docs/schemas/v6-decoded-index-provisional-v0.md`](schemas/v6-decoded-index-provisional-v0.md): prefix+INDEX (NONE/ZLIB/ZSTD/LZ4) via `decode_prefix_chunk_stream_plain` + join plains + `decode_index_body`; optional CRC; multi-chunk record-aligned; fail-closed truncated/corrupt/CRC. **Default parse non-inflating**; **not** SUMMARY decoded; **not** COL-007 C writer. |
| FMT-V6-DECODED-INDEX-MVP | Shipped decoded-INDEX encode/decode + fail-closed tests | done | `encode_decoded_index_profile`/`decode_decoded_index_profile`; NONE+ZLIB+ZSTD+LZ4 logical index round-trip; multi-chunk ZLIB; corrupt zlib + CRC mismatch + truncated fail-closed. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-DECODED-SUMMARY-PROVISIONAL | Provisional decoded SUMMARY profile (stream→inflate→summary-body; not wire freeze) | done | Schema [`docs/schemas/v6-decoded-summary-provisional-v0.md`](schemas/v6-decoded-summary-provisional-v0.md): prefix+SUMMARY (NONE/ZLIB/ZSTD/LZ4) via `decode_prefix_chunk_stream_plain` + join plains + `decode_summary_body`; optional CRC; multi-chunk record-aligned; fail-closed truncated/corrupt/CRC. **Default parse non-inflating**; **not** COL-007 C writer. |
| FMT-V6-DECODED-SUMMARY-MVP | Shipped decoded-SUMMARY encode/decode + fail-closed tests | done | `encode_decoded_summary_profile`/`decode_decoded_summary_profile`; NONE+ZLIB+ZSTD+LZ4 logical summary round-trip; multi-chunk ZLIB; corrupt zlib + CRC mismatch + truncated fail-closed. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-DECODED-MIXED-PROVISIONAL | Provisional decoded multi-kind mixed profile (always-inflate + optional CRC; not wire freeze) | done | Schema [`docs/schemas/v6-decoded-mixed-provisional-v0.md`](schemas/v6-decoded-mixed-provisional-v0.md): prefix+EVENT/SOURCE/INDEX/SUMMARY via `decode_prefix_chunk_stream_plain` + join per kind + body decode; optional CRC; per-kind codecs; fail-closed truncated/corrupt/CRC. **Default parse non-inflating**; **not** COL-007 C writer. |
| FMT-V6-DECODED-MIXED-MVP | Shipped decoded-mixed encode/decode + fail-closed tests | done | `encode_decoded_mixed_profile`/`decode_decoded_mixed_profile`; ≥2 kinds NONE+compressed logical recovery; all four kinds per-kind codecs; corrupt zlib + CRC mismatch + truncated fail-closed. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-DECODED-MIXED-MULTI-CHUNK-PROVISIONAL | Provisional multi-chunk record-aligned recovery on always-inflate multi-kind mixed (not wire freeze) | done | Schema [`docs/schemas/v6-decoded-mixed-multi-chunk-provisional-v0.md`](schemas/v6-decoded-mixed-multi-chunk-provisional-v0.md): ≥2 chunks of one kind via `partition_*` + co-present kind; `encode_decoded_mixed_multi_chunk_profile` + `decode_decoded_mixed_profile` join plains; NONE/ZLIB/ZSTD/LZ4; optional CRC; fail-closed. **Default parse non-inflating**; **not** mid-record as primary claim; **not** COL-007 C writer. |
| FMT-V6-DECODED-MIXED-MULTI-CHUNK-MVP | Shipped multi-chunk decoded-mixed encode/decode + fail-closed tests | done | `encode_decoded_mixed_multi_chunk_profile` + `decode_decoded_mixed_profile`; ≥2 ZLIB EVENT + NONE SOURCE; ≥2 LZ4 SOURCE + ZSTD EVENT; partition count honesty; corrupt 2nd EVENT + truncated fail-closed. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-DECODED-MIXED-MID-RECORD-PROVISIONAL | Provisional mid-record span recovery on always-inflate multi-kind mixed (not wire freeze) | done | Schema [`docs/schemas/v6-decoded-mixed-mid-record-provisional-v0.md`](schemas/v6-decoded-mixed-mid-record-provisional-v0.md): EVENT body mid-record split across ≥2 chunks + co-present kind; `encode_decoded_mixed_mid_record_event_profile` + `decode_decoded_mixed_profile`; NONE/ZLIB/ZSTD/LZ4; optional CRC; fail-closed truncated/corrupt. **Default parse non-inflating**; **not** COL-007 C writer. |
| FMT-V6-DECODED-MIXED-MID-RECORD-MVP | Shipped mid-record-on-mixed encode/decode + fail-closed tests | done | `encode_decoded_mixed_mid_record_event_profile` + `decode_decoded_mixed_profile`; ZLIB EVENT mid-span + NONE SOURCE; NONE/ZSTD/LZ4 EVENT + ZLIB SOURCE; invalid split + corrupt continuation + truncated join fail-closed. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-DECODED-MIXED-MID-RECORD-SOURCE-PROVISIONAL | Provisional SOURCE mid-record span on always-inflate multi-kind mixed (not wire freeze) | done | Schema [`docs/schemas/v6-decoded-mixed-mid-record-source-provisional-v0.md`](schemas/v6-decoded-mixed-mid-record-source-provisional-v0.md): SOURCE body mid-record split across ≥2 chunks + co-present EVENT; `encode_decoded_mixed_mid_record_source_profile` + `decode_decoded_mixed_profile`; NONE/ZLIB/ZSTD/LZ4; optional CRC; fail-closed. Sibling INDEX/SUMMARY mid-on-mixed; **Default parse non-inflating**; **not** COL-007 C writer. |
| FMT-V6-DECODED-MIXED-MID-RECORD-SOURCE-MVP | Shipped SOURCE mid-record-on-mixed encode/decode + fail-closed tests | done | `encode_decoded_mixed_mid_record_source_profile` + `decode_decoded_mixed_profile`; ZLIB SOURCE mid-span + NONE EVENT; NONE/ZSTD/LZ4 SOURCE + ZLIB EVENT; invalid split + corrupt continuation + truncated join fail-closed. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-DECODED-MIXED-MID-RECORD-INDEX-PROVISIONAL | Provisional INDEX mid-record span on always-inflate multi-kind mixed (not wire freeze) | done | Schema [`docs/schemas/v6-decoded-mixed-mid-record-index-provisional-v0.md`](schemas/v6-decoded-mixed-mid-record-index-provisional-v0.md): INDEX body mid-record split across ≥2 chunks + co-present EVENT; `encode_decoded_mixed_mid_record_index_profile` + `decode_decoded_mixed_profile`; NONE/ZLIB/ZSTD/LZ4; optional CRC; fail-closed. **Default parse non-inflating**; **not** COL-007 C writer. |
| FMT-V6-DECODED-MIXED-MID-RECORD-INDEX-MVP | Shipped INDEX mid-record-on-mixed encode/decode + fail-closed tests | done | `encode_decoded_mixed_mid_record_index_profile` + `decode_decoded_mixed_profile`; ZLIB INDEX mid-span + NONE EVENT; NONE/ZSTD/LZ4 INDEX + ZLIB EVENT; invalid split + corrupt continuation + truncated join fail-closed. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-DECODED-MIXED-MID-RECORD-SUMMARY-PROVISIONAL | Provisional SUMMARY mid-record span on always-inflate multi-kind mixed (not wire freeze) | done | Schema [`docs/schemas/v6-decoded-mixed-mid-record-summary-provisional-v0.md`](schemas/v6-decoded-mixed-mid-record-summary-provisional-v0.md): SUMMARY body mid-record split across ≥2 chunks + co-present EVENT; `encode_decoded_mixed_mid_record_summary_profile` + `decode_decoded_mixed_profile`; NONE/ZLIB/ZSTD/LZ4; optional CRC; fail-closed. **Default parse non-inflating**; **not** COL-007 C writer. |
| FMT-V6-DECODED-MIXED-MID-RECORD-SUMMARY-MVP | Shipped SUMMARY mid-record-on-mixed encode/decode + fail-closed tests | done | `encode_decoded_mixed_mid_record_summary_profile` + `decode_decoded_mixed_profile`; ZLIB SUMMARY mid-span + NONE EVENT; NONE/ZSTD/LZ4 SUMMARY + ZLIB EVENT; invalid split + corrupt continuation + truncated join fail-closed. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-DECODED-MIXED-MID-RECORD-CONCURRENT-PROVISIONAL | Provisional concurrent multi-kind mid-record spans on always-inflate mixed (not wire freeze) | done | Schema [`docs/schemas/v6-decoded-mixed-mid-record-concurrent-provisional-v0.md`](schemas/v6-decoded-mixed-mid-record-concurrent-provisional-v0.md): ≥2 kinds mid-record-split in one profile; `encode_decoded_mixed_mid_record_concurrent_profile` + `decode_decoded_mixed_profile`; NONE/ZLIB/ZSTD/LZ4; optional CRC; fail-closed. **Default parse non-inflating**; **not** COL-007 C writer. |
| FMT-V6-DECODED-MIXED-MID-RECORD-CONCURRENT-MVP | Shipped concurrent multi-kind mid-record-on-mixed encode/decode + fail-closed tests | done | `encode_decoded_mixed_mid_record_concurrent_profile` + `MidRecordKindSplits` + `decode_decoded_mixed_profile`; EVENT+SOURCE ZLIB; INDEX+SUMMARY NONE/ZSTD/LZ4; all four kinds mixed codecs; invalid split + corrupt + truncated fail-closed. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-EVENT-BODY-TIME-BLOCK-SUB-ENTRY-PROVISIONAL | Provisional TIME_BLOCK + SUB_ENTRY event-body opcodes (not wire freeze) | done | Schema [`docs/schemas/v6-event-body-time-block-sub-entry-provisional-v0.md`](schemas/v6-event-body-time-block-sub-entry-provisional-v0.md) + update [`v6-event-body-provisional-v0.md`](schemas/v6-event-body-provisional-v0.md): opcodes 3/4; TIME_BLOCK fid/line/block_line/ticks; SUB_ENTRY caller_fid/caller_line; fail-closed truncated/unknown. **Not** full opcode catalog freeze; **Default parse non-inflating**; **not** COL-007 C writer. |
| FMT-V6-EVENT-BODY-TIME-BLOCK-SUB-ENTRY-MVP | Shipped TIME_BLOCK + SUB_ENTRY body + always-inflate EVENT/mixed tests | done | `EventRecordSpec::{TimeBlock,SubEntry}` encode/decode; `OwnedEventRecord` variants; decoded-EVENT NONE/ZLIB/ZSTD/LZ4 + mixed EVENT+SOURCE recovery; truncated mid-record fail-closed. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-EVENT-BODY-SUB-RETURN-SUB-INFO-PROVISIONAL | Provisional SUB_RETURN + SUB_INFO event-body opcodes (not wire freeze) | done | Schema [`docs/schemas/v6-event-body-sub-return-sub-info-provisional-v0.md`](schemas/v6-event-body-sub-return-sub-info-provisional-v0.md) + update [`v6-event-body-provisional-v0.md`](schemas/v6-event-body-provisional-v0.md): opcodes 5/6; SUB_RETURN depth/incl/excl/subname; SUB_INFO fid/first_line/last_line/name; integer ULEB ticks; fail-closed truncated/unknown. **Not** full opcode catalog freeze; **Default parse non-inflating**; **not** COL-007 C writer. |
| FMT-V6-EVENT-BODY-SUB-RETURN-SUB-INFO-MVP | Shipped SUB_RETURN + SUB_INFO body + always-inflate EVENT/mixed tests | done | `EventRecordSpec::{SubReturn,SubInfo}` encode/decode; `OwnedEventRecord` variants; decoded-EVENT NONE/ZLIB/ZSTD/LZ4 + mixed EVENT+SOURCE recovery; truncated mid-record fail-closed. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-EVENT-BODY-SRC-LINE-NEW-FID-PROVISIONAL | Provisional SRC_LINE + NEW_FID event-body opcodes (not wire freeze) | done | Schema [`docs/schemas/v6-event-body-src-line-new-fid-provisional-v0.md`](schemas/v6-event-body-src-line-new-fid-provisional-v0.md) + update [`v6-event-body-provisional-v0.md`](schemas/v6-event-body-provisional-v0.md): opcodes 7/8; SRC_LINE fid/line/text; NEW_FID fid/filename; fail-closed truncated/unknown. **Not** full opcode catalog freeze; **Default parse non-inflating**; **not** COL-007 C writer. |
| FMT-V6-EVENT-BODY-SRC-LINE-NEW-FID-MVP | Shipped SRC_LINE + NEW_FID body + always-inflate EVENT/mixed tests | done | `EventRecordSpec::{SrcLine,NewFid}` encode/decode; `OwnedEventRecord` variants; decoded-EVENT NONE/ZLIB/ZSTD/LZ4 + mixed EVENT+SOURCE recovery; truncated mid-record fail-closed. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-EVENT-BODY-PID-START-END-PROVISIONAL | Provisional PID_START + PID_END event-body opcodes (not wire freeze) | done | Schema [`docs/schemas/v6-event-body-pid-start-end-provisional-v0.md`](schemas/v6-event-body-pid-start-end-provisional-v0.md) + update [`v6-event-body-provisional-v0.md`](schemas/v6-event-body-provisional-v0.md): opcodes 9/10; PID_START pid/ppid/start_time; PID_END pid/end_time; integer ULEB times; fail-closed truncated/unknown. **Not** full opcode catalog freeze; **not** COL-015 fork re-init; **Default parse non-inflating**; **not** COL-007 C writer. |
| SEC-FUZZ-HARDENING-MVP | Security/fuzz hardening package (offline) | **done** | PR-C03: contract [`docs/contracts/SECURITY_FUZZ_HARDENING_PACKAGE_v0.md`](contracts/SECURITY_FUZZ_HARDENING_PACKAGE_v0.md) + schema [`docs/schemas/security-fuzz-hardening-mvp-v0.md`](schemas/security-fuzz-hardening-mvp-v0.md). **No open critical/high** on covered surfaces. v5 DECODE-FUZZ-MVP kept; **v6** deterministic mutation battery on C fixtures (`crates/nytprof-format-v6/tests/decode_fuzz.rs` via `e3_decode_writer_bytes`); smoke `./tools/oracle/selftest_security_fuzz.sh` (cargo-required). P02 wrapper `sec002_continuous_fuzz_mvp.sh` honest `SKIP:` without cargo. **Not** an offline_gate step. Batching (COL-005 SV lifetime) + fork state MVP (COL-002) threats catalogued with unit evidence. **Residuals:** full SEC-002 cargo-fuzz/AFL/deep corpus; COL-015 full fork/PID suite; independent SEC-012 sign-off; E3-mixed fuzz corpus. |
| SEC-012-CHECKLIST-MVP | Release security review checklist | **done (MVP / checklist)** | P02: [`SEC_012_RELEASE_REVIEW_CHECKLIST_v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/contracts/SEC_012_RELEASE_REVIEW_CHECKLIST_v0.md). Covered surfaces + residual threats + explicit non-claims. **Not** independent sign-off / not GA marketing. |
| SEC-002-CONTINUOUS-FUZZ-MVP | Continuous-fuzz job wrapping existing batteries | **done (MVP / job)** | P02: [`sec002_continuous_fuzz_mvp.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/ci/sec002_continuous_fuzz_mvp.sh) + [`.github/workflows/sec002-fuzz-mvp.yml`](https://github.com/hilather/nytprof-modernization/blob/main/.github/workflows/sec002-fuzz-mvp.yml) invoke shipped `selftest_security_fuzz.sh` / `decode_fuzz`. Honest `SKIP:` without cargo. **Not** cargo-fuzz / AFL / sanitizer matrix / deep corpus. |
| FMT-V6-EVENT-BODY-PID-START-END-MVP | Shipped PID_START + PID_END body + always-inflate EVENT/mixed tests | done | `EventRecordSpec::{PidStart,PidEnd}` encode/decode; `OwnedEventRecord` variants; decoded-EVENT NONE/ZLIB/ZSTD/LZ4 + mixed EVENT+SOURCE recovery; truncated mid-record fail-closed. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-EVENT-BODY-SUB-CALLERS-DISCOUNT-PROVISIONAL | Provisional SUB_CALLERS + DISCOUNT event-body opcodes (not wire freeze) | done | Schema [`docs/schemas/v6-event-body-sub-callers-discount-provisional-v0.md`](schemas/v6-event-body-sub-callers-discount-provisional-v0.md) + update [`v6-event-body-provisional-v0.md`](schemas/v6-event-body-provisional-v0.md): opcodes 11/12; SUB_CALLERS fid/line/count/incl/excl/reci/rec_depth + called/caller blobs; DISCOUNT empty body; integer ULEB times; fail-closed truncated/unknown. **Not** full opcode catalog freeze; **not** BASE-003 DISCOUNT accounting freeze; **Default parse non-inflating**; **not** COL-007 C writer. |
| FMT-V6-EVENT-BODY-SUB-CALLERS-DISCOUNT-MVP | Shipped SUB_CALLERS + DISCOUNT body + always-inflate EVENT/mixed tests | done | `EventRecordSpec::{SubCallers,Discount}` encode/decode; `OwnedEventRecord` variants; decoded-EVENT NONE/ZLIB/ZSTD/LZ4 + mixed EVENT+SOURCE recovery; truncated mid-record fail-closed. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-EVENT-BODY-ATTRIBUTE-OPTION-PROVISIONAL | Provisional ATTRIBUTE + OPTION event-body opcodes (not wire freeze) | done | Schema [`docs/schemas/v6-event-body-attribute-option-provisional-v0.md`](schemas/v6-event-body-attribute-option-provisional-v0.md) + update [`v6-event-body-provisional-v0.md`](schemas/v6-event-body-provisional-v0.md): opcodes 13/14; ATTRIBUTE/OPTION key+value string-blobs; fail-closed truncated/unknown. **Not** full opcode catalog freeze; **not** complete key vocabulary freeze (OI-002-03/04); **Default parse non-inflating**; **not** COL-007 C writer. |
| FMT-V6-EVENT-BODY-ATTRIBUTE-OPTION-MVP | Shipped ATTRIBUTE + OPTION body + always-inflate EVENT/mixed tests | done | `EventRecordSpec::{Attribute,Option}` encode/decode; `OwnedEventRecord` variants; decoded-EVENT NONE/ZLIB/ZSTD/LZ4 + mixed EVENT+SOURCE recovery; truncated mid-record fail-closed. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-EVENT-BODY-COMMENT-PROVISIONAL | Provisional COMMENT event-body opcode (not wire freeze) | done | Schema [`docs/schemas/v6-event-body-comment-provisional-v0.md`](schemas/v6-event-body-comment-provisional-v0.md) + update [`v6-event-body-provisional-v0.md`](schemas/v6-event-body-provisional-v0.md): opcode 15; COMMENT text string-blob; fail-closed truncated/unknown. **Not** START_DEFLATE-as-event freeze; **not** COMPAT-002 comment normalize freeze; **Default parse non-inflating**; **not** COL-007 C writer. |
| FMT-V6-EVENT-BODY-COMMENT-MVP | Shipped COMMENT body + always-inflate EVENT/mixed tests | done | `EventRecordSpec::Comment` encode/decode; `OwnedEventRecord::Comment`; decoded-EVENT NONE/ZLIB/ZSTD/LZ4 + mixed EVENT+SOURCE recovery; truncated mid-record fail-closed. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-EVENT-BODY-START-DEFLATE-PROVISIONAL | Provisional START_DEFLATE event-body opcode (not wire freeze) | done | Schema [`docs/schemas/v6-event-body-start-deflate-provisional-v0.md`](schemas/v6-event-body-start-deflate-provisional-v0.md) + update [`v6-event-body-provisional-v0.md`](schemas/v6-event-body-provisional-v0.md): opcode 16; empty typed body (marker only); fail-closed missing flags/unknown. **Not** mid-stream codec switch; **not** VERSION prelude freeze; **Default parse non-inflating**; **not** COL-007 C writer. |
| FMT-V6-EVENT-BODY-START-DEFLATE-MVP | Shipped START_DEFLATE body + always-inflate EVENT/mixed tests | done | `EventRecordSpec::StartDeflate` encode/decode; `OwnedEventRecord::StartDeflate`; decoded-EVENT NONE/ZLIB/ZSTD/LZ4 + mixed EVENT+SOURCE recovery; missing-flags fail-closed. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-EVENT-BODY-VERSION-PROVISIONAL | Provisional VERSION event-body opcode (not wire freeze) | done | Schema [`docs/schemas/v6-event-body-version-provisional-v0.md`](schemas/v6-event-body-version-provisional-v0.md) + update [`v6-event-body-provisional-v0.md`](schemas/v6-event-body-provisional-v0.md): opcode 17; VERSION major/minor ULEB; fail-closed truncated/unknown. **Not** OI-001-03 dual-output freeze; **not** auto-emit from fixed-header; **Default parse non-inflating**; **not** COL-007 C writer. |
| FMT-V6-EVENT-BODY-VERSION-MVP | Shipped VERSION body + always-inflate EVENT/mixed tests | done | `EventRecordSpec::Version` encode/decode; `OwnedEventRecord::Version`; decoded-EVENT NONE/ZLIB/ZSTD/LZ4 + mixed EVENT+SOURCE recovery; truncated mid-record fail-closed. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-EVENT-BODY-DUAL-OUTPUT-SEQUENCE-PROVISIONAL | Provisional dual-output multi-record EVENT order preflight (not OI-001-03 freeze) | done | Schema [`docs/schemas/v6-event-body-dual-output-sequence-provisional-v0.md`](schemas/v6-event-body-dual-output-sequence-provisional-v0.md): dump-aligned VERSION → meta (COMMENT/ATTRIBUTE/OPTION) → optional START_DEFLATE → PID_START … interior … PID_END order recovery. **Not** OI-001-03 sequence-number freeze; **not** mid-stream codec switch; **not** auto-emit VERSION from header; **Default parse non-inflating**; **not** COL-007 C writer. |
| FMT-V6-EVENT-BODY-DUAL-OUTPUT-SEQUENCE-MVP | Shipped dual-output sequence body + always-inflate EVENT/mixed tests | done | Multi-record `encode_event_body`/`decode_event_body` order+field round-trip; `decode_decoded_event_profile` NONE/ZLIB/ZSTD/LZ4 + mixed EVENT+SOURCE co-kind; truncated/unknown fail-closed; default stream parse non-inflating. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-START-DEFLATE-MID-STREAM-CODEC-SWITCH-PROVISIONAL | Provisional START_DEFLATE mid-stream chunk-codec switch (not wire freeze) | done | Schema [`docs/schemas/v6-event-body-start-deflate-mid-stream-codec-switch-provisional-v0.md`](schemas/v6-event-body-start-deflate-mid-stream-codec-switch-provisional-v0.md): pre EVENT (typically NONE) with START_DEFLATE marker then post EVENT under different payload codec (ZLIB/ZSTD/LZ4). Chunk-framed preflight — **not** v5 mid-payload stream deflate; **not** OI-001-03 seq-number freeze; **Default parse non-inflating**; **not** COL-007 C writer. |
| FMT-V6-START-DEFLATE-MID-STREAM-CODEC-SWITCH-MVP | Shipped mid-stream codec-switch encode/decode + always-inflate EVENT/mixed tests | done | `encode_decoded_event_mid_stream_codec_switch_profile` + mixed sibling; always-inflate join recovers order+fields for NONE→ZLIB/ZSTD/LZ4; corrupt post-switch fail-closed; default stream non-inflating. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-AUTO-EMIT-VERSION-PROVISIONAL | Provisional auto-emit VERSION from fixed-header (not wire freeze) | done | Schema [`docs/schemas/v6-auto-emit-version-provisional-v0.md`](schemas/v6-auto-emit-version-provisional-v0.md): header major/minor → dump-aligned VERSION; body VERSION must match or fail closed; explicit `*_auto_version` encode/decode paths. **Not** OI-001-03 seq-number freeze; **not** key-vocab freeze; **Default parse non-inflating**; **not** COL-007 C writer. |
| FMT-V6-AUTO-EMIT-VERSION-MVP | Shipped auto-emit VERSION helpers + always-inflate EVENT/mixed tests | done | `version_record_from_header` / `align_event_records_version_with_header`; `encode/decode_decoded_event_profile_auto_version` + mixed siblings; NONE/ZLIB/ZSTD/LZ4; inject when body omits VERSION; mismatch fail-closed; default stream non-inflating. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-ATTR-OPTION-KNOWN-KEY-PROVISIONAL | Provisional ATTRIBUTE/OPTION known-key vocabulary (not full OI-002 freeze) | done | Schema [`docs/schemas/v6-attr-option-known-key-provisional-v0.md`](schemas/v6-attr-option-known-key-provisional-v0.md): known ATTRIBUTE keys basetime/ticks_per_sec/application/xs_version; OPTION calls/blocks/stmts/compress. **Not** complete OI-002-03/04 inventory; **Default parse non-inflating**; **not** COL-007 C writer. |
| FMT-V6-ATTR-OPTION-KNOWN-KEY-MVP | Shipped known-key table + always-inflate EVENT/mixed tests | done | `known_key::*` + `attribute_kv`/`option_kv`/`known_key_attr_option_sample_specs`; body round-trip key+value asserts; decoded-EVENT NONE/ZLIB/ZSTD/LZ4 + mixed EVENT+SOURCE; truncated fail-closed. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| FMT-V6-EVENT-BODY-UNKNOWN-OPTIONAL-SKIP-PROVISIONAL | Provisional unknown optional length-framed skip (not flag freeze) | done | Schema [`docs/schemas/v6-event-body-unknown-optional-skip-provisional-v0.md`](schemas/v6-event-body-unknown-optional-skip-provisional-v0.md): `FLAG_BODY_LENGTH`; skip opaque body; required/unframed optional still fail-closed. **Not** permanent flag-bit freeze; **Default parse non-inflating**; **not** COL-007 C writer. |
| FMT-V6-EVENT-BODY-UNKNOWN-OPTIONAL-SKIP-MVP | Shipped unknown-optional skip + always-inflate EVENT/mixed tests | done | `encode_unknown_optional_skip_record` + `decode_event_body` skip; known…skip…known order+fields; NONE/ZLIB/ZSTD/LZ4 EVENT + mixed SOURCE; truncated/required/unframed fail-closed. Evidence `cargo test -p nytprof-format-v6`. COL-007 still deferred. |
| COL-001-SINK-MVP | Semantic sink interface scaffold (v5 path only; ADR-0004 overlay) | done | B0-A `collector/` tree: `include/nytp_sink.h` + emit wrappers, counting sink, v5 path adapter, `make -C collector test`, schema [`docs/schemas/collector-sink-api-mvp-v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/schemas/collector-sink-api-mvp-v0.md), smoke `./scripts/packaging/collector_sink_smoke.sh` (isolation + honest CC skip) wired offline_gate step 10. Real wire is **COL-006-V5-WIRE-MVP**. **Not** COL-007, **not** live XS hooks. |
| COL-002-LIFECYCLE-MVP | Sink lifecycle state machine + emit gates | done | Explicit `OPEN/ACTIVE/STOPPED/FINALIZING/CLOSED/FAILED/FORK_SPLIT`; `activate/stop/begin_finalize/begin_fork/end_fork_*/mark_failed`; emit gates; tests `test_lifecycle_seq`. Schema [`docs/schemas/collector-lifecycle-seq-fake-clock-mvp-v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/schemas/collector-lifecycle-seq-fake-clock-mvp-v0.md). **Not** COL-015 full fork/signal/**file-switch**/enable-disable (OI-003-05). |
| COL-003-SEQ-MVP | Monotonic logical event sequence numbers | done | Gapless `nytp_seq` on successful logical emits (not `START_DEFLATE`); peek/last/count + `nytp_seq_check_gapless`; counting seq/kind rings via post-commit `on_logical_committed`; default v5 does **not** write seq on wire. **Not** full OI-001-03 dump dual-index freeze. Evidence `test_lifecycle_seq`. |
| COL-004-FAST-PATH-MVP | No-allocation statement-event fast path | done | POD `TIME_LINE`/`TIME_BLOCK` append into fixed batch; `nytp_fast_emit_*`; `heap_allocs` stable after create; `arena_bytes_copied==0` on stmt path; light microbench engineering counters. Schema [`docs/schemas/collector-batch-fast-mvp-v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/schemas/collector-batch-fast-mvp-v0.md). Evidence `test_batch_fast`. **Not** BENCH-003 certification; **not** live XS hooks. |
| COL-005-BATCH-MVP | Bounded event batching | done | Fixed event headers + side arena; high-water/full flush; emergency oversized path; exact order under capacities 1..64 vs direct counting; SV lifetime (clobber after append); fail-flush retains pending. Evidence `test_batch_fast` + smoke. Flush-discount timing residual. |
| COL-006-V5-WIRE-MVP | Legacy v5 writer on sink (real wire) | done | Real FileHandle.xs protocol in `nytp_sink_v5` (header, packed ints, strings, NV, all mapped tags, zlib after `START_DEFLATE`); `nytp_v5_sink_wire` / path write; I32 tick fail-closed; tests `test_v5_wire` + M4 mini; Rust `nytprof-dump verify` accepts mini artifact. Schema [`docs/schemas/collector-v5-wire-mvp-v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/schemas/collector-v5-wire-mvp-v0.md). **Residual:** full `fixtures/v5/*` oracle corpus equality (complete TEST-003); not live XS hooks; not COL-007; not BENCH-004. |
| V5-DURABLE-SEAL-MVP | Sealed complete-record publish (`durable=1`) | **done (MVP)** | Live RAM uncompressed; `v5_flush`/`v5_close` = idempotent `nytp_v5_seal_publish` (`tmp`+`fsync`+`rename`; `z`+`Z_FINISH` copy). Product: omitted `compress` ⇒ zlib 6; `durable` default **0**. Force-seal + `kill -9`: [`di_durable_kill_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/di_durable_kill_smoke.sh). Design: [`OPERATOR_PROFILE_SIZE_AND_DURABILITY_v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/OPERATOR_PROFILE_SIZE_AND_DURABILITY_v0.md). **Not** default-on durable; **not** `aggregate=1` (ADR-0013 proposed). |
| TEST-003-FAKE-CLOCK-MVP | Deterministic fake-clock + M4 mini sample (scaffold) | done | `nytp_fake_clock` + BASE-003 `nytp_stmt_driver` + `nytp_m4_mini_sample_run` under counting/**v5 wire**; evidence `test_fake_clock` + `test_v5_wire`. **Residual:** full M4 oracle corpus v5-via-sink equality needs complete TEST-003 — not claimed. |
| ADR-0001-V6-PACKING-ACCEPTED | Permanent packing intent ADR accepted (OQ-1 as-is) | done | [`docs/adrs/0001-v6-event-body-packing-candidate.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/adrs/0001-v6-event-body-packing-candidate.md): site-delta, FLAG_HAS_SEQ, TIME_*_RUN, multi-chunk/mid-stream continuity intent; status **accepted** (not superseded). **Not** wire freeze; **not** COL-007 C writer. **Before full COL-007.** |
| ADR-0002-V6-STRING-POOL-ACCEPTED | Permanent FOOTER string-pool ADR accepted (OQ-1 as-is) | done | [`docs/adrs/0002-v6-string-pool-candidate.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/adrs/0002-v6-string-pool-candidate.md): FOOTER-local dictionary intent; status **accepted**. **Not** global pool freeze; **not** COL-007. **Before full COL-007.** |
| FMT-V6-PROVISIONAL-ID-LOCKFILE | ID lockfile (Rust + C mirror; runway for COL-007) | done | [`docs/contracts/V6_PROVISIONAL_ID_LOCKFILE_v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/contracts/V6_PROVISIONAL_ID_LOCKFILE_v0.md) + [`collector/include/nytprof_v6_ids.h`](https://github.com/hilather/nytprof-modernization/blob/main/collector/include/nytprof_v6_ids.h): MAGIC/kind/codec/opcode/flag IDs. Plan **FMT-002..010 deviation** closed for freeze class by **FMT-V6-WIRE-FREEZE** (ADR-0006). |
| FMT-V6-WIRE-FREEZE | Wire freeze ADR + frozen ID catalog (after E3+E4-v0) | done | [ADR-0006](https://github.com/hilather/nytprof-modernization/blob/main/docs/adrs/0006-v6-wire-freeze.md) + [`docs/schemas/v6-wire-ids-frozen-v1.md`](schemas/v6-wire-ids-frozen-v1.md): major=6 numeric IDs frozen; OQ-5 seq policy; OQ-6 global pool deferred. **Residuals:** E3-mixed; CLI v6 default; COL-008; full oracle E4 dual (TEST-008). |
| FMT-V6-GOLDEN-VECTORS | Immutable golden vectors (FMT-012 class) | done | [`fixtures/v6/vectors/`](../fixtures/v6/vectors/) + SHA256SUMS + `cargo test -p nytprof-format-v6 golden_vector_` / `wire_freeze_`. Regenerator: `cargo run -p nytprof-format-v6 --example gen_wire_vectors`. Product C streams remain `fixtures/v6/from-c/**`. |
| DUAL-EQUALITY-READINESS-MVP | Dual-equality readiness contract (E1–E5 checklist) | done | [`docs/contracts/DUAL_EQUALITY_READINESS_v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/contracts/DUAL_EQUALITY_READINESS_v0.md): readiness structure; ADR-0001/0002 accepted; lockfile; **E3-EVENT** + **E4-v0 model** + **E4 product CLI** + **E5 CLI opt-in** ready; **R2-preview packaging honesty (PR-B13)**; COL-009/ADR-0007 C baseline; open E3-mixed / full oracle E4 / convert-merge. **Not** dual-equality product freeze or R2-stable. |
| COL-007-ABS-MVP | Absolute v6 writer MVP (codec NONE EVENT; staged) | done (scaffold) | PR-B06: `nytp_sink_v6` absolute EVENT bodies + file-prefix + single codec-NONE chunk on close; fail-closed limits; unit vectors `test_v6_abs_wire`; IDs from lockfile. Schema [`docs/schemas/collector-v6-absolute-wire-mvp-v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/schemas/collector-v6-absolute-wire-mvp-v0.md). **Not** packing/dict; **not** board COL-007 done (E3-C = PR-B09); **not** wire freeze. Codecs/multi-chunk/CRC: **COL-007-CODEC-MVP**. |
| COL-007-CODEC-MVP | v6 writer codecs + multi-chunk + CRC | done (scaffold) | PR-B07: EVENT codecs NONE/ZLIB/ZSTD/LZ4; records-per-chunk multi-chunk seal; header + payload CRC32 sealed; `create_codec*`; unit vectors `test_v6_codec_chunk_crc`; Rust always-inflate dual-path via `decode_abs_c_mini --require-crc`. Schema [`docs/schemas/collector-v6-codecs-multi-chunk-crc-mvp-v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/schemas/collector-v6-codecs-multi-chunk-crc-mvp-v0.md). **Not** packing/dict/mid-stream codec switch; **not** board COL-007 done (E3-C = PR-B09); **not** wire freeze. Packing stage: **COL-007-PACK-MVP**. |
| COL-007-PACK-MVP | v6 packing + FOOTER dict + mid-stream region | done (scaffold) | PR-B08: ADR-0001 site-delta/`FLAG_HAS_SEQ` packing continuity across multi-chunk; `begin_codec_region` + empty START_DEFLATE; TIME_LINE_RUN; ADR-0002 FOOTER-local string dict; `create_opts`; unit vectors `test_v6_packing_footer`; Rust dual-path `decode_c_b08` / always-inflate packing+dict. Schema [`docs/schemas/collector-v6-packing-footer-dict-mvp-v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/schemas/collector-v6-packing-footer-dict-mvp-v0.md). **Not** board COL-007 done (E3-C = PR-B09); **not** wire freeze. |
| E3-C-EVENT-PRODUCT-MVP | Product E3-EVENT with real C bytes (absolute/packing/dict/mid-stream) | done | C-only fixtures under [`fixtures/v6/from-c/`](../fixtures/v6/from-c/); tests `e3_c_*` in `crates/nytprof-format-v6/tests/e3_c.rs`; `tools/oracle/e3_c_writer_parity.sh`; board COL-007 flipped. Mixed kinds: **E3-MIXED-RESIDUAL** **done (MVP)**. Wire freeze: **FMT-V6-WIRE-FREEZE** (done). |
| E3-MIXED-RESIDUAL | Multi-kind SOURCE/INDEX/SUMMARY product C fixtures | **done (MVP)** | C `mixed.nytprof` via `nytp_v6_sink_emit_{source,index,summary}`; `e3_c_mixed_*` decodes shipped always-inflate mixed path (`e3_decode_mixed_writer_bytes`). Truncated mixed fail-closed. **Not** TEST-008 / COL-008 / CLI v6 collection default / S2 / R3–R4. |
| COL-007 | C v6 writer (product E3-EVENT) | **done** | C `nytp_v6_sink_*` absolute + codecs/multi-chunk/CRC + ADR-0001 packing + ADR-0002 FOOTER dict + mid-stream region; committed C fixtures `fixtures/v6/from-c/**`; product E3 harness tests `e3_c_*` (C bytes only — never stand-in); smoke `./tools/oracle/e3_c_writer_parity.sh`; offline_gate step 11 when cargo; schema [`docs/schemas/collector-v6-e3-c-fixtures-mvp-v0.md`](schemas/collector-v6-e3-c-fixtures-mvp-v0.md). **Residuals:** CLI v6 default; full oracle E4 residual (TEST-008); live XS hooks; COL-008. E3-mixed MVP: `mixed.nytprof`. Wire freeze: **FMT-V6-WIRE-FREEZE** (done). |
| PRODUCT-V6-MODEL-INGEST-MVP | Product v6→ProfileModel dual dispatch + dump prelim | done | Schema [`docs/schemas/product-v6-profilemodel-ingest-mvp-v0.md`](schemas/product-v6-profilemodel-ingest-mvp-v0.md): `ProfileModel::from_path` / `from_bytes` magic detect (`NYTPROF6` vs `NYTProf 5`); always-inflate + FOOTER dict resolve; dump-aligned Event map; CLI `dump`/`verify` prelim on v6; tests C abs/packing/dict + stand-in pair aggregate parity. **Not** E3-mixed. Wire freeze: **FMT-V6-WIRE-FREEZE** (done). Required before full E5 report/html. E4-v0 uses this path (PR-B10). Full E5 surfaces: **CLI-E5-V6-OPT-IN-MVP**. |
| CLI-E5-V6-OPT-IN-MVP | CLI E5 v6 opt-in product surfaces + capability honesty | done | Schema [`docs/schemas/cli-e5-v6-opt-in-mvp-v0.md`](schemas/cli-e5-v6-opt-in-mvp-v0.md): report/html/csv/folded/callgrind/dump/verify on v6 via magic auto-detect; capability `v6_decode`/`v6_report` true; **`collection_default: v5`** (no default flip); tests `cli_e5_v6` + capability_selftest; smoke `capability_selftest_smoke.sh`. **History:** PR-B12 shipped `convert`/`merge` false; **superseded by PR-C01/C02 — capability convert/merge/repack/salvage true**. **Residuals:** collection default flip (R4); packing/string-dict convert. L01 `--allow-lossy` **done (MVP)**. E4 product: **E4-PRODUCT-CLI-SMOKE-MVP**. |
| COL-014-DUAL-SINK-MVP | Same-run dual writer **test/dev-only** (OQ-4) | done | Fan-out semantic sink → v5+v6 children; same-run logical equality (by_kind + seq/kind rings) on M4 mini + primary-fixture-shaped streams (default-calls1 / blocks-calls1 / calls2-default multiplicity patterns); env probe `NYTPROF_DUAL_SINK` / `NYTPROF_FORMAT=dual` (test/dev only — **not** product UX); out-of-band compare-meta JSON; tests `test_dual_sink`; schema [`docs/schemas/collector-dual-sink-mvp-v0.md`](schemas/collector-dual-sink-mvp-v0.md); smoke `collector_sink_smoke.sh`. **Residuals:** full fixtures/v5 oracle dual equality (TEST-003/TEST-008); COL-015 fork under dual; product `format=dual` **rejected** (OQ-4). E4-v0 model aggregates on dual pairs: **E4-V0-MODEL-SEMANTIC-MVP**. |
| COL-015-FORK-PID-MVP | Fork/PID harden with buffered sinks | done | Protocol `nytp_fork_prepare/resume_*` + policy/metrics; batch preflush on begin_fork + residual discard on child; addpid path helper; v5/v6 child reinit (clean stream, no inherited compressor); dual wire reinit; stress `test_fork_pid` (near-full buffer, nested depth, dual+batch, fail-closed preflush, POSIX fork addpid files). Schema [`docs/schemas/collector-fork-pid-mvp-v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/schemas/collector-fork-pid-mvp-v0.md). **Residuals:** full TEST-018 oracle forkdepth/addpid/merge corpus; live Perl/XS hooks; mid-deflate compressor continue-in-child vs 6.15; product option wiring; OI-003-05 file-switch. |
| COL-014-DUAL-SINK-MVP | Same-run dual writer **test/dev-only** (OQ-4) | done | Fan-out semantic sink → v5+v6 children; same-run logical equality (by_kind + seq/kind rings) on M4 mini + primary-fixture-shaped streams (default-calls1 / blocks-calls1 / calls2-default multiplicity patterns); env probe `NYTPROF_DUAL_SINK` / `NYTPROF_FORMAT=dual` (test/dev only — **not** product UX); out-of-band compare-meta JSON; tests `test_dual_sink`; schema [`docs/schemas/collector-dual-sink-mvp-v0.md`](schemas/collector-dual-sink-mvp-v0.md); smoke `collector_sink_smoke.sh`. **Residuals:** full fixtures/v5 oracle dual equality (TEST-003/TEST-008); product `format=dual` **rejected** (OQ-4); E4 aggregate enforcement residual. Fork under dual covered by **COL-015-FORK-PID-MVP**. |
| COL-002-LIFECYCLE-MVP | Sink lifecycle state machine + emit gates | done | Explicit `OPEN/ACTIVE/STOPPED/FINALIZING/CLOSED/FAILED/FORK_SPLIT`; `activate/stop/begin_finalize/begin_fork/end_fork_*/mark_failed`; emit gates; tests `test_lifecycle_seq`. Schema [`docs/schemas/collector-lifecycle-seq-fake-clock-mvp-v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/schemas/collector-lifecycle-seq-fake-clock-mvp-v0.md). COL-015 protocol/stress **done** (MVP); residual: full TEST-018 oracle + signal/file-switch (OI-003-05). |
| E4-01-ORACLE-PAIR-MVP | TEST-008 slice 1: one oracle dual pair beyond dual-sink | **done (MVP)** | [`fixtures/e4/oracle-pair/`](https://github.com/hilather/nytprof-modernization/blob/main/fixtures/e4/oracle-pair/) oracle default-calls1 v5 + product `format=v6`; `e4_v0_oracle_pair_*` + `e4_v5_v6_semantic_smoke.sh` compare shipped leaf/mid/edge. **Not** full TEST-008 / `format=dual` / S2 / CLI v6 collection default. |
| E4-02-ORACLE-PAIR-MVP | TEST-008 slice 2: second oracle dual pair (blocks-calls1) | **done (MVP)** | Same dir: oracle `blocks-calls1` v5 + product `format=v6` on the same workload. Shipped `from_path` / `report --json` leaf/mid/edge. **Not** full TEST-008 / A4 **780** live attach / opcode/`entersub` / `format=dual` / S2. |
| E4-03-ORACLE-PAIR-MVP | TEST-008 slice 3: third oracle dual pair (calls2-default) | **done (MVP)** | Same dir: oracle `calls2-default` v5 + product `format=v6` on the same workload. Shipped `from_path` / `report --json` leaf/mid/edge. **Not** full TEST-008 / SUB_ENTRY **27** live attach / `format=dual` / S2. |
| E4-V0-MODEL-SEMANTIC-MVP | E4-v0 model-level v5↔v6 aggregate equality | done | Schema [`docs/schemas/e4-v0-model-semantic-mvp-v0.md`](schemas/e4-v0-model-semantic-mvp-v0.md): `e4_v0_aggregates_equal` on dual-sink pairs under [`fixtures/e4/dual-sink/`](../fixtures/e4/dual-sink/); `cargo test -p nytprof-model e4_v0_`; smoke `./scripts/packaging/e4_v5_v6_semantic_smoke.sh --model-only`. Integer-tick SUB_RETURN/SUB_CALLERS for v5 NV↔v6 u64 equality. **Residuals:** full oracle dual corpus (TEST-003/TEST-008); product `format=dual` rejected. E4-01 count-surface pair: **E4-01-ORACLE-PAIR-MVP**. Product CLI path: **E4-PRODUCT-CLI-SMOKE-MVP**. Wire freeze: **FMT-V6-WIRE-FREEZE**. |
| E4-PRODUCT-CLI-SMOKE-MVP | E4 product CLI smoke in offline_gate (real CLIs on v5+v6) | done | Schema [`docs/schemas/e4-product-cli-smoke-mvp-v0.md`](schemas/e4-product-cli-smoke-mvp-v0.md): `./scripts/packaging/e4_v5_v6_semantic_smoke.sh --full` — model e4_v0_* + verify + `report --json` equality on all dual pairs + report/csv/folded/callgrind greps on default_calls1 both formats; `cargo test -p nytprof-cli e4_product_`; offline_gate step 12 when native CLI available (honest skip otherwise; fixtures still required). **Residuals:** full oracle dual (TEST-008); CLI v6 collection default (R4); product `format=dual` rejected. |
| TOOL-CONVERT-STRICT-MVP | Strict v5↔v6 convert CLI + capability claim | done | Schema [`docs/schemas/convert-strict-mvp-v0.md`](schemas/convert-strict-mvp-v0.md): `nytprof-cli convert --to=v5\|v6 IN -o OUT`; library `nytprof_model::convert_*`; v5 encoder `nytprof_format_v5::encode_all_as_v5`; absolute v6 EVENT output; fail-closed overflow / fractional NV→u64 / non-mantissa-exact NV / non-zero `sub_line` + extended `NEW_FID` (no silent zero); capability `convert: yes` with dual-sink m4 probe. Tests: `cargo test -p nytprof-model convert::`, `cargo test -p nytprof-cli --test convert_cli`. **Residuals:** packing/string-dict v6 out. Opt-in `--allow-lossy`: **TOOL-CONVERT-LOSSY-MVP**. Dual-sink blocks v5→v6 still refuse without `--allow-lossy`. |
| TOOL-CONVERT-LOSSY-MVP | Optional `--allow-lossy` convert (strict default) | **done (MVP)** | L01: `nytprof-cli convert --allow-lossy`; library `ConvertOptions { allow_lossy }`. Oracle default-calls1 strict refuse + lossy `NYTPROF6`. Smoke [`l01_lossy_convert_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/l01_lossy_convert_smoke.sh). **Not** default lossy / packing convert / TEST-008 / S2. |
| TOOL-MERGE-REPACK-SALVAGE-MVP | Merge / repack / salvage for v5/v6 (recovery semantics) | done | Schema [`docs/schemas/merge-repack-salvage-mvp-v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/schemas/merge-repack-salvage-mvp-v0.md): `nytprof-cli merge|repack|salvage`; library `nytprof_model::{merge_*,repack_*,salvage_*}`; v5 progressive `decode_salvage_prefix` (incomplete deflate unit discarded); v6 trailing-garbage / longest prefix; salvage always labels `nytprof.salvage.incomplete=1`; capability `merge`/`repack`/`salvage: yes` with dual-sink probe. Tests: `cargo test -p nytprof-model merge_tools::`, `cargo test -p nytprof-cli --test merge_repack_salvage_cli`. **Residuals:** not full `nytprofmerge` option parity (L02 aggregate-sum MVP is opt-in); not full SEC-003 multi-chunk matrix; packing/dict v6 out; no auto-salvage default. |
| TOOL-MERGE-AGGREGATE-SUM-MVP | Optional `--aggregate-sum` merge (concat default) | **done (MVP)** | L02: `nytprof-cli merge --aggregate-sum`; library `MergeMode::AggregateSum`. Same-filename `NEW_FID` remaps so A4/A5 combine. Smoke [`l02_aggregate_sum_merge_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/l02_aggregate_sum_merge_smoke.sh). Schema [`merge-aggregate-sum-mvp-v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/schemas/merge-aggregate-sum-mvp-v0.md). **Not** full nytprofmerge option parity / TEST-008 / S2. |
| COL-009 | Production v6 writer backend decision (COL-009) | **done** | ADR [`docs/adrs/0007-production-v6-writer-backend-c-baseline.md`](adrs/0007-production-v6-writer-backend-c-baseline.md): **reaffirm C (COL-007)** as production backend; **COL-008 remains deferred** non-baseline (no bake-off / BENCH-006 package opened). Format IDs stay backend-independent (ADR-0006). |
| R2-PREVIEW-READINESS-CUT | R2-preview release packaging + honesty sync | **done** | Release notes [`docs/RELEASE_NOTES_R2_PREVIEW.md`](RELEASE_NOTES_R2_PREVIEW.md); dual-equality readiness + residual matrix + runbook synced; advertise **opt-in only**; COL-007 already done at B09; convert/merge **residual**; dual-path legacy unchanged; **not** R2-stable / R3 / R4. PR-B13. |
| R2-STABLE-READINESS-CUT | R2-stable release packaging + honesty promotion | **done** | PR-C05: [`docs/RELEASE_NOTES_R2_STABLE.md`](RELEASE_NOTES_R2_STABLE.md); dual-equality + residual matrix + runbook; convert/merge/salvage + COL-015 MVP + SEC-FUZZ + P1/P2 methodology integrated; **not** R3/R4; public perf waived; E3-mixed / full oracle E4 residual. |
| R4-FIELD-WINDOW-PACK | R4 field-window instrumentation + report template (no runtime default flip) | **done** | **PR-E01:** guide [`docs/R4_FIELD_WINDOW.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/R4_FIELD_WINDOW.md); schema [`docs/schemas/r4-field-window-mvp-v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/schemas/r4-field-window-mvp-v0.md); report template [`docs/templates/R4_FIELD_WINDOW_REPORT.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/templates/R4_FIELD_WINDOW_REPORT.md); collector `./scripts/field/r4_field_window_collect.sh`; smoke `./scripts/field/r4_field_window_smoke.sh` (dual-sink default_calls1 leaf **15**/mid **3**; convert both ways; `no_default_flip=true`; `collection_default: v5`; residuals all false). **Not** charter R4 runtime flip; not in offline_gate. Prefer convert-capable CLI (target over stale prefix). Never `crates/` on oracle PERL5LIB. Promotion policy: **R4-DEFAULT-CHANGE-ADR**. |
| R4-DEFAULT-CHANGE-ADR | R4 default-format ADR + flip procedure (gated; no runtime flip) | **done** | **PR-E02:** [ADR-0008](https://github.com/hilather/nytprof-modernization/blob/main/docs/adrs/0008-r4-v6-output-default-promotion.md) **accepted (policy)** — `format=v6` product collection/output default on **eligible tiers** after accepted field **Promote**; **`format=v5` retained**; flip **not executed**. Procedure + rollback: [`docs/R4_DEFAULT_FLIP.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/R4_DEFAULT_FLIP.md). ADR-Q025 criteria answered; incomplete evidence → do not flip. **Not** charter R4 complete; **not** capability `collection_default: v6`; no Perl/Rust runtime change in this PR. |
| R5-RETIREMENT-GOVERNANCE-ADR | R5 retirement governance ADR + per-component review procedure (no removal) | **done** | **PR-F01:** [ADR-0009](https://github.com/hilather/nytprof-modernization/blob/main/docs/adrs/0009-r5-legacy-retirement-governance.md) **accepted (policy)** — per-component only; **never automatic**; **no component retired**. Procedure: [`docs/R5_RETIREMENT_REVIEW.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/R5_RETIREMENT_REVIEW.md). ADR-Q026 governance answered; REL-012 execution still deferred on evidence. Absence of retirement is valid success. **Not** deprecation timers; **not** silent removal; no runtime/packaging change. |
| R2-P1P2-METHODOLOGY | R2-scoped P1/P2 methodology (no public claims) | **done** | PR-C04: [`docs/BENCH_NOTES.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/BENCH_NOTES.md) P1/P2 gate map + promotion checklist; `tools/bench/light_bench.sh` `size` / `collector_micro`; residual matrix + runbook honesty — **not** certified public SLOs. |
| DROP-IN-DOD-V0 | Drop-in D1–D6 contract + residual IDs | **done (docs-landed)** | PR-G01: [`docs/contracts/DROP_IN_DOD_v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/contracts/DROP_IN_DOD_v0.md). **Not** live `-d:NYTProf`. |
| PRODUCT-OPTIONS-MATRIX | D1 options residual matrix | **done (docs + tests)** | Matrix in DROP_IN_DOD. G05 smoke [`g05_options_format_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/g05_options_format_smoke.sh): unknown + `format=dual` fail-closed; D1-B `format=v6` fail-closed (`v6_collect` rebuild text, no `NYTPROF6`); default/`format=v5` live attach leaf **15** / mid **3** / edge **15**; D1-A `xs-nytprof-v6` writes `NYTPROF6`. **Residuals:** full opcode, blocks-780, EL8 RPM v6 default. |
| G02-V5-PRODUCT-LINK | v5-only product archive + load-only XS | **done (scaffold)** | PR-G02: `make -C collector libnytp_sink_v5.a` + `probe-v5` (`-lz` only) + `Devel::NYTProf::CollectorBootstrap`. Smoke [`g02_v5_product_link_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/g02_v5_product_link_smoke.sh). Schema [`product-xs-attach-mvp-v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/schemas/product-xs-attach-mvp-v0.md). **Not** product attach. |
| G03A-LOAD-ONLY | Product `perl -d:NYTProf` loads; no TIME_* / profile emit | **done** | PR-G03a: `collector/xs/Devel/NYTProf.pm` + `Core.pm` + `NYTProf.xs`; `make -C collector xs-nytprof`; [`product_attach_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/product_attach_smoke.sh) drives real `-d:NYTProf`. `$PRODUCT_XS_LOAD=1`; `$PRODUCT_XS_ATTACH` stays 0. **Not** collection attach. |
| G03B-STMT-EMIT | Statement path `nytp_emit_time_line` / `time_block` / `discount` via product XS | **done** | PR-G03b: held v5 sink; `DB::enable_sink` + emit wrappers + `run_m4_mini_sample`; [`g03b_stmt_emit_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/g03b_stmt_emit_smoke.sh). Fake-clock mini + overflow fail-closed. `$PRODUCT_STMT_EMIT=1`. **Not** opcode attach / G04. |
| G03C-SUB-EMIT | Call path `nytp_emit_sub_entry` / `sub_return` via product XS | **done** | PR-G03c: `DB::emit_sub_entry` / `emit_sub_return` on the held v5 sink; [`g03c_sub_emit_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/g03c_sub_emit_smoke.sh). Real `NYTProf 5` mini + dump `SUB_ENTRY` / `SUB_RETURN`. `$PRODUCT_SUB_EMIT=1`. **Not** opcode attach / G04 fixture parity. |
| G03D-META-EMIT | Meta/finalize `nytp_emit_attribute` / `option` / `new_fid` / `src_line` / `sub_info` / `pid_start` / `pid_end` via product XS | **done** | PR-G03d: `DB::emit_*` on the held v5 sink; [`g03d_meta_emit_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/g03d_meta_emit_smoke.sh). Real `NYTProf 5` mini + dump ATTRIBUTE / OPTION / NEW_FID / SRC_LINE / SUB_INFO / PID_START / PID_END. `$PRODUCT_META_EMIT=1`. **Not** opcode attach / G04 fixture parity. |
| G03E-COMPRESS-EMIT | Compress path `nytp_emit_start_deflate` via product XS (zlib after `z`; `-lz` only) | **done** | PR-G03e: `DB::emit_start_deflate` / `is_deflating` on the held v5 sink; [`g03e_compress_emit_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/g03e_compress_emit_smoke.sh). Real `NYTProf 5` mini; dump/verify inflate recovers a post-deflate event. `$PRODUCT_COMPRESS_EMIT=1`. **Residual:** mid-deflate fork. Live attach is G04. |
| PRODUCT-XS-ATTACH-MVP | Live product collection attach (`-d:NYTProf` + default-calls1 15/3/15) | **done (MVP)** | PR-G04 + E1b: `NYTPROF file=` live attach. Default omit `entersub` installs `OP_ENTERSUB` (emit after INIT; `$^P` 0x01 off; `DB::sub` stub). wrap remains `wrap=1` / `use_db_sub=1` / `entersub=0`. TIME_LINE stays C `OP_DBSTATE` (PR-15; `$DB::single=0`). Smoke [`g04_v5_parity_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/g04_v5_parity_smoke.sh) — live `perl -d:NYTProf` on `fixtures/v5/default-calls1/workload.pl`; shipped dump/report: leaf **15** / mid **3** / mid→leaf **15**. G03a no-file= still writes no `nytprof.out`. G05 options/`format=v6` and G06 fork/`addpid` landed separately. **Residuals:** E2 `OP_GOTO` / E3 leave / E4 full `slowops.h`, `calls=2` SUB_ENTRY **27**, mid-deflate continue-in-child. DI-01 blocks **780/810** is PR-B1 (`di01_blocks_780_smoke.sh`). Not CPAN-TRIAL / EL8 RPM. |
| PRODUCT-FORK-ADDPID-MVP | Live `-d:NYTProf` `fork` + `addpid=1` | **done (MVP)** | PR-G06: `CORE::GLOBAL::fork` → shipped `nytp_fork_prepare` / `resume_parent` / `resume_child` + `nytp_fork_addpid_path` + `nytp_v5_sink_fork_child_reinit`. Smoke [`g06_fork_addpid_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/g06_fork_addpid_smoke.sh) — live `fork` with `file=` + `addpid=1`; parent `NYTProf 5` and `<file>.<childpid>` `NYTProf 5`. **Residuals:** mid-deflate continue-in-child, full TEST-018, `sigexit`/`_exit`. |
| PRODUCT-GETOPT-COMPILE-MVP | `use Getopt::Long` / Exporter / Time::HiRes compile under `-d:NYTProfM` | **done (MVP)** | PR-7: do **not** set `$DB::single=1` at `file=` enable; `INIT` sets it after compile. `DB::sub` `goto &$raw` for Exporter / Getopt / `vars` (workload subs keep hash-stack `&$raw` wrap). Smoke [`g07_getopt_compile_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/g07_getopt_compile_smoke.sh). **Residuals:** Rocky demo still core-only scanner (ack retry later); full opcode / leavesub. |
| PRODUCT-DATETIME-HINTS-MVP | `use DateTime` / `DateTime::Duration` compile under `-d:NYTProfM` | **done (MVP)** | PR-10: do **not** wrap `CORE::require`. Preload BHES / Variable::Magic / namespace::* and `CvNODEBUG` their CVs **before** `$^P` 0x01. `DB::sub` during `on_scope_end` (even `goto`) breaks `%^H` (`Can't use string ("#pod\\n") as an ARRAY ref` at `B/Hooks/EndOfScope/XS.pm`). Do **not** defer 0x01 to `INIT` (g04 `SUB_RETURN` → 0). Smoke [`g10_datetime_hints_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/g10_datetime_hints_smoke.sh). **Residuals:** full opcode / XSUB. |
| PRODUCT-NODEBUG-STASH-NOGP | `DB::nodebug_stash` must not SEGV on a GP-less stash GV | **done (MVP)** | PR-11: `GvCV` is `GvGP(gv)->gp_cv` — `isGV` is not enough. Walk via `isGV_with_GP` / `product_stash_val_cv`; skip `CvISXSUB` before treating `CvROOT` as an `OP*`. Smoke [`g11_nodebug_stash_nogp_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/g11_nodebug_stash_nogp_smoke.sh) plants a GP-less `SVt_PVGV` and drives shipped `DB::nodebug_stash` + live `-d:NYTProfM`. **Residuals:** full opcode / XSUB. |
| PRODUCT-MEMOIZE-CALLER-MVP | `Memoize::memoize('fn')` under `-d:NYTProfM` | **done (MVP)** | PR-12: `memoize` uses `caller` as the package of `fn`. Wrap ⇒ `Cannot operate on nonexistent function` (looks up `DB::fn`). Goto `Memoize::`. Smoke [`g12_memoize_caller_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/g12_memoize_caller_smoke.sh). **Residuals:** full opcode / other caller-sensitive CPAN. |
| PRODUCT-LOGGER-CALLER-MVP | App loggers must not report `NYTProfM.pm` as `caller` | **done (MVP)** | PR-13: do not `eval { &$raw }` — that eval frame is visible to `caller()` (Perl skips package-DB subs only). DESTROY still emits on die. Smoke [`g13_logger_caller_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/g13_logger_caller_smoke.sh). **Residuals:** full opcode. |
| PRODUCT-NESTED-EXCL-MVP | 3-level exclusive is incl − child inclusive | **done (MVP)** | PR-14: credit parent with child **incl**. `stmts=0` skips TIME_LINE. Smoke [`g14_nested_excl_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/g14_nested_excl_smoke.sh); g17 re-drives on default opcode. **Residuals:** DateTime/Moo/Rex `goto` still have no own SUB_RETURN (E2). |
| PRODUCT-DBSTATE-TIMELINE-MVP | Default `stmts=1` TIME_LINE from C `OP_DBSTATE` | **done (MVP)** | PR-15: `pp_product_dbstate_line` + last-COP fid pointer cache. `INIT` leaves `$DB::single=0` so Perl `DB::DB` is not entered. Do **not** hook NEXTSTATE/UNSTACK (that is `blocks=1`). Smoke [`g15_dbstate_timeline_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/g15_dbstate_timeline_smoke.sh). Default calls are E1b opcode; wrap remains `wrap=1` only. **Not** NEXTSTATE / leave / full `slowops.h`. |
| PRODUCT-WRAP-ENTER-MVP | wrap=1 escape site/clock/emit in C `wrap_push`/`wrap_pop` | **done (MVP)** | PR-16 + E1b: wrap assertions only under `wrap=1`. `NYTPROF_WRAP_SLOW=1` is nested under that escape. Default attach is opcode (g17). Smoke [`g16_wrap_enter_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/g16_wrap_enter_smoke.sh). **Not** E2 GOTO / stock 6.15 XS. |
| PRODUCT-APP-ATTR-MVP | HTML “Profile of …” is `$0`, not `Config_heavy.pl` | **done (MVP)** | Emit `ATTRIBUTE application=$0` (6.15). Treat `/lib64/perl` + `Config_heavy.pl` as INC. |
| PRODUCT-LEGACY-SMOKE | P-PRODUCT-LEGACY without cargo | **done (MVP)** | I01: [`install_product_xs.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/install_product_xs.sh) + [`product_legacy_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/product_legacy_smoke.sh) — cargo-free `make xs-nytprof` install into prefix `lib/perl5`; live `-d:NYTProf` + `file=` leaf **15** / mid **3** / edge **15**. Honest skip without CC/XS. **Not** BUILD-003-FULL / S2 dual_path rewrite / CPAN-TRIAL / EL8 RPM. |
| I03-DIST-SCRIPTS | Familiar report scripts + EngineDispatch prefix install | **done (MVP)** | I03: [`install_product_scripts.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/install_product_scripts.sh) + [`i03_dist_scripts_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/i03_dist_scripts_smoke.sh) — cargo-free EngineDispatch + `nytprofhtml`/`nytprofcsv` wrappers; installed `query --json --jsonl` leaf **15** / mid **3** / edge **15**. **Not** 6.15 nytprofhtml DOM / COMPAT-007 / BUILD-003-FULL / S2. |
| MIG01-MIGRATION-GUIDE | Operator drop-in migration guide | **done (docs)** | [`docs/MIGRATION_DROP_IN_v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/MIGRATION_DROP_IN_v0.md) — Option B `perl-NYTProfM` / `-d:NYTProfM`. **Not** CPAN-TRIAL upload / public COPR. |
| K03-PREBUILT-CLI-ADR | Signed CI prebuilt CLI policy | **done (docs)** | [ADR-0010](https://github.com/hilather/nytprof-modernization/blob/main/docs/adrs/0010-signed-ci-prebuilt-native-cli.md). K02 spec landed; pipeline residual. |
| PRODUCT-V6-COLLECT-EL8 | EL8 default RPM v6 collection | **residual** | Default EL8 = D1-B; D1-A via `--with v6_collect`. |
| BUILD-003-FULL | Full MakeMaker XS dual-build (`full_build003=1`) | **residual** | I01–I02. Depth MVP is not full BUILD-003. |
| J01-CPAN-HYGIENE | CPAN dist identity `NYTProfM` / `Devel::NYTProfM` **6.15** | **done (MVP)** | [`j01_cpan_hygiene_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/j01_cpan_hygiene_smoke.sh): real `Makefile.PL` MYMETA **NYTProfM** **6.15**; MANIFEST excludes `baseline/` `target/` `prefix/`. **Not** TRIAL upload. |
| CPAN-TRIAL-READY | CPAN TRIAL attach-preview notes | **done (notes-ready / MVP)** | J02: [`docs/RELEASE_NOTES_CPAN_TRIAL_v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/RELEASE_NOTES_CPAN_TRIAL_v0.md) + [`Changes`](https://github.com/hilather/nytprof-modernization/blob/main/Changes); smoke [`j02_cpan_trial_notes_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/j02_cpan_trial_notes_smoke.sh). Not uploaded to PAUSE. |
| EL8-RPM-MODULE | EL8 `perl-NYTProfM` D1-B spec | **done (MVP)** | K01: [`packaging/rpm/perl-NYTProfM.spec`](https://github.com/hilather/nytprof-modernization/blob/main/packaging/rpm/perl-NYTProfM.spec) + [`k01_el8_module_rpm_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/k01_el8_module_rpm_smoke.sh). D1-B zlib/no-cargo, **6.15**; **collection-only** (`Devel::NYTProfM` + XS + `nytprofm-cli`); no I03 `nytprofhtml` / no stock bindir overwrite; no self-Obsoletes, no `Provides: perl(Devel::NYTProf)`; G05 attach **15/3/15** + `format=v6` fail-closed. Testdrive unsigned RPM: [`build_el8_module_rpm.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/build_el8_module_rpm.sh) + [`release-el8-rpm.yml`](https://github.com/hilather/nytprof-modernization/blob/main/.github/workflows/release-el8-rpm.yml). **Not** mock-certified / D1-A default / signed COPR. |
| EL8-RPM-TOOLS | EL8 `nytprof-cli` companion spec | **done (MVP)** | K02: [`packaging/rpm/nytprof-cli.spec`](https://github.com/hilather/nytprof-modernization/blob/main/packaging/rpm/nytprof-cli.spec) + [`k02_el8_tools_rpm_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/k02_el8_tools_rpm_smoke.sh). Recommends module; ADR-0010 ingest; no rustup-in-mock. **Not** signed-pipeline complete / tools-alone drop-in. |
| ROCKY8-DOCKER-PROFILE-LAB | Rocky 8 Docker testdrive profile lab | **done (MVP)** | [`scripts/field/rocky8_docker_profile_demo.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/field/rocky8_docker_profile_demo.sh) + smoke [`rocky8_docker_profile_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/field/rocky8_docker_profile_smoke.sh). **Not** `offline_gate` / mock-certified / ack attach. |
| ADR-0011-HTML-OP-V1 | Native operator HTML v1 policy | **done (policy)** | [ADR-0011](https://github.com/hilather/nytprof-modernization/blob/main/docs/adrs/0011-native-operator-html-v1.md). Does **not** un-waive M01 jquery. |
| ADR-0012-HTML-OP-V2 | Native operator HTML v2 policy | **done (policy)** | [ADR-0012](https://github.com/hilather/nytprof-modernization/blob/main/docs/adrs/0012-native-operator-html-v2.md). Does **not** un-waive M01 jquery. |
| HTML-OP-V2 | Operator HTML v2 chrome / IA / sort / 6-col source | **done (MVP)** | Schema [`html-operator-v2-mvp-v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/schemas/html-operator-v2-mvp-v0.md). Dual-docker lab + call_sites (PR-7) residual. |
| LIVE-ATTACH-METRICS | Live incl/excl + elapsed TIME_LINE | **done (MVP)** | `nytp_clock_now` + attributed `TIME_LINE` + SRC_LINE/SUB_INFO at finish. g04 15/3/15, leaf incl > 0, verify OK. **PR-8:** stmt-ops `TIME_BLOCK` last-site ticks (di01 not identically 1) + PRINT/MATCH incl/excl (g08). **Still not** full opcode / `slowops.h`. |
| P01-GA-CANDIDATE | GA-candidate drop-in honesty cut | **done (MVP)** | [`RELEASE_NOTES_GA_CANDIDATE_v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/RELEASE_NOTES_GA_CANDIDATE_v0.md) + [`p01_ga_candidate_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/p01_ga_candidate_smoke.sh). **Not** final GA / SEC-012 complete / R3–R4. |
| P02-SEC-CUT | SEC-012 checklist + SEC-002 job MVP | **done (MVP / checklist / job)** | [`SEC_012_RELEASE_REVIEW_CHECKLIST_v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/contracts/SEC_012_RELEASE_REVIEW_CHECKLIST_v0.md) + [`p02_sec_cut_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/p02_sec_cut_smoke.sh). **Not** independent sign-off / full continuous fuzz / GA marketing / S2. |
| G01-DESIGN-LAND | Approved drop-in design copied into repo | **done** | [`docs/PRODUCT_COMPLETION_DROP_IN_v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/PRODUCT_COMPLETION_DROP_IN_v0.md) (rev 4). Annex: [`docs/schemas/product-xs-graft-annex-v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/schemas/product-xs-graft-annex-v0.md). |
| COL-008 | Batched Rust writer | deferred | Non-baseline; reaffirmed by COL-009 / ADR-0007 |
| NS-NYTPROFM-IDENTITY | Dist identity `NYTProfM` + `$VERSION` **6.15** + `-d:NYTProfM` | **done (MVP / Option B)** | CPAN `DISTNAME` **NYTProfM**, module **`Devel::NYTProfM`**, `$VERSION` **6.15**. Operators switch from `-d:NYTProf` (oracle / stock). Report facades stay `Devel::NYTProf::*`. **Not** PAUSE. Does not `Provides: perl(Devel::NYTProf)`. |
| DROP-IN-REMAINING | Remaining D1–D6 work after attach-preview MVP | **residual** | **DI-01** 780/810, **DI-02** 27+CORE:, **DI-04** mini kinds, **DI-08** sigexit, **DI-09** `compress=1` + slowops policy, **V5-DURABLE-SEAL-MVP** (`durable=1` opt-in) landed. **DI-03 opcode/`entersub` — in progress, not done** (E1b default omit installs `OP_ENTERSUB`, emit after INIT, `$^P` 0x01 off; `wrap=1` / `use_db_sub=1` / `entersub=0` escape; E2 GOTO / E3 leave / E4 full slowops / live di02 **27** residual). Still residual: `_exit` flush / mid-deflate-in-child / TEST-018; leave/findcaller; COMPAT-007; S2; `BUILD-003-FULL`; PAUSE; mock-certified EL8; signed CI pipeline; default-on durable; `aggregate=1`. Design: [`docs/DROP_IN_RPM_COMPLETION_v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/DROP_IN_RPM_COMPLETION_v0.md), [`docs/plan/DI03_OPCODE_ENTERSUB_ATTACH_v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/plan/DI03_OPCODE_ENTERSUB_ATTACH_v0.md). |
| DROP-IN-RPM-COMPLETION | Program completion design (drop-in + RPM) | **done (docs / rev 4)** | [`docs/DROP_IN_RPM_COMPLETION_v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/DROP_IN_RPM_COMPLETION_v0.md). PR-A0–A2 + A4 (Option B operator docs) + B1–B5 collection landed. A3 mock / A5 COPR residual. |
| ROCKY8-DEPLOYMENT-REMAINING | Remaining Rocky 8 collection-deploy design | **done (docs)** | [`docs/ROCKY8_DEPLOYMENT_REMAINING_v0.md`](https://github.com/hilather/nytprof-modernization/blob/main/docs/ROCKY8_DEPLOYMENT_REMAINING_v0.md). Critical path A4b → A3 → A5a. Not mock-certified / not COPR / not S2. |
| RPM-01-STAGED-DIST | Staged `NYTProfM-6.15.tar.gz` (not BUILD-003) | **done (MVP)** | [`make_nytprofm_dist.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/make_nytprofm_dist.sh) + [`rpm01_make_dist_smoke.sh`](https://github.com/hilather/nytprof-modernization/blob/main/scripts/packaging/rpm01_make_dist_smoke.sh). Unpacks and `make -C collector xs-nytprof`. **Not** BUILD-003-FULL / mock `%check`. |

## Explicitly out of first slice

- Default engine/format flips (R3/R4)  
- Full Perl API materializer parity  
- Legacy **component** retirement execution (REL-012) — governance **R5-RETIREMENT-GOVERNANCE-ADR** done; no removal  
- CLI v6 **collection** default / COL-008 (wire freeze **done**; E3-mixed **MVP** landed)  
- Packing-string-dict v6 convert targets (L01 `--allow-lossy` is opt-in; strict remains default)  
- Full `nytprofmerge` option / eval-fold / overflow parity (L02 aggregate-sum MVP landed; stream-concat remains default)  
- R2-stable residual depth after PR-C05 (E3-mixed, full oracle E4, public perf SLOs, R3/R4)

## Dependency sketch

```text
COMPAT-000 + ARCH-008 + BASE-001
    -> BASE-002/003 provisional
    -> FIXTURE-0 + comparator
    -> COMPAT-001 provisional
    -> COMPAT-002-NORM / COMPAT-003-PREC (dump normalize + numeric policy; not full freeze)
    -> Rust v5 reader -> model -> report MVP
    -> ADR-0004 (B0-A collector/) -> COL-001-SINK-MVP scaffold
    -> COL-002/003/004/005 scaffold (lifecycle, seq, fast path, batch)
    -> COL-006-V5-WIRE-MVP (real v5 via sink; mini samples)
    -> (later) complete TEST-003 corpus + v6 format + C writer + dual equality
```

## Update rule

When a row moves to `done`, link the evidence path in the Evidence column. Do not mark inventory rows `done` without an open-item list.
